const e='---\r\ntitle: C Sharp for Coding Interviews\r\ndescription: Collections, LINQ and language idioms a candidate needs to translate an algorithm into working C# quickly under interview pressure\r\ndifficulty: Core\r\ntags: [csharp, collections, linq]\r\n---\r\n\r\nKnowing the algorithm is only half the coding round — the other half is not losing five minutes to `.Count` versus `.Length`, or to a `Stack<T>` constructor that quietly reverses your input. This page is a fast lookup for the C# syntax, collection APIs and LINQ methods that come up constantly while solving DSA problems, organized so you can scan straight to the type you need instead of re-deriving it mid-interview.\r\n\r\n```mermaid\r\nflowchart TD\r\n    A["What do you need to store?"] --> B{"Access pattern?"}\r\n    B -- "Last in, first out" --> C["Stack&lt;T&gt;"]\r\n    B -- "First in, first out" --> D["Queue&lt;T&gt;"]\r\n    B -- "Both ends, O(1)" --> E["LinkedList&lt;T&gt;"]\r\n    B -- "Key to value" --> F["Dictionary&lt;K,V&gt;"]\r\n    B -- "Always sorted" --> G["SortedSet or SortedDictionary"]\r\n    B -- "Repeated min/max extraction" --> H["PriorityQueue&lt;T,P&gt;"]\r\n    B -- "Dynamic array, index access" --> I["List&lt;T&gt;"]\r\n```\r\n\r\n> [!KEY]\r\n> Arrays, `string` and `StringBuilder` use `.Length`; every other collection uses `.Count`. This single rule causes more compile-time friction under pressure than anything else on this page.\r\n\r\n## Setup, primitives and casting\r\n\r\n```csharp\r\nusing System;\r\nusing System.Collections.Generic;\r\nusing System.Linq;\r\nusing System.Text;\r\n\r\nclass Program\r\n{\r\n    static void Main()\r\n    {\r\n        string input = Console.ReadLine()!;\r\n        Console.WriteLine("output");\r\n    }\r\n}\r\n```\r\n\r\n| Type | Width | Range | Notes |\r\n|---|---|---|---|\r\n| `int` | 32-bit | `-2,147,483,648 .. 2,147,483,647` (~2.1e9) | default integer type |\r\n| `long` | 64-bit | `±9,223,372,036,854,775,807` (~9.2e18) | use once a sum can overflow `int` |\r\n| `double` | 64-bit IEEE 754 | ~15-17 significant digits | default floating type |\r\n| `decimal` | 128-bit | exact for base-10 fractions | rarely needed in DSA problems |\r\n| `char` | 16-bit UTF-16 | — | arithmetic works directly on it |\r\n\r\n```csharp\r\nint    x = (int)3.9;                    // 3 — truncates toward zero\r\nlong   y = (long)i * i;                 // cast BEFORE multiplying to avoid int overflow\r\nint    n = int.Parse("42");             // throws FormatException on bad input\r\nbool  ok = int.TryParse(s, out int v);  // preferred — returns false, never throws\r\nint  digit = c - \'0\';                   // \'7\' → 7\r\nchar back  = (char)(digit + \'0\');       // 7 → \'7\'\r\nint  idx   = c - \'a\';                   // \'c\' → 2 (lowercase letter → 0-based index)\r\n```\r\n\r\n> [!WARNING]\r\n> `int * int` overflows silently before the result is ever assigned to a `long`. Cast one operand to `long` *before* the multiplication (`(long)a * b`), not after — casting the finished product is too late.\r\n\r\n## Control flow, tuples and methods\r\n\r\n```csharp\r\nif (cond) { } else if (cond2) { } else { }\r\nvar x = cond ? a : b;                     // ternary\r\nswitch (x) { case 1: /* ... */ break; default: /* ... */ break; }\r\nfor (int i = 0; i < n; i++) { }\r\nforeach (var item in list) { }            // never add/remove during iteration\r\nwhile (cond) { }\r\ndo { } while (cond);\r\n```\r\n\r\n```csharp\r\n(int, int) p = (2, 5);\r\np.Item1;                                  // unnamed access\r\n\r\nvar t = (x: 5, y: 10);                    // named fields — preferred\r\nt.x;\r\n\r\nvar (mn, mx) = MinMax(arr);               // deconstruct into separate variables\r\n(a, b) = (b, a);                          // swap, no temp variable needed\r\n\r\nstatic int    Add(int a, int b) => a + b;                     // expression-bodied\r\nstatic void   Swap(ref int a, ref int b) { (a, b) = (b, a); } // ref mutates the caller\r\nstatic bool   Try(out int r) { r = 5; return true; }          // out must be set before return\r\nstatic (int, int) MinMax(int[] a) => (a.Min(), a.Max());      // tuple return\r\n\r\nFunc<int, int>  square = x => x * x;\r\nComparison<int> desc   = (x, y) => y.CompareTo(x);\r\nIComparer<int>  descC  = Comparer<int>.Create((a, b) => b.CompareTo(a));\r\n```\r\n\r\n## Classes, structs and records\r\n\r\n```csharp\r\nclass Node                     // reference type — assignment copies the reference\r\n{\r\n    public int Val;\r\n    public Node? Next;\r\n    public Node(int v) { Val = v; }\r\n}\r\n\r\nstruct Point                   // value type — assignment copies the whole struct\r\n{\r\n    public int X, Y;\r\n    public Point(int x, int y) { X = x; Y = y; }\r\n}\r\n\r\nrecord Point2(int X, int Y);   // immutable, value-equality by default (C# 9+)\r\n```\r\n\r\n## Arrays and strings\r\n\r\n```csharp\r\nint[] a = new int[n];               // all zeros\r\nint[] c = [1, 2, 3];                // collection expression (C# 12+)\r\nint len = a.Length;                 // arrays → Length; collections → Count\r\n\r\nArray.Sort(a);                                // in-place ascending (IntroSort)\r\nArray.Sort(a, (x, y) => y.CompareTo(x));      // descending\r\nArray.Reverse(a);\r\nArray.Fill(a, -1);\r\nint bs = Array.BinarySearch(a, 5);            // MUST be sorted; bs < 0 → ~bs = insertion point\r\n```\r\n\r\n```csharp\r\n// Rectangular int[,] — single allocation, O(1) access, but LINQ won\'t work on rows\r\nint[,] grid = new int[rows, cols];\r\ngrid[i, j] = 1;\r\nint r = grid.GetLength(0), c2 = grid.GetLength(1);\r\n\r\n// Jagged int[][] — rows are independent arrays; LINQ works, rows can be sorted/reversed\r\nint[][] jag = new int[rows][];\r\nfor (int i = 0; i < rows; i++) jag[i] = new int[cols];\r\njag[i][j] = 1;\r\n```\r\n\r\n```csharp\r\nstring s = "hello";\r\nstring sub = s.Substring(1, 3);            // (startIndex, length) → "ell", O(k)\r\nbool has = s.Contains("ell");\r\nint  idx = s.IndexOf(\'l\');                 // first match; -1 if not found\r\nstring[] parts = s.Split(\',\');\r\nstring joined = string.Join(",", parts);\r\nchar[] arr2 = s.ToCharArray();              // mutable copy\r\nstring rev2 = new string(s.Reverse().ToArray());     // LINQ Reverse on IEnumerable<char>\r\n\r\nbool eq  = s == t;                          // value equality — NOT reference (unlike Java)\r\nbool eqi = string.Equals(s, t, StringComparison.OrdinalIgnoreCase);\r\nint  lex = string.Compare(s, t, StringComparison.Ordinal);\r\n\r\nvar sb = new StringBuilder();\r\nsb.Append("hello");                 // O(1) amortised\r\nsb.Insert(0, "prefix");             // O(n) shift\r\nsb.Remove(2, 3);                    // (startIndex, count)\r\nsb.Length = 0;                       // fast clear — reuses the buffer\r\nstring result = sb.ToString();\r\n```\r\n\r\n## Collections\r\n\r\n| Remember by shape | Add | Read / remove |\r\n|---|---|---|\r\n| **List / Set** — general bag | `Add(x)` | `Contains(x)` / `Remove(x)` |\r\n| **Stack** — top | `Push(x)` | `Peek()` / `Pop()` |\r\n| **Queue** — line | `Enqueue(x)` | `Peek()` / `Dequeue()` |\r\n| **LinkedList** — two ends | `AddFirst/Last(x)` | `First/Last.Value` / `RemoveFirst/Last()` |\r\n| **Dictionary** — key → value | `map[k] = v` | `TryGetValue(k, out v)` / `Remove(k)` |\r\n| **PriorityQueue** — priority | `Enqueue(element, priority)` | `Peek()` / `Dequeue()` |\r\n\r\n```csharp\r\n// List<T> — dynamic array, O(1) amortised append\r\nvar list = new List<int> { 1, 2, 3 };\r\nlist.Add(1); list.Insert(0, 9);                 // Insert is O(n) — shifts right\r\nlist.RemoveAt(0); list.RemoveAll(x => x < 0);   // both O(n)\r\nlist.Sort(); list.Reverse();\r\nint bsResult = list.BinarySearch(5);            // MUST be sorted\r\n\r\n// Stack<T> — LIFO\r\nvar st = new Stack<int>(source);                // NOTE: reversed order from source!\r\nst.Push(1); int top = st.Peek(); int pop = st.Pop();\r\nbool ok = st.TryPop(out int v);                 // safe — false when empty\r\n\r\n// Queue<T> — FIFO\r\nvar q = new Queue<int>();\r\nq.Enqueue(1); int front = q.Peek(); int d = q.Dequeue();\r\n\r\n// LinkedList<T> — doubly linked, O(1) at both ends\r\nvar ll = new LinkedList<int>();\r\nll.AddFirst(1); ll.AddLast(2);\r\nLinkedListNode<int> node = ll.First!;\r\nvar next = node.Next; var prev = node.Previous;\r\n\r\n// HashSet<T> — unordered, O(1) average\r\nvar set = new HashSet<int>();\r\nbool added = set.Add(1);                        // false if already present — dedupe idiom\r\n\r\n// SortedSet<T> — sorted, no duplicates, O(log n)\r\nvar ss = new SortedSet<int>();\r\nint ssMin = ss.Min; int ssMax = ss.Max;          // O(log n)\r\n\r\n// Dictionary<K,V> — unordered, O(1) average\r\nvar map = new Dictionary<string, int>();\r\nmap["a"] = 1;                                    // add or overwrite\r\nbool got = map.TryGetValue("a", out int val);    // safe read\r\nmap[key] = map.GetValueOrDefault(key) + 1;       // frequency-counting idiom\r\n\r\n// SortedDictionary<K,V> — sorted by key, O(log n)\r\nvar sd = new SortedDictionary<int, int>();\r\nint sdFirst = sd.Keys.First(); int sdLast = sd.Keys.Last();\r\n\r\n// PriorityQueue<E,P> — min-heap (.NET 6+)\r\nvar pq = new PriorityQueue<int, int>();          // lowest priority dequeues first\r\nvar maxPq = new PriorityQueue<int, int>(Comparer<int>.Create((a, b) => b.CompareTo(a)));\r\npq.Enqueue(1, 3);\r\nbool deqOk = pq.TryDequeue(out int el, out int pri);   // safe\r\n```\r\n\r\n> [!TIP]\r\n> Every `Try...` method (`TryPop`, `TryDequeue`, `TryGetValue`, `TryPeek`, `TryParse`) exists specifically so you never have to wrap a collection operation in a `try/catch` to test for empty or missing — check the return value instead.\r\n\r\nNote that `new Stack<int>(source)` enumerates `source` and pushes each element, which means the *last* element of `source` ends up on **top** — iterating the resulting stack does not reproduce `source`\'s order. If order matters, reverse the source first or use a `List<T>` instead.\r\n\r\n## LINQ quick reference\r\n\r\nLINQ turns multi-line filter/transform/aggregate loops into one readable expression — useful for setup code in a solution, less often for the hot inner loop where a plain `for` avoids enumerator overhead.\r\n\r\n| Method | Example | What it does |\r\n|---|---|---|\r\n| `Where` | `nums.Where(x => x > 0)` | Filters to elements matching a predicate |\r\n| `Select` | `nums.Select(x => x * x)` | Projects/transforms each element |\r\n| `OrderBy` / `OrderByDescending` | `nums.OrderBy(x => x)` | Sorts, returns a new sequence (does not mutate) |\r\n| `GroupBy` | `words.GroupBy(w => w.Length)` | Buckets elements by a key selector |\r\n| `Aggregate` | `nums.Aggregate((a, b) => a + b)` | Left-to-right fold/reduce over the sequence |\r\n| `Sum` / `Min` / `Max` / `Average` | `nums.Sum()` | Standard numeric reduction |\r\n| `Any` / `All` | `nums.Any(x => x < 0)` | Existence / universal predicate check, short-circuits |\r\n| `First` / `FirstOrDefault` | `nums.First(x => x > 5)` | First match; `OrDefault` returns `default(T)` instead of throwing |\r\n| `Single` / `SingleOrDefault` | `nums.Single(x => x == target)` | Exactly one match; throws if zero or more than one |\r\n| `Distinct` | `nums.Distinct()` | Removes duplicate elements |\r\n| `Count` | `nums.Count(x => x > 0)` | Count of matches, or total count with no predicate |\r\n| `Take` / `Skip` | `nums.Take(3)` | First `n` elements / all but the first `n` |\r\n| `Zip` | `a.Zip(b, (x, y) => x + y)` | Pairs two sequences element-wise |\r\n| `SelectMany` | `rows.SelectMany(r => r)` | Flattens a sequence of sequences |\r\n| `ToDictionary` | `items.ToDictionary(x => x.Id, x => x.Name)` | Materializes into a `Dictionary<K,V>` |\r\n| `ToList` / `ToArray` / `ToHashSet` | `nums.Where(x => x > 0).ToList()` | Materializes a deferred query |\r\n\r\n```csharp\r\nvar evens = nums.Where(x => x % 2 == 0).ToList();\r\nvar squares = nums.Select(x => x * x).ToArray();\r\nvar total = nums.Aggregate(0, (acc, x) => acc + x);\r\nvar byLength = words.GroupBy(w => w.Length).ToDictionary(g => g.Key, g => g.ToList());\r\n```\r\n\r\n> [!NOTE]\r\n> LINQ queries are lazily evaluated until a terminal call like `ToList()`, `ToArray()`, `Count()`, or a `foreach` forces enumeration. Chaining several `Where`/`Select` calls costs nothing extra until that final materialization.\r\n\r\n## Graph representation and conversions\r\n\r\n```csharp\r\n// Adjacency list — sparse graphs, O(V + E) space\r\nList<int>[] g = new List<int>[n];\r\nfor (int i = 0; i < n; i++) g[i] = new List<int>();\r\ng[u].Add(v); g[v].Add(u);            // undirected\r\n\r\n// Weighted adjacency list\r\nList<(int to, int w)>[] wg = new List<(int, int)>[n];\r\nfor (int i = 0; i < n; i++) wg[i] = new List<(int, int)>();\r\nwg[u].Add((v, w));\r\n\r\n// Adjacency matrix — dense graphs, O(1) edge lookup, O(V^2) space\r\nint[,] mat = new int[n, n];\r\nmat[u, v] = 1;\r\n```\r\n\r\n| From | To | How |\r\n|---|---|---|\r\n| `IEnumerable<T>` | `List<T>` / `T[]` / `HashSet<T>` | `.ToList()` / `.ToArray()` / `new HashSet<T>(source)` |\r\n| `string` | `char[]` | `s.ToCharArray()` |\r\n| `char[]` | `string` | `new string(arr)` |\r\n| `Dictionary<K,V>` | keys / values / KVP list | `.Keys` / `.Values` / `.ToList()` |\r\n| Anything | `Dictionary<K,V>` | `source.ToDictionary(keySelector, valueSelector)` |\r\n| Anything | `Lookup` (allows duplicate keys) | `source.ToLookup(keySelector)` |\r\n\r\n## Cheat sheet\r\n\r\n- `.Length` for array/`string`/`StringBuilder`; `.Count` for every other collection.\r\n- `Try...` methods avoid exceptions on empty/missing — prefer them over bare `Pop`/`Dequeue`/indexers in loops.\r\n- Cast to `long` *before* multiplying two `int`s that might overflow, not after.\r\n- `new Stack<T>(source)` reverses order; `new Queue<T>(source)` does not.\r\n- `map[key] = map.GetValueOrDefault(key) + 1;` is the standard frequency-counting idiom.\r\n- `PriorityQueue<T,P>` is a min-heap by default; pass a reversed `Comparer<T>` for a max-heap.\r\n- `Array.Sort`/`list.Sort()` mutate in place; LINQ\'s `OrderBy` returns a new sequence and leaves the original untouched.\r\n- LINQ is lazily evaluated until `.ToList()`/`.ToArray()`/`.Count()`/`foreach` forces it.\r\n\r\n## Common mistakes\r\n\r\n| Mistake | Fix |\r\n|---|---|\r\n| Using `.Count` on an array or `.Length` on a `List<T>` | Arrays/`string`/`StringBuilder` → `.Length`; everything else → `.Count` |\r\n| Multiplying two `int`s and casting the result to `long` | Cast one operand to `long` before the multiplication |\r\n| Assuming `new Stack<int>(arr)` preserves `arr`\'s order | It pushes in enumeration order, so the last element ends up on top |\r\n| Calling `map[key]` for a possibly-missing key | Use `TryGetValue` or `GetValueOrDefault` to avoid a `KeyNotFoundException` |\r\n| Mutating a `List<T>` while iterating it with `foreach` | Collect changes separately, or iterate a copy/index-based `for` loop |\r\n| Expecting `Array.BinarySearch`/`list.BinarySearch` to work on unsorted data | Sort first — the result is undefined (and usually wrong) otherwise |\r\n| Chaining `OrderBy` and expecting the original list to be sorted | `OrderBy` returns a new sequence; assign it or call `.Sort()` for in-place mutation |\r\n| Forgetting `int[,]` rows can\'t use LINQ directly | Use a jagged `int[][]` if you need to `Select`/`Sort`/`Reverse` individual rows |\r\n\r\n## Summary\r\n\r\nMost C# friction in a coding interview comes from a handful of recurring decisions: which collection matches the access pattern you need, `.Length` versus `.Count`, `Try...` versus an exception-throwing call, and whether an operation mutates in place or returns a new sequence. None of it is conceptually hard — it just needs to be automatic so it doesn\'t eat into the time budget for the actual algorithm. LINQ is worth reaching for in setup and post-processing code where readability wins, but a hand-written loop is usually still the right call in a hot path where you\'re being asked about complexity. Memorize the shape-to-method table once and the rest — `Dictionary` for lookups, `PriorityQueue` for repeated extremes, `HashSet` for membership — falls out naturally from the problem\'s own vocabulary.\r\n\r\n## Top Interview Questions\r\n\r\n### Q1. What\'s the most common C#-specific mistake candidates make under interview time pressure, and how do you avoid it?\r\n\r\nMixing up `.Length` and `.Count` is the single most frequent one — arrays, `string`, and `StringBuilder` expose `.Length`, while `List<T>`, `Dictionary<K,V>`, `HashSet<T>`, and the other generic collections expose `.Count`. It\'s a compile error either way, so it doesn\'t cause a wrong answer, but it costs time fixing a red squiggle mid-explanation, which breaks your narration flow. The fix is just memorizing the rule as one fact — "fixed-size buffers use Length, growable collections use Count" — rather than re-deriving it by trial and error each time you switch collection types mid-solution.\r\n\r\n### Q2. Why would you choose `TryGetValue` over indexing directly into a `Dictionary<K,V>`?\r\n\r\nIndexing with `map[key]` throws a `KeyNotFoundException` if the key isn\'t present, which means you\'d need a `ContainsKey` check first (a second lookup) or a `try/catch` (expensive and awkward) to handle a possibly-missing key safely. `TryGetValue` does the existence check and the retrieval in a single lookup, returning a boolean and using an `out` parameter for the value, which is both faster and reads more clearly as "attempt to get this, and tell me if it worked." It\'s the same pattern as `TryParse`, `TryPop`, and `TryDequeue` — C#\'s general convention for "this operation might not succeed, and that\'s not exceptional."\r\n\r\n### Q3. What\'s the risk in constructing a `Stack<T>` directly from an existing collection, like `new Stack<int>(nums)`?\r\n\r\nThe constructor enumerates the source collection and pushes each element in that enumeration order, which means the *last* element pushed — the last element of the source — ends up on top of the stack. If your algorithm assumes the stack preserves the source\'s front-to-back order when popped, you\'ll get elements in reverse of what you expected, and it\'s an easy bug to miss because the code compiles and often looks correct in a small mental trace. If you need the source\'s order preserved when popping, reverse the source first, or reconsider whether a `List<T>` used as an explicit stack (via `Add`/`RemoveAt(Count-1)`) is clearer.\r\n\r\n### Q4. When is it appropriate to reach for LINQ in an interview solution, and when should you avoid it?\r\n\r\nLINQ is a good fit for setup, transformation, and post-processing code where clarity matters more than shaving constant-factor overhead — building a frequency map, filtering a collection before the main loop, or projecting a result at the end. It\'s a weaker choice inside the hot path of an algorithm you\'re being asked to optimize, both because chained LINQ calls carry enumerator overhead that a plain loop avoids, and because an interviewer evaluating your grasp of complexity may want to see the loop structure explicitly rather than have it hidden behind a one-line `Aggregate`. A reasonable rule: use LINQ where it makes the *supporting* code more readable, not where it obscures the core algorithm being evaluated.\r\n\r\n### Q5. Explain the difference between `list.Sort()` and `list.OrderBy(...)` and why mixing them up causes bugs.\r\n\r\n`list.Sort()` mutates the list in place and returns `void` — after calling it, `list` itself is sorted, and any variable already referencing it sees the sorted order. `list.OrderBy(...)` is a LINQ method that returns a **new** `IOrderedEnumerable<T>` without touching the original list at all; if you call `list.OrderBy(x => x);` and discard the return value, `list` remains exactly as unsorted as before, which is a common silent bug. The fix is either to assign the `OrderBy` result to a new variable (or call `.ToList()` on it) if you want a sorted copy, or call `.Sort()` directly if in-place mutation is what you actually want.\r\n\r\n### Q6. Why does `(long)a * b` behave differently from `(long)(a * b)` when `a` and `b` are both `int`?\r\n\r\n`a * b` is evaluated entirely in 32-bit `int` arithmetic first — if the true mathematical product exceeds `int.MaxValue`, it silently overflows (wraps around) before the cast to `long` ever happens, so `(long)(a * b)` can hold a `long`-typed but already-wrong value. `(long)a * b` instead casts `a` to `long` *before* the multiplication, which forces the entire multiplication to be promoted to 64-bit arithmetic, giving the mathematically correct result even when it wouldn\'t have fit in an `int`. This is a common silent source of wrong answers in problems involving products of large indices or values, and it\'s worth defaulting to the cast-before-multiply form whenever an overflow is even plausible.\r\n\r\n### Q7. What\'s the tradeoff between a jagged array (`int[][]`) and a rectangular array (`int[,]`) for a 2D grid problem?\r\n\r\nA rectangular array is a single contiguous memory allocation with `O(1)` access via `grid[i, j]`, which is slightly more cache-friendly and avoids the null-row bugs a jagged array can introduce if a row isn\'t initialized. A jagged array is an array of independent arrays, so it uses slightly more memory (one allocation per row) but gains full LINQ support per row (`.Sort()`, `.Reverse()`, `.Select()` on `jag[i]` directly) since each row is a genuine `int[]`, whereas you cannot LINQ over a single row of a rectangular `int[,]` without manually extracting it first. In practice, use rectangular arrays for simple fixed grids, and jagged arrays when you need to manipulate individual rows independently.\r\n\r\n### Q8. How would you implement a max-heap in C# given that `PriorityQueue<TElement, TPriority>` is a min-heap by default?\r\n\r\nPass a custom `IComparer<TPriority>` into the constructor that reverses the natural comparison order — `new PriorityQueue<int, int>(Comparer<int>.Create((a, b) => b.CompareTo(a)))` — so that what the priority queue treats as "smallest" is actually the largest value by the original ordering. An equivalent, commonly used trick if you don\'t want to build a custom comparer is to negate the priority values you enqueue with (`pq.Enqueue(x, -x)`), so the smallest negated priority corresponds to the largest original value; this works cleanly for numeric priorities but is less readable, so a reversed comparer is usually the clearer choice in an interview setting.\r\n\r\n### Q9. What does it mean that LINQ queries are "lazily evaluated," and why does that matter for correctness?\r\n\r\nA LINQ query like `nums.Where(x => x > 0).Select(x => x * x)` doesn\'t actually run any filtering or projection when that line executes — it builds up a description of the operations to perform, which only executes when something forces enumeration: a `foreach`, or a terminal call like `.ToList()`, `.ToArray()`, `.Count()`, or `.First()`. This matters for correctness because if the underlying collection changes between when you define the query and when you enumerate it, the query sees the collection\'s state *at enumeration time*, not at definition time — which can produce surprising results if you expected a snapshot. It also means chaining several `Where`/`Select` calls has no performance cost until you actually materialize the result, so it\'s safe to build up a readable multi-step query without worrying about redundant passes.\r\n\r\n### Q10. Why is `map[key] = map.GetValueOrDefault(key) + 1;` considered the idiomatic way to write a frequency counter in C#, instead of checking `ContainsKey` first?\r\n\r\n`GetValueOrDefault(key)` returns the existing value if the key is present, or `default(int)` (which is `0`) if it isn\'t, in a single lookup — so the whole line does "read-or-default, increment, write back" without ever needing a branch or a second dictionary access. The alternative, `if (map.ContainsKey(key)) map[key]++; else map[key] = 1;`, is functionally equivalent but requires two separate dictionary operations (`ContainsKey` then the indexer) in the common case, and is more code to get right under pressure. The one-liner form is also easy to adapt for non-`int` accumulation, such as `map[key] = map.GetValueOrDefault(key, someDefault) + increment;` when the default isn\'t zero.\r\n';export{e as default};
