const e={"ai-engineering/llm-fundamentals":`before you can design an llm feature you need a working mental model of what happens between "send prompt" and "receive text". this page covers the mechanics interviewers expect a backend engineer to know cold: tokens, context windows, sampling and the latency shape of a request. tokens, not words models don't see words or characters — they see tokens , sub word units produced by a tokeniser (bpe style for most gpt family models). a token is roughly 4 characters or ¾ of an english word on average, but this varies wildly by language and content. text approx. tokens note 1 common word, single token 3–4 split into sub words (tokyo, japanese) 2–4 non english is far less token efficient a uuid or hash 10–20 high entropy strings tokenise poorly 1,000 english words 1,300 tokens rule of thumb: words × 1.3 [!key] billing, rate limits and context limits are all measured in tokens , not characters or words. a prompt that "looks small" in a code editor can be expensive if it's full of json, code or non english text — all of which tokenise less efficiently than prose. this directly drives cost: if input tokens cost $3/million and output tokens cost $15/million, a request with a 4,000 token retrieved context and a 200 token answer costs mostly on the input side. trimming unnecessary context (boilerplate, repeated system prompts) is a real cost lever, not a micro optimisation. the context window the context window is the maximum number of tokens the model can attend to in one call — input and output combined. everything the model "knows" for this request must fit inside it: the system pro`,"ai-engineering/prompting-and-structured-output":`once you move from "chat with a model" to "ship a feature", prompting becomes an engineering discipline: you need output your code can parse every time, not most of the time. this page covers prompt structure, structured output contracts and the defensive engineering around them. anatomy of a production prompt a prompt that needs to work reliably in production, not just in a demo, generally has six distinct parts: part purpose example role frames the model's persona and authority "you are a support ticket triage assistant." task the single, explicit instruction "classify the ticket into one of the categories below." context facts the model needs but doesn't have the ticket text, account tier, prior ticket history constraints boundaries and things to avoid "never invent a category not in the list." format the exact shape of the expected output "respond with json matching this schema." examples worked input/output pairs 2–3 representative examples with edge cases [!key] separating these six parts isn't cosmetic — it lets you version and test each one independently. a regression in "format" shouldn't require re validating "task" logic, and vice versa. zero shot, few shot, chain of thought technique what it is cost best for zero shot instruction only, no examples cheapest, fastest simple, well known tasks (sentiment, basic extraction) few shot 2–5 worked examples included in the prompt more input tokens tasks with a specific format or edge case behavior that's hard to describe in words chain of thought (cot) ask the model to reason step by step before answering more output toke`,"ai-engineering/tool-calling":`tool calling is what turns an llm from a text generator into something that can act — query a database, call an internal api, write a file. the mechanism is simpler than it looks, and interviewers care most about how you handle the parts that go wrong: bad schemas, failures, and unsafe side effects. how tool calling actually works the model never executes anything itself. it only ever emits structured text describing an intended call — a tool name and a json object of arguments. your application code is entirely responsible for actually running that call and feeding the result back in. [!key] say this clearly in an interview: "the model decides what to call and with what arguments — it never runs code. my backend is the thing that actually executes the call, and it's my backend's job to validate, authorize, and sandbox that execution before trusting the result back to the model." this means every tool call is a round trip : one api call to get the intended tool call, your own execution, then a second api call with the result appended to the conversation so the model can produce (or chain into) a final answer. multi step agents just repeat this loop. tool schema design the schema is the model's only information about what a tool does and when to use it — treat it like api documentation for a very literal minded caller. design factor why it matters tool name should read like a verb noun action; ambiguous names get misused or ignored description the single biggest driver of correct selection — state what it does and when to use it (and when not to) parameter types enums over f`,"ai-engineering/embeddings-and-vector-search":`embeddings are the foundation every rag and semantic search feature is built on, and interviewers use them to test whether you understand what's actually happening beneath a vector database's api — not just that you can call . what an embedding is an embedding is a fixed length vector of floating point numbers produced by a model, positioned in a high dimensional space such that semantically similar inputs end up close together and dissimilar inputs end up far apart. two sentences with completely different words but the same meaning ("the car wouldn't start" / "my vehicle failed to turn on") land near each other; two sentences with overlapping words but different meaning land far apart. [!key] an embedding model doesn't store meaning symbolically — it compresses semantic similarity into geometric distance. "search" becomes "find nearby points," which is why the whole field runs on nearest neighbour algorithms rather than keyword matching. typical dimensionality ranges from 256 (small, fast models) to 3,072 (large, high fidelity models); more dimensions generally capture finer semantic distinctions at the cost of storage and query time. similarity metrics metric formula intuition sensitive to magnitude? typical use cosine similarity angle between two vectors no — normalises out length default for most text embedding models dot product sum of element wise products yes — larger magnitude vectors score higher fast (no normalisation step); correct when vectors are pre normalised, then equivalent to cosine euclidean (l2) distance straight line distance between points yes common i`,"ai-engineering/chunking-strategies":"chunking is the least glamorous part of a rag system and the one most likely to quietly cap its quality. get it wrong and no amount of prompt tuning or reranking downstream fixes it — retrieval can only return what was chunked sensibly in the first place. why chunking exists two hard constraints force documents to be split before embedding and retrieval: 1. embedding models have an input limit (often 512–8,000 tokens) — you cannot embed an entire 50 page document as one vector without losing most of its specific detail into a single averaged representation. 2. retrieval needs precision — if a whole document is one chunk, a query about one paragraph pulls in the entire document's noise, wasting context window budget and diluting the signal the generator needs. [!key] the chunk is the unit of retrieval. whatever granularity you chunk at is the granularity your search can ever return — chunk too coarse and you retrieve noise, chunk too fine and you lose the surrounding context a correct answer needs. chunking strategies compared strategy how it splits strength weakness fixed size every n tokens/characters, regardless of content simple, predictable, fast cuts mid sentence or mid idea; ignores structure entirely sentence based splits on sentence boundaries, groups to a target size never splits mid thought loses paragraph level context; many small chunks paragraph based splits on paragraph breaks preserves a coherent unit of thought paragraph length varies wildly by source, hard to size bound semantic embeds sentences, splits where semantic similarity drops between neighbours spl","ai-engineering/rag-architecture":`rag (retrieval augmented generation) is the default architecture for grounding an llm in your own data. interviewers use it to test whether you can reason about a multi stage system end to end — where the latency goes, where it fails, and why you didn't just fine tune instead. the full pipeline rag has two halves that run at very different times: an offline ingestion path that runs whenever content changes, and an online query path that runs on every user request. [!key] say the two halves explicitly in an interview: "ingestion happens whenever content changes and is not latency sensitive; the query path happens on every user request and is where your latency budget and failure modes actually live." conflating the two is a common junior mistake. why rag instead of fine tuning both approaches inject knowledge the base model doesn't have, but they solve different problems. factor rag fine tuning freshness update the index, effective immediately requires retraining/re tuning to reflect new facts cost to update cheap — re embed changed documents expensive — a training run per update cycle attribution / citation natural — you know which chunk was retrieved not possible — knowledge is baked into weights, no source trace effort to stand up lower — mostly plumbing (index, retrieval, prompt) higher — data curation, training infra, eval best for knowledge that changes, needs sourcing, or is too large to memorize teaching a behavior , style , or format , not new facts hallucination risk on ungrounded questions lower — can say "not found" and refuse unchanged — fine tuning doesn't add `,"ai-engineering/retrieval-quality-and-reranking":`retrieval quality is the ceiling on everything a rag system can do — a perfect generator fed the wrong chunks still produces a wrong answer. this page covers why pure vector search isn't enough, the standard fixes, and the single debugging skill that separates people who've actually run a rag system in production from people who've only read about it. why vector search alone underperforms semantic (vector) search excels at matching meaning but is often weaker on exact terms — product codes, acronyms, error messages, proper nouns, or numbers — because embeddings compress specific tokens into a general semantic representation, and two strings differing only in an exact identifier can end up very close in embedding space even though only one is actually correct. [!warning] a query for error code can retrieve chunks about error handling in general far more readily than the one chunk that specifically documents , because the embedding model represents the query's overall semantic gist ("an error code lookup") rather than treating the exact string as a hard constraint. pure vector search structurally struggles with this class of query. keyword vs vector vs hybrid approach strength weakness keyword / bm25 exact term matches (codes, names, acronyms, rare terms) score highly and predictably misses semantically related content with no shared vocabulary vector / semantic finds conceptually related content regardless of exact wording weaker on exact identifiers, numbers, rare or out of distribution terms hybrid combines both signals, covering each approach's blind spot more moving part`,"ai-engineering/ai-agents-fundamentals":`"agent" gets used for everything from a single prompt template to a multi day autonomous system, and interviewers will probe whether you know the difference. the useful definition is about control flow, not vocabulary. what makes something an agent a chatbot maps one input to one output through a fixed prompt — no branching, no tools. a workflow (or chain) executes a fixed, developer defined sequence of steps, possibly calling an llm at each step, but the order and choice of steps is decided by code. an agent is a system where the llm itself decides, at runtime, which action to take next, based on what it observes — the model controls the control flow, not just the content. property chatbot workflow / chain agent decides next step developer (fixed) developer (fixed sequence) model (dynamic) can call tools rarely yes, in fixed order yes, in model chosen order number of steps 1 fixed, known upfront variable, unknown upfront predictability high high lower failure mode wrong answer wrong step output wrong step choice [!key] the interview winning line: "an agent is defined by autonomy over control flow — the model decides what to do next, not just what to say." everything else (tools, memory, loops) is implementation detail on top of that one property. the react loop most agents are built on react (reason + act): the model produces a thought, chooses an action (usually a tool call), observes the result, and repeats until it decides it has enough information to answer. the "reason" step is usually just the model's own chain of intermediate text before it emits a tool call — you d`,"ai-engineering/agent-memory-and-planning":'an agent without memory re derives everything from scratch on every step and forgets what it tried five minutes ago; an agent without planning reacts one step at a time and stumbles into avoidable dead ends. both are engineering problems with well known solutions, not model magic. memory types in agent systems "memory" for an llm agent is not one thing — different memory types solve different problems and are implemented completely differently. memory type what it holds typical implementation lifespan short term (context window) the current conversation and recent tool results messages array sent with every model call one session, until it overflows working / scratchpad intermediate reasoning, partial results for the current task a structured buffer or "notes" field written by the model mid task one task episodic specific past interactions or task runs logged transcripts, keyed by session or task id days to indefinite semantic / vector facts, documents, general knowledge embeddings in a vector store, retrieved by similarity indefinite procedural learned skills, successful strategies, tool use patterns distilled into prompts, few shot examples, or fine tuning indefinite [!key] only short term and working memory live in the prompt you send. episodic and semantic memory live outside the model entirely — in a database or vector store — and are pulled into the context window on demand. conflating "memory" with "context window" is the most common mistake in agent design interviews. managing the context window context windows are large but not infinite, and cost scales with tokens',"ai-engineering/multi-agent-and-guardrails":"multiple agents sound like an easy way to scale capability, but every extra agent is another model call, another failure surface, and another thing that can act on the world unsupervised. this section covers how to coordinate agents deliberately and how to keep any agentic system from doing something expensive or dangerous. orchestration patterns pattern how it works best for main risk supervisor / router one agent decides which specialist agent handles the request clearly separable domains (billing, support, sales) router misclassifies, wrong specialist engaged sequential pipeline fixed order, output of one agent feeds the next multi stage transforms (draft → edit → fact check) errors compound down the chain parallel fan out + aggregation same task sent to several agents/models, results merged or voted redundancy, ensembling, speed via parallelism aggregation logic itself becomes a failure point debate two or more agents critique each other's answer before a final decision reducing single model blind spots on hard reasoning cost multiplies; can converge on shared wrong answer hierarchical a manager agent decomposes a task and delegates subtasks to worker agents large decomposable projects (research, codegen) coordination overhead, manager becomes a bottleneck [!key] multi agent is an orchestration decision, not a capability upgrade. every pattern above can be built with a single well designed agent and good tools — the question to answer before adopting one is what specifically the split buys you: isolation, parallelism, or specialisation. agent to agent handoff and shared","ai-engineering/model-context-protocol":"every team building llm tools eventually reinvents the same wiring — how the model discovers what a tool does, how arguments get validated, how results come back. the model context protocol (mcp) standardises that wiring so it is built once, per integration, instead of once per model provider. what mcp solves: the n×m problem without a standard, every ai application that wants to use external tools has to write custom integration code for every tool it supports, and every tool provider has to write custom integration code for every ai application that might call it. with m applications and n tools, that is up to n×m bespoke integrations. mcp defines one protocol both sides implement once: an application becomes an mcp client and a tool becomes an mcp server , and any client can talk to any server without custom glue code. [!key] mcp is to tool integration what a database driver interface is to databases — the application code doesn't change per database, and here the agent code doesn't change per tool provider. that reduction from n×m to n+m bespoke pieces of work is the entire value proposition. client server architecture an mcp host (the ai application — an ide, a chat app, an agent runtime) runs one or more mcp clients , each maintaining a stateful, one to one connection to an mcp server — a lightweight process that exposes a specific integration (a filesystem, a database, a saas api). communication is json rpc 2.0 over a chosen transport. the server owns the actual integration logic and credentials; the client and host never talk directly to the underlying system, which","ai-engineering/evaluating-llm-systems":'unit tests do not catch a model that got 3% worse at extracting dates after a prompt tweak. evals are the discipline that replaces "it feels better" with a number you can put in a pull request, and they are the single biggest predictor of whether an llm feature survives contact with production. why you cannot ship without evals llm output is non deterministic, sensitive to prompt wording, and silently degrades when the underlying model, retrieval index, or a single system prompt line changes. without evals you have no signal until a customer complains. with evals, a prompt change or model upgrade becomes a diffable, reviewable artifact: "context recall dropped from 0.91 to 0.78, do not merge." [!key] an eval suite is a regression test suite for behaviour that cannot be asserted with . treat it with the same seriousness as unit tests — run it in ci, block merges on it, and version it alongside the prompt. offline vs online evaluation offline evals run against a fixed dataset before deploy; online evals watch real traffic after deploy. you need both — offline is fast and safe but cannot see distribution shift in real user queries, online is realistic but only tells you something is wrong after users hit it. offline eval online eval data golden/curated dataset live production traffic when pre merge, ci, nightly post deploy, continuous signal precise, repeatable noisy, real world catches known regressions unknown unknowns, drift feedback loop minutes hours to days examples assertion suite, judge scoring thumbs up/down, a/b, alerts building a golden dataset a golden dataset is a',"ai-engineering/ai-system-design-patterns":`most llm system design questions are really asking you to recognise which of a small set of recurring patterns fits the problem, and to name the trade off it makes. this page is a catalogue you can pull from directly in a design interview. control flow patterns router / classifier front door : a cheap, fast classification step (often a small model or even a non llm classifier) decides which specialised prompt, model, or downstream system handles a request, instead of one generic prompt trying to handle everything. prompt chaining : break one complex task into an explicit sequence of smaller llm calls, each with a focused prompt, where step n's output feeds step n+1 — trades latency and cost for reliability, since each step is easier to get right and easier to validate in isolation. parallelisation with voting : run the same task through multiple calls (different prompts, temperatures, or models) and combine results by majority vote or an aggregator — improves reliability on tasks with a single correct answer by averaging out individual call variance, at n times the cost. evaluator optimiser loop : one call generates a candidate, a second call (or a deterministic check) evaluates it against criteria, and generation is retried with the critique fed back in until it passes or a retry budget is hit — trades latency for measurably higher quality on tasks where "good" is checkable. [!key] every control flow pattern above is trading cost/latency for reliability in a specific, nameable way. the interview answer that scores well says which axis you're trading and why the task justif`,"ai-engineering/ai-system-design-case-studies":"design interviews for ai features reward the same discipline as any system design round — requirements, architecture, data flow, trade offs — applied to problems where retrieval quality, hallucination and per call cost are first class constraints. these four worked designs are the ones that come up most often. case study 1: enterprise document q&a over a private corpus requirements answer employee questions grounded in an internal document corpus (policies, contracts, wikis) spanning millions of pages, where every document has its own access permissions and a user must never see an answer built from a document they cannot open directly. freshness matters — a policy updated this morning should be reflected within minutes, not the next reindex cycle. architecture data flow and key decisions documents are chunked and embedded at ingest time, but the access control list for each chunk is stored as metadata and enforced at query time rather than baked into a per user index, which would not scale and would go stale immediately. the retriever applies a permission filter, drawn from the user's live entitlements, before similarity search — a restricted document never competes for the top k slots, and never appears in a trace a lower privileged session might see. decision choice why permission enforcement point pre filter at retrieval, re verify at generation never let a restricted chunk reach the prompt at all freshness incremental re index on document change webhook minutes level freshness without full reindex cost citations require the model to cite chunk ids, verify citations exi","azure/azure-fundamentals":`before an interviewer lets you talk about services, they check whether you understand the scaffolding those services live in. this page covers the hierarchy, the control plane, identity scopes and the cost model — the vocabulary every "why did you choose azure" conversation is built on. the resource hierarchy everything in azure sits inside a four level tree. getting the order right, and knowing what each level is for , is the first thing a senior interviewer probes. level purpose typical boundary tenant one entra id directory, one organisation identity boundary management group groups subscriptions for policy and rbac at scale governance boundary subscription billing container, hard quota limits, deployment boundary billing + isolation boundary resource group lifecycle container — resources that deploy/delete together lifecycle boundary resource the actual thing — a vm, a database, a queue — [!key] a resource group is a lifecycle boundary, not a network or security boundary. resources in different resource groups can share a vnet; resources in the same resource group can be in different regions. don't imply otherwise in an interview. regions, availability zones, paired regions a region is a set of datacentres with low latency networking between them. an availability zone (az) is a physically separate datacentre (own power, cooling, network) inside a region — using 2–3 zones protects against a datacentre level failure, not a regional one. a paired region is a pre defined partner region (e.g. east us ↔ west us) used for platform level disaster recovery and staggered platform`,"azure/networking-and-private-endpoints":'networking questions are where "explain your architecture" interviews separate people who deployed resources from people who understand isolation. this page covers vnet design, the service endpoint versus private endpoint decision that trips up most candidates, dns resolution, and connectivity options. vnets, subnets, and cidr planning a virtual network (vnet) is an isolated network boundary within a region; subnets partition it into smaller ranges, each of which can carry different nsgs, route tables, and service delegations. azure reserves the first four and last one address in every subnet (network id, default gateway, two dns reserved, broadcast), so a subnet gives you 251 usable addresses, not 256. [!key] plan cidr ranges for the whole company before the first vnet is deployed, not per project. overlapping address spaces are the single biggest reason vnet peering and hub and spoke migrations get expensive later — you cannot peer two vnets with overlapping ranges. planning concern guidance size subnets for growth leave headroom — resizing a subnet with resources already deployed is disruptive reserve ranges centrally a shared ipam spreadsheet or tool prevents two teams claiming the same delegate subnets per service some paas (e.g. vnet integrated app service, azure firewall) require a dedicated, delegated subnet keep a gateway subnet vpn/expressroute gateways require a subnet literally named nsgs vs azure firewall vs application security groups these three operate at different layers and are often used together, not as alternatives. control layer scope stateful typical ',"azure/traffic-routing-and-load-balancers":`every azure system design interview eventually asks "how does traffic get to your service", and there are four different answers depending on the layer. picking the wrong one — or not knowing why you picked the right one — is an easy way to lose credibility after a strong start. the four options at a glance service layer scope key features typical use azure load balancer l4 (tcp/udp) regional (or zonal) ultra low latency, health probes, no waf/ssl offload internal traffic between tiers, non http protocols, vmss backend balancing application gateway l7 (http/https) regional path/host based routing, ssl offload, waf (owasp rules), cookie based session affinity single region web app needing routing rules and waf azure front door l7, global edge (anycast) global global http routing, waf at the edge, caching (cdn), url rewrite, header based routing, health probe failover across regions multi region web apps, global audiences, ddos resilient edge traffic manager dns level global no data plane visibility — just resolves a hostname to an endpoint ip based on policy non http global routing, or as a coarse failover layer above services front door can't front [!key] the layer determines what the service can see and act on. load balancer sees packets, so it can't route by url path. traffic manager only ever answers a dns query, so it can't inspect headers, terminate tls, or run a waf — it just tells the client which ip to try next. azure load balancer (l4) distributes tcp/udp connections across a backend pool (vms, vm scale sets, or containers) using a 5 tuple hash, with health probes `,"azure/identity-managed-identity-and-key-vault":`"how do your services authenticate to each other and to data stores, and where do the secrets live" is one of the highest value questions in an azure interview, because the honest answer usually reveals whether a candidate actually removed connection strings or just says they did. this page covers entra id, service principals versus managed identities, and key vault integration end to end. microsoft entra id basics microsoft entra id (formerly azure ad) is azure's identity provider — a directory of users, groups, and application identities, issuing oauth 2.0/oidc tokens that azure services trust. every azure tenant has exactly one entra id directory. two identity types matter for service to service auth: identity type what it represents has credentials you manage? service principal (app registration) an application's identity, usable across tenants, often backing a ci/cd pipeline or third party app yes — client secret or certificate, which you create, store, and rotate managed identity an identity tied to the lifecycle of an azure resource no — azure creates, stores and rotates the credential for you service principals vs managed identities a managed identity is really a service principal that azure manages for you — same underlying object in entra id, but the credential lifecycle is invisible and automatic. aspect system assigned managed identity user assigned managed identity lifecycle tied 1:1 to the resource — created and deleted with it standalone azure resource, created independently reuse across resources no — one identity per resource yes — assign the same identity `,"azure/app-service-and-functions":'compute choice is usually the first question in an "explain your architecture" interview, and "we used app service because it was easy" is a weak answer. this page covers app service scaling and slots, functions triggers/bindings and cold starts, durable functions, and how to justify functions versus app service versus containers. app service plans and scaling an app service plan is the underlying set of vm instances (a specific sku/size) that one or more app service apps run on; the plan, not the app, is what you scale. scaling is either vertical (scale up — bigger sku, more cpu/ram per instance) or horizontal (scale out — more instances), and horizontal scaling can be manual, schedule based, or metric based autoscale (cpu%, memory, queue length). tier use case key limits free/shared prototypes only no custom domain ssl, shared compute, low quotas basic dev/test no autoscale, no slots standard small production workloads 5 slots, autoscale, daily backups premium v3 production 20 slots, vnet integration, higher scale out limits, faster cpus isolated (app service environment) regulated/high isolation workloads dedicated deployment inside your own vnet, single tenant [!key] everything in the same app service plan shares the same instances and scales together. putting a cpu hungry background app in the same plan as a latency sensitive api means a spike in one starves the other — split them into separate plans when their scaling profiles differ. deployment slots and slot swap with warm up a deployment slot is a live, separately addressable copy of the app running in the same pla',"azure/aks-and-containers":"aks questions test whether you understand the operational cost you're taking on, not just the yaml. this page covers aks architecture, the decision against app service/container apps, autoscaling layers, node pool design, and how pods authenticate to azure without stored credentials. aks architecture azure manages the control plane (api server, etcd, scheduler, controller manager) for free — you don't see or patch those vms. you manage the node pools — the actual vms that run your pods. a cluster always has a system node pool (runs core cluster components like coredns, metrics server, and tunnel/proxy agents) and typically one or more user node pools for your application workloads. component who manages it notes control plane (api server, etcd) azure free on the standard/free tier tiers, paid uptime sla on the standard tier system node pool you (vms), azure (scheduling core components) should be isolated from app workloads via taints user node pool(s) you where your workloads actually run; can scale to zero [!key] isolate the system node pool with a taint ( ) so application pods can't land there and starve cluster critical components — a very common finding in aks reviews. when aks over app service or container apps aks is the most powerful and most operationally expensive option. the decision table below is what interviewers want you to reason through out loud, not just state a conclusion. requirement app service (containers) container apps aks ops overhead lowest low highest — you own upgrades, networking, node health kubernetes api / crds needed no no yes fine grained au","azure/service-bus":"service bus is the enterprise messaging broker interviewers expect you to know cold if your resume mentions distributed systems on azure. this page covers queues versus topics, sessions, delivery semantics, dead lettering, and the tuning parameters that actually show up in incident reviews. queues vs topics/subscriptions a queue is point to point — one message, consumed by exactly one competing consumer. a topic is publish/subscribe — one message is published once, and delivered to every subscription attached to that topic, each subscription acting like its own independent queue with its own competing consumers. queue topic + subscriptions delivery pattern one consumer group gets each message once every subscription gets its own copy of each message use case work distribution across competing workers fan out to multiple independent consumers filtering n/a each subscription can filter which messages it receives subscription filters subscriptions can filter which messages of a topic they actually receive, avoiding the need for consumers to filter after receiving (and paying for) every message. filter type how it matches example sql filter sql like boolean expression against message properties correlation filter cheaper, exact match against specific properties (correlation id, label, etc.) true/false filter matches everything or nothing (default is match all) used to disable a subscription temporarily prefer correlation filters over sql filters when you only need exact match logic — they're evaluated more cheaply by the service and scale better with high subscription counts. r","azure/event-hubs":"event hubs questions test whether you understand log based streaming versus queue based messaging — a distinction interviewers use to see if you'd reach for the right tool for high throughput telemetry versus transactional work. this page covers partitioning, throughput scaling, consumer groups, checkpointing, and the partition count decision you cannot walk back. the partitioned log model event hubs is a distributed, append only log, conceptually close to kafka. events are appended to one of n partitions , each partition an independently ordered, replayable sequence — event hubs does not delete a message once read (unlike service bus); instead, every event sits in its partition until it ages out of the configured retention window, and any number of independent consumers can read the same events at their own pace. [!key] service bus is a broker — a message is consumed and gone. event hubs is a log — a message stays put and multiple independent readers replay the same stream at their own offsets. reach for event hubs when you need high throughput ingestion with replay (telemetry, clickstreams, iot); reach for service bus when you need transactional guarantees per message (ordering via sessions, dead lettering, exactly once feeling delivery via peek lock). partitions and partition keys producers can specify a partition key ; event hubs hashes it to consistently route all events with the same key to the same partition, guaranteeing ordering within that key — the same guarantee service bus sessions provide, but as a routing hash rather than an exclusive lock. without a partitio","azure/event-grid":`event grid is the piece that trips people up because it looks like service bus or event hubs but solves a genuinely different problem — reacting to discrete things happening, pushed to you, rather than processing a stream or a work queue. this page covers the push model, topic types, delivery guarantees, and the three way comparison interviewers love to ask for. the push based eventing model event grid pushes events to subscribers the moment they occur, rather than subscribers pulling/polling for them — the defining difference from service bus and event hubs, both of which are pull based (a consumer actively receives or reads). a publisher raises an event once; event grid fans it out to every matching subscription's destination (a webhook, a function, a service bus queue, a storage queue, an event hub, a logic app) with typically sub second latency. [!key] event grid answers "something happened, who needs to know right now" — reactive, low latency, fan out notification. event hubs answers "a continuous stream of data needs to be ingested and replayed." service bus answers "this unit of work needs to be reliably processed exactly once." confusing these three is the single most common mistake in an azure messaging design discussion. topics, system topics, and domains concept what publishes to it typical use custom topic your own application code app level custom events ("ordershipped", "userregistered") system topic azure itself, on behalf of a resource built in events from blob storage, resource groups, key vault, iot hub, etc. — no code required to publish domain acts as a `,"azure/choosing-a-messaging-service":`every azure system design round eventually asks "how do these two services talk to each other", and the honest answer is almost never "call it directly". this page is the decision framework for picking a messaging service and the language to justify it under follow up questions. the four shapes of "messaging" azure's messaging options are not five flavours of the same thing — they solve four different problems. brokered queue (storage queues, service bus queues) — one message, one consumer, work is removed once processed. brokered pub/sub (service bus topics/subscriptions) — one message, many independent subscribers, each gets its own copy with its own filter. event notification (event grid) — "something happened", pushed at low latency, http native, not meant to carry the full payload. event streaming / log (event hubs, kafka on azure) — an ordered, replayable, retained log that many consumers can read at their own pace and rewind. [!key] queues distribute work , pub/sub and event grid notify , and event hubs/kafka stream a durable log . naming which of the four you need is 80% of the design answer. comparison table dimension service bus event hubs event grid storage queues kafka on azure (hdinsight / confluent) model broker (queue + pub/sub) partitioned log push event router simple broker queue partitioned log ordering fifo within a session guaranteed per partition not guaranteed not guaranteed guaranteed per partition delivery guarantee at least once (dedup available) at least once at least once (retries + dlq) at least once at least once (exactly once with transactions)`,"azure/cosmos-db-fundamentals":'cosmos db interview questions almost never start with "how do you write a query" — they start with "how is data organised and paid for". get the account/database/container/item model, the api choice, and the throughput model solid first; everything else (partitioning, consistency, change feed) is built on top of these four ideas. the resource hierarchy account — the top level resource; you pick the api here (nosql, mongodb, cassandra, gremlin, table) and it cannot be changed afterwards. also the boundary for global distribution, failover policy, and keys/rbac. database — a namespace for containers; can optionally hold shared throughput. container — the unit of storage and throughput scaling; roughly analogous to a table/collection, but backed by physical partitions that split automatically as data grows. item — a single json document (or row/vertex/edge depending on api), up to 2 mb. [!key] a container — not a database — is where partition key and throughput decisions live. interviewers listen for whether you know throughput and partitioning are container scoped concepts. choosing an api cosmos db is a multi model engine underneath, but you commit to one wire protocol per account. api data model when to use when to avoid nosql (core/sql) json documents, sql like query language default choice for new applications; full feature access (change feed, best indexing control) n/a — this is the reference api mongodb api bson documents, mongodb wire protocol lift and shift existing mongodb apps/drivers with minimal code change new greenfield apps — you lose some nosql api only featu',"azure/cosmos-partitioning-and-request-units":`partitioning is the single most consequential design decision in cosmos db — get it wrong and no amount of ru/s or clever indexing will save you from throttling and hot partitions. this page covers the logical/physical partition model, how to pick a partition key, and how to reason about request unit cost like someone who has actually been paged for a 429 storm. logical vs physical partitions a logical partition is every item sharing the same partition key value — cosmos guarantees all of a logical partition's data lives together on one physical partition and can be queried/transacted together. a physical partition is the actual replicated compute+storage unit cosmos manages behind the scenes; you never address it directly, but its two hard limits shape every partitioning decision: limit value consequence max storage per logical partition 20 gb a partition key value that accumulates unbounded data (e.g. "tenantid" for a huge tenant) will eventually hit a hard write failure max throughput per physical partition 10,000 ru/s a container provisioned at 100,000 ru/s still has that split across roughly 10 physical partitions — no single partition key's traffic can exceed 10,000 ru/s no matter the container total [!key] physical partitions are cosmos's internal scaling unit; you only ever choose the logical partition key , and cosmos maps many logical partitions onto each physical partition, splitting physical partitions automatically as storage or throughput demands grow. choosing a partition key the three properties to check, in order: 1. high cardinality — many distinct values,`,"azure/cosmos-consistency-and-indexing":'consistency and indexing are the two cosmos db knobs that most directly trade ru cost against correctness and query speed, and interviewers use them to check whether you actually operated cosmos or just read the docs once. this page covers the five consistency levels, session tokens, and how the automatic indexing policy shapes both query performance and your write bill. the five consistency levels cosmos db is unusual among databases in offering five distinct, well defined consistency levels, chosen per account (with per request override) rather than forcing a single global trade off. level guarantee read latency availability ru cost (reads) strong linearizable — reads always see the latest committed write highest lowest (no reads during a region failure without failover) highest bounded staleness reads lag writes by at most k versions or t time, configurable low to moderate high moderate session (default) within one client session, always read your own writes (monotonic reads/writes) low high low consistent prefix reads never see out of order writes (no gaps), but may be stale low high low eventual no ordering guarantee at all; eventually converges lowest highest lowest [!key] only strong and bounded staleness are "global" guarantees enforced across all replicas; session , consistent prefix , and eventual are progressively weaker but far cheaper and more available, especially across multi region writes. why session is the default, and what a session token is session consistency gives the single most useful guarantee for typical application code — read your own writes — at',"azure/cosmos-change-feed-and-multi-region":`the change feed is cosmos db's answer to "how do i react to every write without polling", and multi region configuration is how cosmos delivers its headline global scale story. both come up constantly in system design interviews once cosmos is on the table, and both have sharp edges that separate real experience from surface knowledge. change feed semantics the change feed is a persistent, ordered log of every insert and update (by default) in a container, exposed per logical partition key. it is not a general purpose event stream with arbitrary retention — it reflects the container's own data. ordered per partition key — within one logical partition, changes appear in the order they were committed. there is no global ordering guarantee across different partition keys. no deletes by default — the classic "latest version" change feed mode only surfaces creates and updates; a delete simply removes the item, leaving no change feed entry, unless you either soft delete (a / flag update, which does appear in the feed) or use the newer all versions and deletes mode. at least once delivery to consumers — a change may be observed more than once after a consumer restart, so downstream processing must be idempotent. mode what it shows notes latest version (default) only the most recent state per changed item, creates + updates no deletes; simplest and cheapest; good enough for most projections all versions and deletes every intermediate change and deletes, as a true audit log needed for cdc style pipelines, audit trails, or exact replication; requires explicit container configuration `,"azure/cosmos-data-modeling":`relational modelling starts from the entities and normalises; cosmos db modelling starts from the queries and denormalises on purpose. this is the mental switch that trips up experienced relational engineers, and it's the thing interviewers probe hardest when cosmos is on your resume — not whether you can write a . access pattern first modelling before drawing a single entity, list every query the application actually runs: "get order by id", "list a customer's last 20 orders", "find orders shipped to a region in the last week". each access pattern implies a partition key candidate and a document shape. only after this list exists do you decide what to embed, what to reference, and what to duplicate. [!key] in cosmos db, the schema is a side effect of the queries , not the other way around. if you can't name the top five queries before modelling, you will re model after production traffic tells you which ones actually mattered. embedding vs referencing factor embed reference relationship shape bounded (a few, known max) — e.g. an order's line items unbounded or large — e.g. a customer's lifetime orders read pattern almost always read together usually queried independently write frequency child rarely changes independently of parent child updates far more often than parent size combined document stays well under the 2 mb item limit would push the document toward or over the limit consistency need fine to be slightly stale as one unit needs independent access/update without touching the parent [!tip] a senior answer names the failure mode of over embedding explicitly: "an ord`,"azure/azure-sql-and-redis":`azure sql and azure cache for redis are the two services almost every azure system design answer eventually reaches for — a relational store for transactional integrity, a cache in front of it for latency. interviewers expect fluency in both the deployment/pricing options and the failure handling patterns, not just "sql for structured data, redis for speed". azure sql deployment options option isolation best for single database fully isolated database with its own resources simple apps, one database per workload, easiest to reason about elastic pool many databases share a pool of resources (dtus/vcores) many databases with unpredictable, non simultaneous peaks — saas with one database per tenant managed instance near complete sql server surface area (cross database queries, sql agent, clr, linked servers) in a fully managed paas instance, in your vnet lift and shift of on prem sql server with minimal app changes, needs sql server features single database doesn't expose [!key] managed instance exists specifically to close the gap between "cloud native single database" and "everything our legacy sql server app relies on" — cross database queries, sql agent jobs, service broker. if a candidate answer says "just use single database" for a lift and shift with cross database stored procedures, that's a red flag. purchasing models model billing unit scaling best for dtu based bundled compute+storage+io "database transaction unit" tiers (basic/standard/premium) fixed tier steps simple, predictable workloads; easiest initial choice vcore based (provisioned) compute and storage bille`,"azure/blob-storage-and-ai-search":'blob storage is the substrate almost every azure architecture eventually touches — files, backups, static assets, data lake landing zones — and azure ai search is the layer that makes that unstructured content findable. interviewers use this pairing to test whether you understand durability trade offs, least privilege access, and when "just query the database" stops being good enough. storage account redundancy options option copies durability failover behaviour lrs (locally redundant) 3 copies in one datacenter 11 nines no protection against datacenter loss zrs (zone redundant) 3 copies across availability zones in one region 12 nines survives a datacenter/zone failure; no cross region protection grs (geo redundant) lrs in primary + async replicated lrs copy in paired region 16 nines secondary region data is not readable until microsoft initiated failover ra grs (read access geo redundant) same as grs 16 nines secondary region is readable at all times via a endpoint, even before failover gzrs / ra gzrs zrs in primary + geo replicated to secondary region highest combines zone resilience in region with geo redundancy [!key] grs protects against a regional disaster but the secondary copy is not readable until microsoft actually fails over the account — if you need to read from the secondary region proactively (e.g. for read scale out or to validate dr readiness), you need ra grs , not plain grs. blob types type structure use case block blob composed of blocks, optimised for large sequential uploads/downloads files, images, video, backups — the default choice append blob optim',"backend/rest-principles":"rest is not a protocol or a library, it is a set of architectural constraints for building networked systems that roy fielding described in his 2000 doctoral dissertation. interviewers rarely ask you to recite the constraints, but they will ask you to justify a design decision — a nested uri, a status code, a caching header — and the constraint behind it is the correct justification. the name is itself a definition: re presentational s tate t ransfer means a client and server exchange representations (json, xml) of a resource's current state , and that representation — not the resource itself — is what's transferred over the network. business domains such as orders, customers and tickets become rest resources; the http verb and status code describe what happened to that resource's state, not a remote procedure being invoked. the six constraints constraint what it means interview relevance client server ui and storage concerns are separated lets clients and servers evolve independently stateless each request contains all the context the server needs no server side session; enables horizontal scaling cacheable responses declare whether they can be cached reduces load, improves latency uniform interface resources identified by uri, manipulated through representations, self descriptive messages, hateoas the part everyone gets wrong in practice layered system client cannot tell if it talked to the origin server or an intermediary enables gateways, load balancers, cdns code on demand (optional) server can send executable code to the client rarely used outside browsers (js) [!key]","backend/http-methods-status-codes-headers":`http is the substrate almost every backend interview question sits on top of. interviewers use it to check that you know the difference between "the request failed" (4xx/5xx) and "the request succeeded but here's what happened" (2xx/3xx), and whether you can reason about safety, idempotency and caching from first principles rather than memorised trivia. http methods method purpose safe idempotent cacheable request body retrieve a resource ✅ ✅ ✅ no like get, headers only ✅ ✅ ✅ no create a resource / trigger an action ❌ ❌ only if explicitly marked cacheable yes replace a resource entirely ❌ ✅ ❌ yes partially modify a resource ❌ ❌ ❌ yes remove a resource ❌ ✅ ❌ usually no discover allowed methods/headers ✅ ✅ ❌ no can be made idempotent (e.g., json merge patch that sets absolute values), but a delta style patch like "increment count by 1" is not — calling it twice doubles the effect. safe means the request doesn't change server state (so it's fine to prefetch or retry blindly). idempotent means calling it n times has the same effect as calling it once — critical for retry logic on flaky networks. every safe method is idempotent, but not every idempotent method is safe ( changes state yet is idempotent: deleting twice leaves the same end state as deleting once). [!key] idempotency is what makes retries safe. if your client can't tell whether a succeeded before the connection dropped, retrying it blindly can double charge a customer — this is exactly why idempotency keys exist (see the dedicated idempotency page). put vs patch vs post put patch post semantics replace the whole res`,"backend/api-versioning":`an api is a contract, and once external clients depend on it you cannot change that contract for free — someone's mobile app, integration, or batch job will break. versioning is how you evolve a contract without breaking everyone who already trusts it, and interviewers use this topic to see whether you think about api design as a long lived product, not a one off endpoint. why version at all if you owned every client, you could change the api and redeploy every caller simultaneously — no versioning needed. real systems rarely have that luxury: mobile apps lag behind server deployments by weeks (app store review, staged rollout), partner integrations are maintained by other companies on their own schedule, and internal services are deployed independently. versioning lets the server change while giving old clients a stable, working contract until they choose to migrate. [!key] the real goal isn't "have version numbers," it's "never break a client who didn't ask to be broken." versioning is one tool for that; additive, backwards compatible design is the other, and it should be your default. breaking vs non breaking changes change breaking? why adding a new optional field to a response no clients ignore fields they don't know about adding a new optional query parameter no old requests still work unchanged adding a new endpoint no doesn't affect existing callers removing a field yes clients reading it will fail or silently misbehave renaming a field yes old field name disappears changing a field's type (string → int) yes deserialization breaks making an optional field required y`,"backend/pagination-filtering-and-sorting":'any list endpoint that can grow past a few hundred rows needs a pagination strategy, and the naive choice — offset and limit — quietly degrades as a table grows, which is exactly the kind of thing interviewers probe to separate "wrote a crud api" from "operated one in production." filtering and sorting sit right alongside pagination because all three interact: a sort on an unindexed column can make pagination slow regardless of which strategy you pick. offset/limit vs keyset (cursor) pagination offset/limit keyset (cursor) request shape underlying query performance at page 1 fast fast performance at page 10,000 slow — scans and discards all skipped rows fast — uses the index, no scan and discard jump to arbitrary page n easy ( ) not possible, only "next"/"previous" stable under concurrent inserts/deletes no — rows shift between pages, causing skips or duplicates yes — anchored to a value, unaffected by inserts elsewhere total page count trivial ( ) requires a separate, possibly approximate count best for small to medium tables, ui page number controls large or fast growing tables, infinite scroll, apis [!key] offset pagination pays a cost proportional to the offset itself: still makes the database walk (or at least count) 100,000 rows before it can discard them and return the next 20. keyset pagination pays a cost proportional to the page size only, because it seeks directly via an index condition. the deep pagination performance problem even with an index on , most engines still need to walk the index that many entries deep before it can start returning rows, because is ap',"backend/openapi-and-api-contracts":`an api contract is a promise about shape and behaviour that exists independently of any single implementation, and openapi is the dominant way to write that promise down for http apis so humans and tooling can both rely on it. interviewers ask about this to see whether you treat an api as a product with a stable interface, or as "whatever the code currently happens to return." what openapi/swagger gives you openapi is a machine readable specification (yaml or json) describing every endpoint, its parameters, request/response schemas, authentication requirements, and possible error responses. from that one document you get, largely for free: interactive documentation (swagger ui, redoc) that's always in sync with the spec. generated client sdks in many languages, removing hand written http client boilerplate. generated server stubs for contract first workflows. request/response validation middleware that rejects payloads not matching the schema. mock servers generated straight from the spec, so frontend teams can start against a fake backend before the real one exists. a single source of truth other tools (contract diff, linters, test generators) can consume. [!key] the value of openapi isn't the yaml file itself, it's that a single artifact becomes the input to five different tools — docs, codegen, validation, mocking, diffing — instead of five teams maintaining five separately drifting descriptions of the same api. contract first vs code first contract first code first source of truth the openapi spec, written/reviewed first the code (controllers, attributes); spec generate`,"backend/beyond-rest":`rest's request/response model quietly assumes three things: the client wants a fixed shape of data, the interaction is one request in and one response back, and a plain http cache in front of it is useful. plenty of real traffic breaks at least one of those assumptions — a mobile dashboard that needs wildly different fields per screen, an internal call where every microsecond of latency matters, or a feed that needs to keep pushing updates long after the request finished. graphql, grpc, websockets and server sent events (sse) each fix a different assumption; none of them replace rest wholesale, and knowing which one to reach for — and why — is exactly what separates "i've heard of graphql" from a defensible design decision. when each protocol wins dimension rest graphql grpc websockets sse schema optional (openapi) mandatory, strongly typed (sdl) mandatory, strongly typed ( ) none built in — you define the message contract none built in — you define the event/data contract streaming no — request/response only subscriptions (over websocket transport) native — unary, server, client, bidirectional native, full duplex native, server to client only browser support native ( / ) native (post over / ) no — needs grpc web or connect plus a proxy native ( api) native ( api) tooling extremely mature — curl, postman, gateways, cdns mature — graphiql, apollo/relay, codegen mature but steeper — , generated stubs moderate — you build your own protocol on top minimal — plain http plus caching native http caching ( , , cdns) hard — single endpoint, everything is manual, connection level not`,"backend/idempotency":`networks fail in the worst possible place — after the server processed a request but before the client received the response. when that happens the client cannot tell success from failure, and its only reasonable option is to retry. idempotency is what makes that retry safe instead of dangerous, and it's a favourite interview topic because it forces you to reason about state, concurrency and failure together. definition and why it matters an operation is idempotent if performing it multiple times has the same effect as performing it once. for naturally idempotent operations ( , , ), retries are free — call them as many times as you like. for inherently non idempotent operations ( , "charge this card", "send this email"), a naive retry after a timeout can duplicate the side effect: charge twice, send the email twice, create two orders from one click. [!key] the failure mode isn't "the request failed" — it's "the client doesn't know whether it succeeded." a client that assumes failure and retries a non idempotent is the single most common cause of duplicate side effects in distributed systems. which http methods are naturally idempotent method idempotent why , , yes safe — no state change at all yes replaces the resource with the same representation each time yes deleting an already deleted resource is a no op end state no creates a new resource or triggers an action each call, by default depends idempotent if it sets absolute values; not if it applies a relative delta is the method that needs deliberate idempotency engineering — it is the one used for "create a resource" and`,"backend/rate-limiting-implementation":`rate limiting protects a system from being overwhelmed — by a runaway client, a scraping bot, an unintentional retry loop, or genuine excess demand — and it enforces fairness so one noisy tenant can't starve everyone else. interviewers use it to check whether you can reason about algorithms, distributed state and graceful degradation together, since a real rate limiter touches all three. why rate limit goal example protection stop a buggy client's retry loop or a ddos style burst from taking the service down fairness prevent one tenant on a shared multi tenant api from starving others cost control cap usage against a paid upstream (sms provider, llm api) per customer contractual tiers enforce a documented "1,000 requests/hour on the free plan" limit [!key] rate limiting is a resilience pattern, not a security control on its own — it complements authentication and input validation, it doesn't replace them. a determined attacker with many ips or accounts can still exceed a per key limit in aggregate. algorithms compared algorithm how it works memory burst behaviour weakness fixed window count requests in a fixed clock interval (e.g., per minute bucket), reset at boundary o(1) per key allows up to 2× the limit right at the window boundary boundary burst problem sliding window log store a timestamp per request, count how many fall within the trailing window o(n) per key (n = requests in window) perfectly accurate memory grows with request rate sliding window counter weighted average of current and previous fixed windows o(1) per key smooths the boundary burst, approximate sligh`,"backend/validation-and-error-handling":`every request that reaches your api is either well formed and permitted, or it isn't — and where you check that, how consistently you report failures, and how much internal detail you leak in the process are all things interviewers probe to see if you've actually run an api in production versus just built one that works on the happy path. where to validate layer checks example edge (gateway/api layer) malformed syntax, auth, request size, content type invalid json, missing bearer token, oversized payload application (model binding) required fields, types, ranges, formats must be a valid email, must be a positive integer domain (business rules) rules that depend on state or cross field logic "cannot cancel an order that already shipped" database (constraints) last line of defence — uniqueness, foreign keys, not null duplicate email violates a unique index [!key] fail fast, and fail at the cheapest layer that can catch the problem. a malformed json body should never reach your domain layer; a business rule violation should never be caught only by a database constraint throwing an exception you have to translate after the fact. model validation vs business rule validation model (or input) validation asks "is this request shaped correctly" — types, required fields, string length, format (email, url). business rule validation asks "is this request allowed given the current state of the system" — it usually needs a database read or domain knowledge the model alone doesn't have. that attribute based check runs before the request ever reaches your handler. but "this customer's acco`,"backend/aspnetcore-pipeline-and-middleware":'every asp.net core request travels through a pipeline of middleware components before it produces a response. understanding this pipeline — what runs, in what order, and why — is one of the fastest ways to separate someone who has configured from someone who understands the framework underneath it. the pipeline as a chain of delegates middleware is just a function that takes a and a reference to "the next thing", does some work, optionally calls that next thing, and optionally does more work on the way back out. the runtime wires these into a single nested delegate chain built at startup. the arrows going back show the key idea: middleware is onion shaped . code before runs on the way in; code after it runs on the way out, in reverse order. this is why an exception filter registered first can catch errors thrown by everything downstream — it wraps the entire rest of the pipeline. [!key] middleware order is not a style choice — it changes behaviour. a component can only act on what already ran before it, and it only sees the response after everything after it has run. use, run and map exposes three registration verbs with different intents. method behaviour typical use runs, then optionally calls most middleware — logging, auth, custom checks terminal — never calls last component in a branch, e.g. a fallback 404 / branches the pipeline based on path or predicate splitting behaviour for a path prefix like creates a genuinely separate branch — middleware registered inside it does not run for requests outside that path, which is useful for isolating expensive or sensitive compo',"backend/controllers-and-minimal-apis":`asp.net core gives you two ways to build http apis: controllers (the mvc style approach with attributes and conventions) and minimal apis (endpoints defined as lambdas on ). they compile down to similar routing internals, but they differ enough in ergonomics, performance and testability that "which one and why" is a standard interview question. the two styles, side by side both produce the same route, the same model binding for , and the same json response. the differences show up in scale, tooling and conventions rather than raw capability. comparison table dimension controllers minimal apis startup performance slightly slower (reflection based discovery) faster cold start, fewer allocations testability easy — test the controller class directly easy for the handler, but route wiring itself is only covered by integration tests filters rich / pipeline endpoint filters ( ) — simpler, lighter model binding attribute driven, many built in sources and conventions explicit — inferred from parameter types and a smaller rule set conventions heavy — action results, behaviours, filters, areas minimal — you write what you mean, less "magic" discoverability great with swagger/openapi tooling and years of ecosystem support good, catching up; less tooling for very large surfaces best fit large apis, teams used to mvc, heavy cross cutting filter logic microservices, small focused apis, latency sensitive endpoints [!key] minimal apis are not "a lesser version" of controllers — they're a lower ceremony way to express the same routing and binding model, optimized for services where startup t`,"backend/dependency-injection-lifetimes":`dependency injection (di) is how asp.net core wires collaborators together instead of having classes construct their own dependencies. the mechanics are simple to learn and easy to misuse — almost every "weird bug under load" story in a .net backend traces back to a lifetime mismatch, which is exactly why interviewers probe this deeply. why di, and the built in container di inverts control: a class declares what it needs (via constructor parameters) instead of creating those dependencies itself. this makes classes easier to test (swap in a fake), easier to reconfigure (swap an implementation without touching consumers), and forces you to think about lifetime explicitly. asp.net core ships a built in container ( / ) that is lightweight by design — no interception, no property injection, no circular dependency resolution — you can swap in autofac or others if you need more, but the built in container covers the vast majority of real applications. transient vs scoped vs singleton lifetime instance created lives until concrete example transient every time it's resolved immediately eligible for gc after use a lightweight validator or mapper with no state, scoped once per request (per ) end of the http request / scope , a unit of work, a per request tenant context singleton once, on first resolution (or eagerly at startup) application lifetime , , configuration snapshots [!key] pick the lifetime based on state and cost , not habit. stateless and cheap → transient. needs to be consistent across one request → scoped. expensive to build, thread safe, and safe to share forever → sing`,"backend/authentication-and-authorization":`authentication answers "who are you"; authorization answers "what are you allowed to do". they are separate concerns implemented as separate middleware, run in that order, and conflating them is one of the fastest ways to design an insecure api. this page covers how asp.net core implements both, and the jwt bearer flow that backs most modern api auth. authn vs authz in the pipeline runs first and populates (a ) by asking each registered authentication scheme to try to identify the caller from the request — a header, a cookie, a token. runs after and, using the endpoint metadata routing attached, decides whether the now identified (or still anonymous) user is allowed to reach that endpoint. [!key] a means "i don't know who you are" (authentication failed or was never attempted). a means "i know who you are, and you're not allowed to do this" (authorization failed). mixing these up in api design is a common and telling mistake. authentication schemes and handlers a scheme is a named configuration of an — the code that inspects the request and produces (or fails to produce) a . an app can register multiple schemes (jwt bearer, cookies, api key) and pick a default, or require a specific scheme per endpoint. api key and basic authentication handlers not every caller is a browser driven user with a jwt. server to server integrations and partner apis often authenticate with a simpler scheme, implemented as a custom registered alongside jwt bearer: basic authentication (username/password base64 encoded in the header) is rarely appropriate for a public api — the encoding isn't encry`,"backend/configuration-logging-health-checks":"configuration, logging and health checks are the unglamorous plumbing that decides whether an incident takes five minutes or five hours to diagnose. interviewers use this area to check whether you've actually run something in production, not just built it. configuration providers and precedence asp.net core configuration is a layered stack of providers, each contributing key value pairs to one flattened . later providers override earlier ones for the same key. order source typical use 1 defaults shared across all environments 2 per environment overrides (development, staging, production) 3 user secrets local developer secrets, never committed 4 environment variables container/orchestrator injected values, the standard 12 factor mechanism 5 command line arguments ad hoc overrides at launch 6 key vault / aws secrets manager / etc. secrets pulled from a managed secret store at startup [!key] precedence is registration order, not the order above by convention — registers them in this order by default, but if you add providers manually, whichever is added last wins for a duplicate key. environment variables and key vault environment variables are the standard way orchestrators (kubernetes, ecs, docker compose) inject configuration without baking it into the image — maps to via the double underscore convention, since isn't valid in most shell environments. key vault (or an equivalent secret manager) is added as a configuration provider too, so secrets appear in alongside everything else — the code consuming a connection string doesn't need to know whether it came from or a vault.","backend/background-services":"background work — processing a queue, running a periodic job, warming a cache — is a normal part of most backend services, and .net gives you a first class abstraction for it: hosted services. getting them wrong is subtle because the failures (a blocked startup, a leaked scope, duplicate work across replicas) don't show up in local testing. ihostedservice vs backgroundservice is the low level interface with / , both invoked by the host at application startup/shutdown. is an abstract base class implementing for you, exposing a single method that you fill with a loop — it's the right starting point for almost all background work. [!key] all hosted services are registered as singletons and share the app's di container and lifetime. if your background work needs a scoped service (a , a repository), you must create a scope manually — you cannot inject it directly. the startasync blocking trap the host calls every sequentially and awaits each one before the application is considered started and begins accepting requests. (inherited, not overridden in typical usage) calls but does not await its completion — it fires it and returns, which is exactly right for a long running loop. the trap is overriding directly and awaiting a long running operation inside it, or writing an whose first line does something that never yields (a synchronous blocking call before the first ), which delays the whole application's startup or, worse, the readiness probe. graceful shutdown and stopping tokens when the host begins shutting down, it signals the passed to (and calls on every hosted service), th","backend/resilience-retries-and-circuit-breakers":`every network call can fail, hang, or slow down, and a distributed system that doesn't plan for that will eventually turn one slow dependency into a full outage. resilience patterns are the standard vocabulary for reasoning about this — interviewers use this topic to check whether you've actually operated a service that calls other services, not just built one. timeouts: the most common production failure an http call, a database query, or a queue receive with no timeout will wait as long as the underlying transport allows — often minutes, sometimes forever. under load, threads (and connections) pile up waiting on a slow dependency, and the caller runs out of capacity long before the dependency actually fails outright. this single missing setting is behind an outsized share of real production incidents. [!key] always set an explicit timeout shorter than whatever timeout the caller of your service has, so your service fails fast enough that its own caller doesn't time out first while you're still "trying". a chain of unaligned timeouts is a classic cause of cascading slowness. retries — and when retrying is harmful retrying makes sense for transient failures: a dropped connection, a from an overloaded but recovering service, a brief network blip. retrying is actively harmful for non transient failures — a will fail identically every time, and retrying it wastes capacity and adds latency for no benefit. retrying a without honouring backoff makes the underlying overload worse , not better. failure type retry? why connection reset / timeout yes often transient network blip yes,`,"backend/production-readiness":"getting a service to work is table stakes; getting it to survive real traffic, real failures, and real deploys is a different, larger checklist. this page is a tour of the guards a production asp.net core service needs that a demo or a happy path implementation usually skips. connection pooling and httpclientfactory creating a new per call exhausts sockets: each instance owns its own connection pool, and disposing it doesn't immediately release the underlying tcp connection — it lingers in . under load, this exhausts available ports, a failure mode known as socket exhaustion . the opposite mistake — one static, long lived — causes dns staleness : the client caches the resolved ip for a connection's lifetime, so it never notices a dns change (a failed over load balancer, a rotated ip) until the process restarts. [!key] solves both problems at once: it pools and reuses instances (avoiding per call socket cost) while still recycling them periodically (avoiding permanent dns staleness), by default every two minutes. graceful shutdown and draining when an orchestrator wants to stop an instance (deploy, scale down, node drain), it sends a termination signal and expects the process to stop accepting new work while finishing in flight requests within a bounded window — not stop instantly, and not hang forever. [!warning] if readiness doesn't fail immediately on shutdown signal, the load balancer keeps sending new requests to an instance that's already draining, and some of them arrive after the process has exited — visible to users as a burst of connection reset errors during every","behavioural/star-and-your-story-bank":`behavioural rounds are not a memory test, they are a structure and evidence test. interviewers are pattern matching your answer against a rubric while you talk, and star is the shape that makes that matching easy for them and easy for you under pressure. the star method and where the time goes star stands for situation, task, action, result . most candidates get the shape right but the proportions badly wrong — they spend two minutes on backstory and twenty seconds on what they actually did, which is the only part that differentiates them from anyone else who was in the room. part target share what it answers common failure situation 15% where, when, what was the context rambling for a minute before the point task 10% what was your specific responsibility or goal confusing task with action action 60% what you did, step by step, and why skipped in favour of more context result 15% what happened, ideally with a number vague ("it went well") [!key] action is the majority of the answer. if you can't fill 60% of your airtime describing decisions you made, it is not yet a strong story — it is an anecdote. why "we" kills answers interviewers are evaluating an individual, not a team, and "we decided", "we built", "we fixed" gives them nothing to score. the fix is not to erase the team — a story where you claim sole credit for a group effort reads as dishonest and is easy to puncture with a follow up. the fix is precision: use "we" for the shared context and switch to "i" the moment you describe a decision, a trade off, or an action that was specifically yours. [!tip] a reliable pat`,"behavioural/leadership-principles-mapping":`large technology companies do not evaluate behavioural answers on vibes — they score against a written competency rubric, usually shared across interviewers as a scorecard. understanding the rubric changes how you prepare: instead of memorising stories, you map stories onto the specific things each interviewer is scored on eliciting from you. how large companies score behavioural rounds most senior loops split interviewers across a small set of competencies, each interviewer typically responsible for probing two or three in depth rather than all of them shallowly. they take structured notes against specific behavioural indicators — not "did i like this candidate" but "did they show evidence of independently identifying and closing a gap" or "did they show they sought disconfirming evidence before deciding." scores are usually calibrated in a debrief where interviewers compare notes against the same rubric, which is why a vague, unstructured answer scores poorly even if it was a genuinely interesting story — the interviewer literally cannot write it down against a specific indicator. [!key] if you can't imagine which box on a scorecard your story would tick, the interviewer probably can't either. structure your answer so the competency being tested is unmistakable, not just implied. common themes across companies different companies use different names, but the underlying competencies converge heavily. the table below uses commonly recognised terms and applies broadly regardless of which specific company's language is used. principle what it's really testing a prompt that ta`,"behavioural/leadership-and-mentoring":`leadership questions in a senior or tech lead loop are rarely about a job title. interviewers want evidence that you can set direction, get people to follow it without formal power, and grow the people around you — three distinct skills that candidates routinely blur together into one vague story about "leading a project". what leadership means for an ic versus a tech lead an individual contributor demonstrates leadership through influence on a specific decision or person — proposing a design, unblocking a teammate, driving a fix nobody owned. a tech lead is expected to show leadership as a sustained pattern across a team or set of teams — setting technical direction, owning a roadmap trade off, being the person others escalate ambiguity to. signal ic level story tech lead level story scope one project, one decision multiple projects, a team's technical direction timeframe days to weeks a quarter or more mechanism proposed and convinced set expectations, delegated, followed up people affected peers peers, juniors, other teams, sometimes management [!key] if your leadership stories are all about doing the hard technical work yourself, you are reading as a strong senior ic, not a tech lead. tech lead stories need at least one where your main contribution was making someone else successful. influence without authority this is the single most tested leadership competency, because most engineers do not have formal power over the people they need to move. a concrete approach that works and is easy to narrate: 1. lead with the shared goal , not your preferred solution — "we both w`,"behavioural/conflict-and-disagreement":`every senior loop asks some version of "tell me about a disagreement." interviewers are not looking for evidence you avoid conflict, or that you win every one — they are listening for whether you handle it with data, empathy, and enough self awareness to know when you were wrong. the structure of a good conflict story a conflict story needs more scaffolding than a standard star answer because it has two people's positions in it, not one. the shape that works: 1. context — what was at stake, briefly. 2. both positions, stated fairly — including why the other person's view was reasonable, not a strawman. 3. how you sought data — what you did to move the conversation from opinion to evidence. 4. resolution — what actually happened, and who moved. 5. relationship preserved — a concrete sign the working relationship survived or improved. 6. outcome — what happened afterward, ideally with a number. [!key] the step candidates skip most is stating the other person's position fairly. if your story makes the other person sound unreasonable, the interviewer hears ego, not judgement — even if your technical call was correct. disagreeing with an engineer the most common variant. the strongest version shows you engaged with the technical substance of their position before pushing back, and that the resolution came from evidence or a jointly agreed test, not from you outranking them or simply talking longer. disagreeing with a manager this one tests whether you can push back up , which is a different skill from pushing back sideways. the key elements: you raised the disagreement privately`,"behavioural/execution-and-delivery":`execution questions test something different from leadership or conflict questions: can you actually get things shipped when reality is messy — the deadline is real, the requirements are half formed, and something has to give. interviewers are listening for judgement about what you cut and how you decided, not just that you hit the date. tight deadlines and competing priorities the weak version of this story is "i worked long hours to hit the date." the strong version shows a decision about scope , not just effort — you identified what could be cut, negotiated it explicitly with stakeholders, and protected the parts that mattered most (correctness, security, data integrity) while trimming the parts that didn't (polish, edge cases with low probability, nice to have configurability). [!key] "i worked extra hours" is not a differentiator — most candidates can say that. "i identified exactly what to cut and got explicit agreement to cut it" is the signal interviewers are actually listening for. negotiating and cutting scope well cutting scope badly means silently dropping something and hoping nobody notices, or unilaterally deciding what's "not important" without checking with the people who asked for it. cutting scope well is a negotiation: 1. state clearly what is at risk given the current scope and date. 2. propose two or three concrete options (cut x, extend by n days, add resourcing) rather than just raising a problem. 3. get an explicit decision from whoever owns the trade off — don't assume silence is agreement. 4. write down what was cut and why, so it doesn't quietly b`,"behavioural/ownership-and-initiative":`ownership is one of the most cited but least differentiated competencies in interviews, because almost every candidate claims it. what separates a real ownership story is that you acted on something before anyone assigned it to you , and stayed with it through the unglamorous parts, not just the interesting first ninety percent. going beyond assigned scope the clearest ownership signal is a gap between what your ticket said and what you actually did. a strong story names the moment you noticed the gap — a related bug nobody had filed, a missing safeguard, a process that clearly wasn't anyone's job — and shows you picked it up without being asked, then followed through to a real conclusion rather than just raising a flag and moving on. [!key] "i noticed x wasn't anyone's job, so i made it mine" is close to the highest signal sentence you can say in this category. it shows initiative and follow through in one line. taking on unglamorous work senior engineers are tested on whether they will still do the boring but necessary work once they no longer have to prove themselves with visible projects. a good story here names work that had low visibility and little glory — flaky test cleanup, an outdated runbook, a manual deploy process — and explains why you chose to do it (it was blocking other people invisibly, or it was a recurring source of toil that nobody had prioritised) rather than framing it as altruism alone. weak framing strong framing "i like helping the team" "it was costing us roughly two hours per release in manual steps, so i automated it" "nobody else wanted to do i`,"behavioural/failure-and-learning":`"tell me about a failure" is the highest signal question in most behavioural loops, and also the most commonly fumbled. candidates either pick something too trivial to matter, spin a success story with a fake flaw attached, or over correct into self flagellation that makes an interviewer worry about their resilience rather than their judgement. why this is the highest signal question almost any candidate can describe a success convincingly, because success stories are easy to be generous with yourself about. a failure story forces a choice: be honest about something that went wrong and own your part in it, or reach for a safe, sanitised non answer. interviewers use this question specifically because the quality of honesty is itself the signal — more than the failure's content. [!key] the failure itself matters less than what it reveals about your self awareness and what changed afterward. a moderate failure with a sharp, honest account of the lesson beats a dramatic failure told defensively. choosing a real failure not every setback makes a good story. a good failure candidate is: big enough to matter — a real consequence, not a typo you caught yourself. honestly yours — your decision or action was a meaningful contributing cause, not just "the team missed a deadline." recent enough to be credible and to show current judgement, ideally within the last two to three years. followed by genuine learning — something specific changed in how you work, not a generic "i learned to communicate more." bad failure choice why it fails "i once mistyped a config value" too small, no real `,"behavioural/hiring-manager-round":`the hiring manager round is different from a competency scored behavioural loop — it is closer to a mutual fit conversation, and the questions probe motivation, level, and risk as much as any specific story. treating it like another star round misses half of what's actually being assessed. what this round is actually for hiring managers are usually trying to answer four questions that a panel interview doesn't fully cover: are you motivated for the right reasons, are you actually at the level you're interviewing for, will you fit the specific team and its way of working, and is there any risk they should worry about — a pattern of job hopping, an unresolved conflict with a previous employer, unrealistic expectations about the role. what they ask what they're actually assessing "why are you looking to leave?" risk — is there a pattern, or a reasonable, forward looking reason "why this company?" motivation — did you do the research, or is this generic "what are you looking for next?" fit — does the role actually match what you want "tell me about your biggest accomplishment" level — is the scope consistent with the level you're interviewing for "what are your weaknesses?" self awareness and credibility "what do you want from a manager?" team fit — will this specific manager's style work for you [!key] this round is as much about you evaluating them as the reverse. hiring managers notice candidates who ask nothing back, and it reads as low investment in whether the role is actually a good fit. answering "why are you leaving" without criticising your employer the safest and mos`,"behavioural/questions-to-ask-and-company-prep":`the questions you ask at the end of every interview are not a courtesy slot — they are actively evaluated, and they are also your best tool for finding out whether the role is actually right for you. treating this as an afterthought wastes one of the highest leverage parts of the entire loop. why your questions are part of the evaluation interviewers form an impression from the questions you ask: specific, well researched questions signal genuine interest and preparation, while "no, i think you've covered everything" signals low investment, even if unintentional. good questions also let you tailor follow up conversations — asking a manager about team structure gets a different, more useful answer than asking an ic the same question, so matching your question to the interviewer's role gets you better signal as well as better optics. [!key] prepare more questions than you'll use, tailored per interviewer, and let earlier answers in the loop retire questions that get answered naturally — asking something you were already told is a bigger miss than asking too few questions. a categorised question bank team and role question what it signals about you what the answer signals about them "what does the team's on call rotation actually look like week to week?" you think about sustainability, not just the job title whether on call load is reasonable or a hidden burden "how is work typically divided across the team?" you're thinking about fit and autonomy whether roles are well defined or chaotic "what would success look like in this role after six months?" you think in outcomes, not `,"csharp-dotnet/value-and-reference-types":`every c type is either a value type or a reference type , and that single fact drives how assignment, method calls and equality behave. interviewers use this topic to check whether you actually understand the language or just write code that happens to work. the core distinction a value type ( , , numeric primitives) holds its data directly. a reference type ( , , , array, ) holds a pointer to data allocated elsewhere. assigning a value type copies the bits ; assigning a reference type copies the pointer . [!key] value type = copy the value. reference type = copy the address. every other behavior in this topic — mutation, , boxing, passing to methods — follows from that one sentence. where they live lives entirely on the stack as part of its containing frame. is a reference sitting on the stack, but the object it points to — with its object header, method table pointer and fields — lives on the heap. a struct field inside a class lives inline inside that object on the heap; it only sits on the stack when it is itself a local variable or parameter. [!warning] "structs are on the stack, classes are on the heap" is the interview shorthand, but it is not the full rule. a struct captured in a lambda closure, boxed to , stored as a field of a class, or held in an array of structs on the heap does not live on the stack. the stack/heap distinction is really about storage location of the containing slot , not the type itself. passing to methods by default, both value and reference types are passed by value — meaning the variable's contents are copied into the parameter. for a struct`,"csharp-dotnet/stack-heap-and-memory":`interviewers use "where does this live in memory" questions to separate people who memorized "structs are stack, classes are heap" from people who understand allocation. this page builds the accurate mental model: stack frames, heap objects, boxing, and the tools ( , , returns) that let you control allocation deliberately. the stack the stack is a contiguous, per thread region of memory that grows and shrinks as methods are called and return. each method call pushes a stack frame holding local variables, parameters, and the return address. frames are freed automatically the instant the method returns — no garbage collector involved, which makes the stack extremely cheap. a value type local, a reference type local's pointer , and method parameters all live in the frame. default stack size per thread is 1 mb on windows for a typical .net thread — deep or unbounded recursion overflows it with a fatal that cannot be caught . the heap and object headers the heap is a larger, shared region managed by the garbage collector (gc). every reference type instance allocated there carries a fixed object header overhead before its fields even start. component size (64 bit) purpose sync block index 8 bytes locking ( ), hash code cache type object pointer (method table) 8 bytes identifies the runtime type, vtable fields varies actual instance data, padded for alignment so a class with a single field still costs at least 24 bytes (16 byte header + 4 bytes + alignment padding) — versus a struct with the same field costing exactly 4 bytes inline. this overhead is why wrapping millions of small`,"csharp-dotnet/classes-structs-and-records":"modern c gives you four ways to define a type — , , , — and interviewers expect you to pick the right one for a given scenario, not just recite definitions. this page is a comparison and decision guide, not just a syntax tour. the four options at a glance (class) storage reference (heap) value (inline) reference (heap) value (inline) default equality reference identity field by field (reflection, slow) value equality (generated) value equality (generated) mutability mutable by default mutable by default immutable by convention ( setters) mutable by default; for immutable inheritance full class hierarchy none (implicitly sealed) can inherit other records none (implicitly sealed) copy behavior assignment copies reference assignment copies all fields expression for non destructive copy expression for non destructive copy default prints type name default prints type name auto generated, prints all properties auto generated, prints all properties typical use entities, services, behavior heavy types small immutable values ( , ) dtos, value objects, messages small immutable value objects needing structural equality [!key] is not a fifth kind of storage — it's a set of compiler generated members ( , , , , deconstruction) layered on top of either a (default) or a ( ). the storage question (value vs reference) is orthogonal to the equality question (identity vs value). positional records, with expressions, deconstruction readonly struct and init only setters tells the compiler every field is immutable after construction, which lets it skip defensive copies when the struct is passed w","csharp-dotnet/interfaces-and-abstract-classes":'"interface or abstract class?" is one of the oldest interview questions in the book, and it still matters because the answer shapes how testable, extensible, and coupled your codebase becomes. this page covers the mechanics, the controversial newer features, and the design judgment interviewers actually want to hear. interface vs abstract class aspect interface abstract class instantiable never never members methods, properties, events, indexers; can have default implementations (c 8+) any member; can mix abstract and concrete fields not allowed (only static in newer c ) allowed constructors none yes — run when a derived class is constructed multiple inheritance a class can implement many interfaces a class can inherit only one base class access modifiers on members implicitly public (unless explicit implementation) any access modifier versioning adding a member breaks all implementers (unless given a default body) adding a concrete method is backward compatible typical use contract/capability ("can do x") shared implementation across a family of related types [!key] interfaces answer "what can this type do ?" (a capability, a contract). abstract classes answer "what is this type, and what does it share with its siblings?" (an identity, a partial implementation). reach for an interface first; reach for an abstract class only when you have real shared code to put in the base. multiple inheritance of behavior c forbids multiple class inheritance to avoid the "diamond problem" (ambiguity when two base classes define the same member), but a class can implement any number of int',"csharp-dotnet/keywords-that-get-asked":"some c keywords rarely come up in day to day code but show up constantly in interviews because they test precise understanding rather than familiarity. this page is a fast reference sweep — tables and short examples, not essays — so you can refresh each one in seconds before a round. parameter modifiers: ref, out, in, params keyword direction must assign before use? must assign before return? typical use in and out yes, by caller not required mutate caller's variable out only no yes, required style multiple return values in only (readonly) yes, by caller n/a avoid copying a large struct in (variable count) n/a n/a variable length argument list static, const vs readonly (instance) evaluated compile time runtime (once, at type init or in static constructor) runtime (once, per instance, in constructor) can depend on runtime values no yes yes storage inlined into il at every call site one shared value per type one value per instance versioning risk changing it requires recompiling every dependent assembly safe to change without breaking callers n/a [!warning] values are copied into the il of every assembly that references them at compile time. if library a defines and library b compiles against it, then a is updated to without recompiling b, b still sees 1 until it's rebuilt. this is a real production versioning trap — use for anything that might change across a library boundary. virtual, override, and new (method hiding) this is the classic trick question. on a method hides a base member instead of overriding it — dispatch then depends on the compile time (static) type of the ","csharp-dotnet/generics-boxing-and-collections":`generics and collections questions test whether you understand why the type system is shaped the way it is, not just which method name to call. this page covers the type safety plus performance argument for generics, the variance rules that trip people up, and a practical collection choice reference. why generics exist before generics (pre c 2.0), general purpose collections stored , which meant two costs: no compile time type safety (you could put a into a list meant for , discovered only at runtime via ), and boxing every value type on the way in and out. [!key] generics deliver two benefits at once: compile time type safety (catch mistakes before runtime) and performance (no boxing for value types, no casting for reference types). interviewers want both halves of this answer, not just "type safety." type parameter constraints constraints restrict what a generic type parameter can be, unlocking operations the compiler couldn't otherwise guarantee are valid. constraint meaning enables reference type only can be compared to directly value type only guarantees non nullable, enables wrapping must have a public parameterless constructor inside the generic method must inherit access to 's members on must implement the interface call interface members on non nullable (reference or value) used heavily in internals covariance and contravariance (in/out) variance describes whether a generic interface/delegate with can be treated as (or vice versa) safely. modifier name direction example covariant usable as — you only ever read t out contravariant usable as — you only ever pass t in`,"csharp-dotnet/delegates-events-and-lambdas":"delegates are the backbone of callbacks, linq, and events in c , and interviewers use them to check whether you understand what's really happening under / syntax sugar — not just whether you can use it. this page builds from the delegate type up through events, closures, and the loop variable trap everyone eventually hits. delegate as a type safe function pointer a delegate is a type that represents a reference to a method with a specific signature — think of it as a function pointer with compile time type checking and support for multiple targets. unlike a raw c function pointer, a delegate carries the method's signature (checked at compile time), can wrap an instance method (bundling the target object alongside the method pointer), and can point to more than one method at once (multicast, below). func, action, predicate , , and are pre defined generic delegate types the .net team gave you so you rarely need to declare your own keyword. type signature shape example no parameters, no return parameters, no return parameters, returns (last type argument) one parameter, returns multicast delegates and return values delegates are inherently multicast — appends another method to an invocation list, and invoking the delegate calls each in order. [!warning] with a multicast , only the last delegate's return value is observable through a direct call — earlier ones execute but their results are discarded. if you need every result, iterate and call each manually. events: restricted delegates an is a delegate field with compiler enforced restrictions: outside the declaring class, code","csharp-dotnet/linq-and-deferred-execution":`linq questions in interviews almost always converge on one thing: do you actually understand when a query runs, and do you know the difference between linq to objects and linq to entities. this page covers both, plus the operator reference and ef core pitfalls that come up in senior rounds. query syntax vs method syntax both compile to the same underlying method calls — query syntax is pure syntactic sugar over method (fluent) syntax. method syntax covers more operators than query syntax has keywords for ( , , , , , etc. have no query syntax equivalent), so most production code mixes both or leans fully method syntax. deferred vs immediate execution most linq operators are deferred — they build up a description of the query but don't run it until you actually enumerate the result (via , , , etc.). a minority are immediate — they execute right away and return a concrete result. deferred (lazy) immediate (eager) , , , , , , , , , , , , , [!key] a deferred query is re evaluated every time it's enumerated. this is the source of both a classic bug (multiple enumeration re running expensive work) and a classic feature (a query automatically reflecting the latest state of its source). the multiple enumeration trap if the source is a database query ( ), each enumeration re runs the sql — hitting the database twice for and above. the fix is to materialize once: [!danger] multiple enumeration is worse than just "slow" when the source isn't stable across enumerations — a based filter, a query against a changing database, or a query over a mutating in memory collection can return diffe`,"csharp-dotnet/async-await-fundamentals":'/ is the feature every c interview eventually pokes at, because it is easy to use and easy to misuse. the goal here is to build a mental model precise enough that you can explain why a piece of async code behaves the way it does, not just that it does. what async actually does does not mean "runs on another thread". the compiler rewrites an method into a state machine — a class that implements with a method and fields for every local variable that needs to survive across an . each point becomes a state transition: the method runs synchronously up to the first incomplete awaited operation, then returns control to its caller, and is scheduled to resume once that operation completes. for i/o — a socket read, a database call, a file operation — the underlying os api is asynchronous already. no thread is occupied while the disk or network does its work; the request is registered with the os, and a completion callback (via i/o completion ports on windows) resumes the state machine on a thread pool thread later. this is why async i/o scales: a server can have thousands of in flight requests using a handful of threads. [!key] does not create a thread. for i/o bound work, there is no thread at all between the and the completion callback — the thread is returned to the pool and reused for other work. task, task<t and valuetask type represents allocation use when an asynchronous operation with no result heap allocated fire and track void returning async work an asynchronous operation that yields heap allocated most async apis / a result that is often already available synchronously st',"csharp-dotnet/task-cancellation-and-pitfalls":`writing one correct is easy. writing a whole async pipeline that cancels cleanly, reports errors correctly, and never deadlocks is where interviewers separate people who use async from people who understand it. this page covers the operational side: cancellation, composition, and the mistakes that show up in production incidents. cancellationtoken and cooperative cancellation cancellation in .net is cooperative — nothing forcibly kills a running operation. a owns a ; calling flips a flag that any code holding the token can observe and react to, by throwing (usually via ) or by checking in a loop. [!key] cancellation is a request, not a command. if your code never checks the token — inside a tight cpu loop, or by ignoring the token parameter on an api call — cancellation silently does nothing. linked tokens and timeouts combines multiple sources so that cancelling any of them cancels the linked token — useful for "cancel if the caller cancels or a timeout elapses": and are the two idiomatic ways to express a timeout; a around the awaited call distinguishes "caller cancelled" from "we gave up" if you check which token was the one actually signalled. whenall vs whenany vs waitall api returns blocks caller thread? exception behaviour a that completes when all finish no — awaitable throws the first exception on await; all exceptions available via ( ) a that completes when one finishes no — awaitable the winning task's exception only; others are unobserved unless you also await them yes , blocks throws with all failures (index) yes , blocks throws only if the first completed task`,"csharp-dotnet/threads-tasks-and-the-threadpool":`"what's the difference between a thread and a task?" is one of the most common opening questions in a c concurrency interview, and the honest answer requires understanding three separate layers: the os thread, the managed object, and the thread pool that is usually built on top of. thread vs task vs threadpool concept what it is creation cost lifetime control a dedicated os thread wrapped by a managed object expensive — allocates a 1mb stack (configurable) and os kernel resources you own it fully: start, join, background/foreground, priority a managed pool of reusable worker threads threads created once, reused for many work items managed automatically; you queue work, not threads a higher level abstraction representing a unit of (usually async) work cheap — a lightweight object, typically scheduled onto the thread pool composable: continuations, cancellation, exception handling built in is not a replacement concept for — it's a different abstraction layer. a cpu bound is usually executed by a thread pool thread; an i/o bound method may execute on no dedicated thread at all while it's awaiting. interviewers want you to say this distinction explicitly: describes a unit of work and its completion; describes an execution vehicle. [!key] creating a raw costs roughly 1mb of reserved stack space plus kernel object overhead, and takes on the order of hundreds of microseconds to milliseconds. queuing work to the thread pool reuses an already warm thread and is orders of magnitude cheaper — this is why almost nobody should call directly for everyday work. thread creation cost and wh`,"csharp-dotnet/locks-and-synchronization":`synchronization questions test whether you can reason precisely about shared mutable state — not just "use a lock", but which lock, on what object, and what actually goes wrong when two threads race. this page builds the vocabulary to answer those questions cleanly. race conditions and critical sections a race condition occurs when the correctness of a result depends on the unpredictable timing of two or more threads accessing shared state. a critical section is the piece of code that must not be executed by more than one thread at a time to avoid that race — typically the code between a check and the update that depends on it. looks like one operation but compiles to a read, an add, and a write. two threads can both read the same value before either writes back, and one increment is lost. this is the canonical race condition example in almost every interview. [!key] a race condition doesn't require exotic timing — it requires only that two threads interleave somewhere between reading and writing shared state. it reliably reproduces under load even though it may never show up in a quick manual test, which is exactly why it's dangerous in production. lock / monitor, and what lock compiles to is syntactic sugar over / wrapped in a : associates a lock with an object's sync block (a small header attached to every reference type instance), making any reference type usable as a lock target without a separate lock object type. only one thread can hold the lock on a given object at a time; others calling on the same object block until it's released. what to lock on (and what not to`,"csharp-dotnet/concurrent-collections-and-parallelism":'once you know how to lock correctly, the next question is usually "why write your own locking at all?" — the bcl ships a set of collections and pipelines purpose built for concurrent access, each with a specific shape of problem it solves best. concurrentdictionary allows safe concurrent reads and writes without an external lock, using fine grained internal locking (striped across buckets) rather than one lock for the whole structure. its most misunderstood methods are and . [!danger] the factory delegate passed to / is not guaranteed to run exactly once under contention. if two threads call for the same missing key simultaneously, both factories may execute — only one result is actually stored, and the other is discarded — but any side effects in the discarded factory (a network call, a counter increment, a log write) still happened. if the factory must run exactly once, use as the stored value, or an external lock around the check and create. concurrentqueue, concurrentstack and concurrentbag collection order best for fifo producer consumer where order matters (e.g. work items processed in arrival order) lifo recently added items should be processed first (undo stacks, work stealing) unordered best when items are typically added and removed by the same thread (thread local free lists); poor fit for cross thread producer/consumer optimizes for the case where a thread mostly consumes what it itself produced, using per thread local queues internally — using it as a general cross thread queue is a common performance mistake, since consuming from a bag populated entirely by ot',"csharp-dotnet/exceptions-and-idisposable":"exceptions and show up in nearly every c interview, often as a quick warm up question that turns into a deeper discussion about resource management. getting the details exactly right — stack traces, filters, deterministic disposal — is a fast way to demonstrate solid fundamentals. the exception hierarchy all exceptions derive from . two broad branches matter in practice: (thrown by the runtime/bcl, generally recoverable ish) and (intended for user defined exceptions, though in practice most people just derive from directly — never gained real adoption). exception type meaning an argument's value is invalid for the operation a required argument was an argument is outside its acceptable range the object is in a state that doesn't support the call (e.g. modifying a collection while enumerating) dereferenced a reference — almost always a bug, not something you catch array/index access outside bounds the member exists but this implementation doesn't support it placeholder for unfinished code — should not reach production an operation exceeded its allotted time raised when a cancellation is observed wraps one or more exceptions from parallel/task operations file, stream, or network i/o failure [!key] catch the most specific exception type you can meaningfully handle. catching bare (or worse, ) hides bugs and makes debugging production incidents much harder. try/catch/finally and exception filters always runs — whether the try block succeeds, throws, or even returns early — making it the right place for cleanup that isn't already covered by . exception filters ( ) let you catch co","csharp-dotnet/garbage-collection":`the .net garbage collector (gc) is automatic, but "automatic" does not mean "free to ignore". interviewers use gc questions to test whether you understand what makes an object collectible, why memory leaks still happen in a managed language, and how to reason about latency in a gc based runtime. the generational hypothesis the gc is built on one empirical observation: most objects die young . short lived objects (temporary strings, loop variables, request scoped dtos) vastly outnumber long lived ones (caches, singletons, static data). rather than scanning the entire heap on every collection, the gc organizes memory into generations and collects the youngest, cheapest to scan generation far more often than the rest. generation contains collection frequency typical cost gen 0 newly allocated objects very frequent microseconds — scans a small, mostly dead region gen 1 objects that survived one gen 0 collection less frequent small buffer between short and long lived gen 2 long lived objects (survived gen 1, statics, large caches) rare most expensive — scans the whole surviving graph large object heap (loh) objects ≥ 85,000 bytes (large arrays, big strings) collected with gen 2 not compacted by default — can fragment an object that survives a gen 0 collection is promoted to gen 1; survive again and it's promoted to gen 2. the idea is that the longer an object has lived, the more likely it is to keep living, so it's not worth re checking it as often. [!key] a gen 0 collection only scans gen 0 (and roots pointing into it) — it does not touch gen 1 or gen 2. this is what makes freq`,"csharp-dotnet/clr-jit-and-runtime-internals":`most c developers never need to think about the clr — until an interview asks "what happens between writing and it printing to the screen?" this page traces that pipeline end to end, because that trace is exactly what senior interviewers want to hear. what the clr is responsible for the common language runtime (clr) is the execution engine underneath every .net program. it is responsible for: loading assemblies and resolving their dependencies jit compiling intermediate language (il) into native machine code memory management via the garbage collector type safety enforcement — verifying il doesn't violate the type system exception handling — structured exception propagation across frames security and code access (much reduced in modern .net compared to legacy code access security) interop with unmanaged code via p/invoke and com [!key] the clr is a language agnostic runtime — c , f , vb.net all compile to the same il and run on the same clr. this is exactly why cross language interop within .net (a c library calling an f library) requires no special glue: they share a common type system (the cts) and a common intermediate format. il and metadata the c compiler (roslyn) never produces machine code directly — it produces il (intermediate language, also called msil/cil), a stack based, cpu independent instruction set, packaged into an assembly (a / ) alongside metadata describing every type, method, field, and their signatures. compiles to il resembling: metadata is what makes .net reflective : works because the assembly carries a full description of its own types alongside th`,"databases/sql-joins-and-aggregation":`almost every sql bug traces back to a wrong mental model of when each clause runs. this page fixes that model first, then walks every join type and the aggregation rules that sit on top of it. all examples use t sql (sql server); differences from ansi/mysql/postgres are called out inline. sample schema used in these examples every worked example on this page and the rest of this section reads from the same two tables, so the / pairs recur throughout instead of being re invented per example. employee id employee name department id salary city manager id status 1 aman 1 80000 delhi active 2 anita 1 65000 mumbai 1 active 3 ravi 2 55000 bengaluru 1 active 4 neha 45000 pune 2 inactive note the on : deleting a department doesn't fail or cascade delete its staff, it just detaches them ( becomes ) — neha's row already shows that detached state. sales ( ) has no employees at all, which is what makes it useful for the outer join examples below. logical query processing order sql reads like , but that is the written order, not the execution order. the engine actually processes clauses in this sequence: this explains almost every "why doesn't this work" moment: you cannot use a column alias in — runs before computes it. you can use an alias in — it runs after . cannot reference an aggregate ( ) because aggregates do not exist until / runs — that is what is for. / applies last, after sorting, which is why "top 5 by x" requires an to be meaningful. [!key] memorise the order: from → where → group by → having → select → order by → limit . every "why did my query fail" question is answered `,"databases/window-functions":"window functions compute a value across a set of rows related to the current row without collapsing them into one row , unlike . they power ranking, running totals, top n per group and deduplication queries that come up constantly in interviews. this page uses t sql syntax throughout. anatomy of the over clause every window function is . partition by — optional; resets the window per group, like an implicit that doesn't collapse rows. omit it to treat the whole result set as one partition. order by — required for ranking/offset functions; defines the row sequence the function walks. frame — the subset of rows within the partition the function actually sees, relative to the current row. only meaningful for aggregate window functions; ranking functions ignore it. [!key] a window function runs after / / but before / in logical processing order — it operates on already filtered, already grouped rows, and you cannot filter its result in the same 's clause (it doesn't exist yet). wrap in a cte and filter the outer query instead. ranking functions and how they handle ties given salaries ordered descending: salary row number() rank() dense rank() ntile(2) 90,000 1 1 1 1 90,000 2 1 1 1 80,000 3 3 2 2 70,000 4 4 3 2 row number() — always unique, arbitrary tie break order (whatever doesn't fully disambiguate). use for pagination and deduplication. rank() — ties share the same rank, but the next rank skips (1, 1, 3, 4) — leaves gaps equal to the number of tied rows. dense rank() — ties share the same rank, no gap afterward (1, 1, 2, 3). ntile(n) — splits the partition into roughly equa","databases/subqueries-and-ctes":`subqueries and ctes are both ways to compose a query out of smaller pieces, but they differ in readability, materialisation and — critically — in one specific interaction that causes silently wrong results in production. this page uses t sql syntax throughout. types of subqueries type returns where it's used example scalar single value (1 row, 1 column) anywhere a literal value is valid row single row, multiple columns row comparisons table (derived table) multiple rows/columns / clause correlated re evaluated per outer row / , references outer query a scalar subquery used where a single value is expected must return exactly one row — more than one row raises a runtime error, not a silent truncation. correlated vs uncorrelated — the performance difference an uncorrelated subquery has no reference to the outer query; it can be evaluated once and reused. a correlated subquery references a column from the outer query, so conceptually it must be re evaluated for every outer row. [!note] modern query optimizers frequently rewrite a correlated subquery into an equivalent join or semi join internally, so "correlated = always row by row execution" is not literally true at runtime — but it's still the right mental model for reasoning about worst case cost, and a good one to state before adding "though the optimizer may rewrite this." in vs exists vs join returns duplicate outer rows on multiple matches no no yes, one per match short circuits on first match no (conceptually builds full list) yes n/a null safe with a null containing subquery list no — classic trap yes n/a can pull col`,"databases/sql-query-cheatsheet":'this page is a practice reference, not a tutorial — it assumes you know / / and drills the specific problem shapes that show up repeatedly in sql interview rounds. work through the technique column before looking at the solution. the classic problem list problem technique 1 second highest salary / or 2 nth highest value per group 3 top n per group 4 running total 5 gaps in a sequence /self join to find missing numbers 6 islands of consecutive values row number difference trick 7 consecutive rows matching a condition self join or / chain 8 find duplicate rows 9 delete duplicate rows, keep one + on rn 1 10 employees earning more than their manager self join to itself 11 top earner per department / 12 second most recent order per customer 13 month over month growth 14 percentage of total window as denominator 15 median value or paired row number trick 16 pivot rows to columns inside / , grouped 17 unpivot columns to rows or 18 customers with no orders 19 customers with orders in every month + 20 rank with ties vs vs 21 date bucketing (weekly/monthly) / 22 cumulative distinct count window workaround or subquery 23 first purchase date per customer grouped, or 24 retention: active this month and last self join on month offset 25 average order value per customer segment + + 26 products never ordered / 27 longest streak of daily activity islands technique on date differences 28 deduplicate on insert or unique constraint 29 find the nth row without / support correlated subquery counting rows 30 employees with no direct reports [!key] nearly every "advanced" sql question is a window ',"databases/indexes-explained":`an index is the single biggest lever you have over query performance, and interviewers use it to test whether you understand storage internals or only syntax. this page covers the b+ tree structure, the clustered/non clustered distinction that trips up most candidates, and the concrete reasons an index gets ignored. the b+ tree almost every relational database index (sql server, mysql/innodb, postgres's default) is a b+ tree : a balanced, sorted, multi way tree where all actual data lives in leaf nodes, and internal nodes only hold routing keys. leaf pages are linked to their neighbours, so once you find the starting leaf, a range scan just walks the chain — no re traversal from the root. because the tree is balanced and each node holds many keys (typically hundreds, since a page is 8kb and keys are small), the height stays tiny even for huge tables: a tree of fan out 200 needs only 4 levels to index over 1.6 billion rows. [!key] lookup cost is because each level of the tree eliminates most of the remaining rows — with a fan out of a few hundred, 3–4 page reads can locate a row among billions, versus a full scan reading every page. clustered vs non clustered clustered index non clustered index data storage table's actual rows, physically sorted by the key separate structure; leaf holds key + a pointer back to the row per table exactly one (or zero — a heap) up to 999 in sql server leaf contains the full row indexed columns + row locator lookup for a non indexed column direct — row is right there extra "key/bookmark lookup" step needed typical choice of key primary key, ofte`,"databases/composite-indexes-and-query-design":`a single column index is the easy case; composite (multi column) indexes are where most real world query tuning actually happens, and where interviewers probe whether you understand column order rather than just "add an index." this page works through the leftmost prefix rule, column ordering, and a full before/after query rewrite. the leftmost prefix rule a composite index on is one sorted structure keyed first by , then within each , then within each — like a phone book sorted by last name, then first name, then city. the index can only be seeked using a contiguous prefix of its columns, starting from the left. query predicate uses the index as a seek? why ✅ yes leftmost column alone — valid prefix ✅ yes first two columns — valid prefix ✅ yes, seek + residual range full prefix; range narrows within the seek ❌ no (scan) skips , the leftmost column ⚠️ partial seeks on , then must scan/filter since was skipped ❌ no doesn't touch the leftmost column at all [!key] the index is only as useful as the longest prefix your query's equality/range predicates actually match, left to right . skipping a leading column, or only filtering a later column, drops you back to a scan (or a much less efficient partial seek). column order: equality, then range, then sort when designing a composite index, order columns by how they're used , not alphabetically or by "importance": 1. equality predicates first ( ) — these narrow the seek to a single contiguous range in the tree. 2. range predicate next ( , ) — at most one range column can be usefully seeked; anything after a range column in the key `,"databases/execution-plans":`an execution plan is the optimizer's chosen strategy for running a query, and reading one is the skill that separates "i added an index and it got faster" from "i know exactly why it got faster." this page covers plan reading, the operators worth memorising, parameter sniffing, and a worked diagnosis. all examples use sql server terminology. estimated vs actual plans estimated plan actual plan when it runs compiled without executing the query query actually executes row counts shown estimates from statistics actual rows that flowed through each operator cost never touches real data real i/o, cpu, duration available best use quick check of the chosen strategy, cheap on a huge/slow query diagnosing why a specific run was slow, comparing estimate vs actual the single most useful diagnostic signal is the gap between estimated and actual row counts on the same operator — a large gap means the optimizer's statistics based guess was wrong, and everything downstream (join type, memory grant, operator order) was chosen based on a bad assumption. [!key] always look at the actual plan when diagnosing a real slow query. the estimated plan tells you what the optimizer thought would happen; the actual plan tells you what did happen, including where the estimate was wrong. reading operator order execution plans are drawn right to left, top to bottom, but the data flow — and the order operators actually execute in — runs the opposite way: the rightmost, deepest leaf operators run first, feeding their output as input into the operator immediately to their left. read this as: seek , seek , j`,"databases/transactions-and-acid":`acid is usually recited as a definition; interviewers actually want to know you can explain what breaks without each property. this page walks through each guarantee with a concrete failure scenario, then covers transaction scope, nested transactions, and why distributed transactions across services are a design smell. examples use t sql and c . a database transaction is a sequence of operations executed as a single logical unit of work — everything inside it succeeds together, or the database behaves as if none of it happened. atomicity a transaction is all or nothing: every statement inside it commits together, or none of them do. failure it prevents : transferring money between two accounts as two separate statements — if the process crashes after debiting account a but before crediting account b, the money vanishes. wrapping both in one transaction guarantees that a crash mid way rolls back the debit too. consistency a transaction moves the database from one valid state to another, never violating constraints, triggers, or cascading rules — even mid transaction, the final committed state must satisfy every constraint. failure it prevents : a constraint or a foreign key referencing a non existent department would reject a commit that violates it. consistency in acid is really "the sum of atomicity, isolation, and the schema's own integrity rules" — it's the outcome the other three properties combine to guarantee, which is why it's the property people find hardest to explain crisply. isolation concurrent transactions should not see each other's uncommitted, in progress ch`,"databases/isolation-levels":`isolation level is the single knob that trades correctness guarantees for concurrency, and it is the most reliably asked "do you actually understand concurrency" question in a database interview. this page maps every standard anomaly to the levels that prevent it, contrasts sql server's two flavours of snapshot isolation, and works through a lost update scenario with two concrete sessions. the anomalies anomaly what happens dirty read transaction a reads a row that transaction b has modified but not yet committed; b then rolls back, so a read data that never really existed non repeatable read transaction a reads a row twice; between reads, b commits an update to that row, so a's two reads return different values phantom read transaction a runs the same range query twice; between runs, b commits an insert/delete that changes which rows match, so a sees a different set of rows lost update transactions a and b both read the same row, then both write back a modification based on their (now stale) read; whichever commits second overwrites the first's change entirely write skew a and b each read overlapping data and each modify a different row based on it, and both commits succeed even though the combination violates an invariant neither transaction alone would have broken isolation levels mapped to anomalies level dirty read non repeatable read phantom read lost update write skew read uncommitted possible possible possible possible possible read committed prevented possible possible possible possible repeatable read prevented prevented possible prevented (via locking) possible s`,"databases/locks-and-deadlocks":`any database that guarantees isolation between concurrent transactions has to make some transactions wait for others. locks are the mechanism that enforces that waiting, and any system built on waiting can deadlock — which is exactly why this is a favourite whiteboard topic for senior backend interviews. why transactions need locks a lock is a claim a transaction takes on a piece of data so that conflicting transactions cannot touch it until the claim is released (usually at commit or rollback). this is pessimistic concurrency control : assume conflicts will happen and block them up front. the alternative, optimistic concurrency control , lets transactions proceed and checks for conflicts at commit time using row versions (a / column) or mvcc snapshots — postgres and oracle read committed data this way without taking read locks at all. [!key] locks exist to protect isolation , the "i" in acid. every lock you see in a database is ultimately there to stop one transaction from observing or corrupting another's uncommitted work. lock modes and the compatibility matrix mode short taken for blocks shared s reads ( ) writers, not other readers update u rows a statement might update ( ) prevents two readers both upgrading to x and deadlocking each other exclusive x writes ( / / ) everyone intent (is/ix/six) i signals "i hold a lock somewhere below this level" nothing directly — lets the engine skip scanning every row to check for conflicts compatibility of the core modes when two transactions want the same row : held ↓ / requested → s u x s ✅ ✅ ❌ u ✅ ❌ ❌ x ❌ ❌ ❌ update locks exist `,"databases/normalization-and-denormalization":`normalization is a set of rules for organising columns and tables to minimise data duplication and the anomalies it causes. interviewers use it to see whether you can reason about functional dependencies, not just recite "1nf, 2nf, 3nf" — and whether you know when breaking those rules on purpose is the right call. the relational model in three terms a relational database maps directly onto three simple terms, each with an everyday synonym worth knowing for interviews that phrase the question either way: relation = table — a named collection of rows sharing the same structure. tuple = row = record — one complete set of related values about a single entity. attribute = column = field — one named property every tuple in the relation has a value for. keys: from super keys to surrogate keys before functional dependencies and normal forms make sense, it helps to be precise about what a "key" actually is — the term covers several related but distinct ideas. key meaning super key any set of columns that uniquely identifies a row — can include redundant extra columns. candidate key a minimal super key — remove any column and it stops being unique. primary key the candidate key a designer chooses as the table's main identifier. alternate key any candidate key that exists but wasn't chosen as primary. composite key a key made of more than one column together. surrogate key an artificial identifier (an /auto increment value, or a uuid) with no business meaning, used purely to identify a row. a table can have several candidate keys (e.g. both and uniquely identify an employee) but only `,"databases/nosql-families":`"nosql" is not one technology — it's a label for databases that gave up something a relational engine guarantees (usually joins, fixed schema, or strict transactional consistency) in exchange for horizontal scale, flexible schema, or a data model that matches one specific access pattern better. interviewers use this topic to see if you can match a workload to a family instead of reaching for whatever is trendy. why nosql exists relational databases hit real limits at scale: a single writer node caps write throughput, joins across a sharded table are expensive or impossible, and a rigid schema slows down teams iterating quickly on evolving data. nosql databases trade some of sql's guarantees for one or more of: horizontal scale — data and load spread across many commodity nodes instead of scaling one machine up. flexible schema — records don't need a predefined, uniform shape; fields can vary or evolve without a migration. access pattern first modeling — the data is shaped around how it's queried, not around normalised entities. [!key] every nosql choice is a trade: you give up something (joins, ad hoc queries, strong consistency) to get something else (scale, throughput, schema agility). name the trade explicitly rather than saying "nosql is faster." the families, compared family data model query capability scaling model consistency best fit examples key value opaque value per key get/put/delete by key only near linear horizontal (hash partitioning) tunable, often eventual sessions, caching, feature flags redis, dynamodb, memcached document nested json/bson documents rich q`,"databases/access-pattern-driven-modeling":"relational modeling starts with entities and normalises; nosql modeling at scale starts with queries and works backwards. this is the single biggest mental shift interviewers probe for when discussing dynamodb, cassandra, or any partition oriented store — get it wrong and every other answer in the conversation will sound like relational thinking bolted onto the wrong tool. schema design starts before the keys even before choosing between a relational table and a single table nosql item collection, schema design is a process with a fixed set of inputs and a fixed set of outputs — worth doing explicitly rather than discovering ad hoc while writing the first query. three inputs drive everything that follows: input what it decides data volume whether the data lives on a single machine or needs to be distributed from day one access patterns whether the workload is read heavy or write heavy, and therefore how normalized or denormalized the schema should be consistency requirements whether you need strong consistency (most relational workloads) or can tolerate eventual consistency (most access pattern driven nosql workloads) and every schema design session should produce the same four outputs, in order: the tables or items and their relationships, the keys and constraints that enforce integrity, an explicit normalized or denormalized decision, and an indexing and sharding plan. applied to a real schema, that process turns into a concrete sequence of decisions — pick the database, outline the columns, add primary and foreign keys, decide on indexes, decide whether to denormalize, a","databases/redis-data-structures":`redis is often described as "a data structure server" rather than a cache — the reason it fits so many roles (cache, queue, leaderboard, session store, rate limiter) is that it exposes rich, purpose built data types with predictable complexity, all served from memory by a single threaded event loop. single threaded event loop, and why it's fast redis executes commands on a single thread using an event loop (epoll/kqueue) that multiplexes thousands of client connections without context switching between them for each command. this sounds like it should be slow, but it removes lock contention entirely — no command needs to coordinate with another running "at the same time," because none ever do. combined with in memory storage and o(1)/o(log n) data structures, this is why redis routinely does hundreds of thousands of ops/sec on a single core. newer versions add i/o threads for network read/write, but command execution stays single threaded. [!key] every redis command runs to completion before the next one starts. this is what makes operations like or atomic for free — no locking code required in your application. [!warning] because it's single threaded, one slow command (an unbounded , a huge , or a giant on a million member set) blocks every other client until it finishes. always prefer over , and check commands against realistic collection sizes. the core data types type key commands complexity realistic use case string , , , o(1) counters, cached blobs, feature flags list , , , o(1) push/pop ends, o(n) range task queues, recent activity lists set , , o(1) add/check, o(n) `,"databases/redis-caching-patterns":"caching is simple to describe and easy to get subtly wrong — the ordering of two operations, a missing jitter on a ttl, or an unprotected cache miss stampede are the kind of production incidents that come up directly in interviews. this page covers the patterns and the failure modes together. cache aside (lazy loading) the application owns the cache; redis never talks to the database itself. the ordering on writes matters more than it looks. [!key] on writes, update the database first, then delete the cache key — never update the cache in place. if the delete fails or races with a concurrent read, the worst case is a stale cache that self heals on the next ttl expiry, not permanently wrong data serving from an update that silently didn't apply. write through and write behind pattern how it works read latency write latency risk cache aside app checks cache, falls back to db, populates cache on miss fast after warm up normal (db only) cache and db can briefly disagree after a write write through every write goes to cache and db together, synchronously always fast (cache always warm) slower (two writes, one blocking) simpler consistency, but write latency includes cache round trip write behind (write back) write goes to cache immediately; db write is queued/async fast fastest (db write deferred) data loss risk if the process crashes before the queued write flushes write through suits read heavy data where writes can tolerate a little extra latency; write behind suits extremely write heavy paths (metrics, activity logs) where eventual durability is acceptable. ttl strategy and ","databases/redis-locks-streams-and-scaling":`beyond caching, redis is often reached for as a distributed lock, a lightweight queue, and a horizontally scaled cluster — each of these has sharp edges that make good interview material, because "just use redis for that" is rarely the whole answer. distributed locks with set nx px a basic distributed lock is a single atomic command: (only set if absent) makes acquisition atomic; gives the lock a 30 second expiry so a crashed holder doesn't lock the resource forever. the token (a random uuid, not a constant) is what makes release safe — you must never blindly the key, because if your work took longer than the ttl, the lock might already belong to someone else. release with a lua script that checks the token first: [!danger] without checking ownership is the single most common distributed lock bug: client a's lock expires, client b acquires it, then client a finishes late and deletes client b's lock — now two clients believe they hold it. always compare and delete via lua. lock expiry vs work duration, and fencing tokens the lock ttl is a guess about how long the protected work takes. if the actual work runs longer (a slow gc pause, a network stall), the lock can expire while the client still thinks it holds it , letting a second client acquire it — both now believe they have exclusive access. fencing tokens fix the consequence, not the race itself: the lock service hands out a monotonically increasing number with each acquisition, and the protected resource (e.g., a storage service) rejects any write tagged with a token lower than the highest it has already seen. even if tw`,"devops/docker-fundamentals":"docker made containers usable, but the underlying mechanism is plain linux — namespaces and cgroups, not magic. interviewers use this topic to check whether you understand what is being isolated, because that understanding is what lets you debug a container in production instead of just running commands you memorised. containers vs virtual machines a vm virtualises hardware and runs a full guest kernel; a container shares the host kernel and isolates a process . that single fact explains almost every trade off in the table below. aspect virtual machine container isolation unit full os on virtual hardware process(es) on the host kernel boot time seconds to minutes milliseconds to 1 second image size gbs (full os) mbs to low hundreds of mbs kernel own kernel per vm shared host kernel density per host tens hundreds to thousands isolation strength strong (hardware assisted) weaker (shared kernel attack surface) typical use mixed os, strong tenant isolation packaging apps, microservices, ci [!key] a container is a normal linux process. does not create a magical sandbox — it starts a process with a restricted view of the system, built from namespaces (what it can see) and cgroups (what it can use). the real mechanism: namespaces and cgroups namespaces partition kernel resources so a process believes it has the whole machine to itself: namespace isolates process ids — container's pid 1 is a fresh tree network interfaces, routes, ports filesystem mount points hostname and domain name shared memory, semaphores, message queues uid/gid mapping (root in container ≠ root on host, if ena","devops/dockerfiles-and-builds":`anyone can write a dockerfile that works. interviewers care about whether you can write one that builds fast, produces a small image, doesn't leak secrets, and doesn't run as root — because that is the difference between a dockerfile from a tutorial and one that survives a security review. instruction reference instruction purpose notes sets the base image first instruction (or after used in it); can appear multiple times for multi stage builds executes a command, commits a new layer chain with to avoid extra layers copies files from build context into the image prefer over — explicit and predictable like , plus tar auto extraction and remote url fetch avoid unless you need those extras — surprising behaviour sets the working directory for subsequent instructions creates the directory if missing; avoid in sets an environment variable baked into the image persists at runtime; visible via build time only variable not available at runtime; must be re declared per stage documents listening ports metadata only — does not publish the port fixed executable for the container use exec form so it's pid 1 default arguments, or default command if no overridable at sets the user (and group) subsequent instructions/runtime run as use for non root runtime defines a command docker runs periodically to judge container health surfaces in as healthy/unhealthy [!key] vs : always reach for first. 's "helpful" behaviour (auto extracting tarballs, fetching urls) is a source of surprises and has even been a vector for build time supply chain issues. use only for the one thing truly cannot do. entr`,"devops/docker-networking-and-storage":`most docker interview failures aren't about syntax — they're about not knowing what happens to data when a container dies, or why two containers can't reach each other by name. this covers the networking drivers and storage mechanisms that make multi container local development actually work. network drivers driver scope typical use (default) single host standard isolated networking for local containers single host shares host's network stack — no port isolation, no nat overhead multi host (swarm/cluster) container to container networking across multiple hosts single host, physical network assigns containers a real mac/ip on the physical lan n/a full network isolation — no interfaces at all [!key] the default bridge network (unnamed, created automatically) does not provide dns based service discovery between containers — only a user defined bridge network does. this single fact explains most "why can't my containers talk to each other by name" interview questions. dns and service discovery on a user defined bridge network, docker runs an embedded dns server that resolves container names (and compose service names) to their current internal ip automatically. on the legacy default network, containers can only reach each other by ip (or via the deprecated flag), which is why the user defined bridge is the recommended default for anything beyond a single container. port publishing vs expose in a dockerfile is documentation only. at (or in compose) creates an actual iptables/nat rule mapping a host port into the container's network namespace. containers on the same user defined `,"devops/kubernetes-architecture":`kubernetes interviews rarely test whether you can recite object names — they test whether you understand the reconciliation loop , because that single idea explains why a deployment self heals, why is safe to re run, and why a misbehaving controller can quietly fight your changes forever. control plane vs node components the control plane makes cluster wide decisions; nodes run the actual workloads. component location responsibility api server control plane front door — validates and persists all requests, the only component that talks to etcd directly etcd control plane distributed, consistent key value store — the single source of truth for cluster state scheduler control plane watches for unbound pods, decides which node they should run on controller manager control plane runs core reconciliation loops (deployment, replicaset, node, etc.) cloud controller manager control plane talks to the cloud provider api (load balancers, volumes, node lifecycle) kubelet node agent that ensures containers described in assigned pod specs are actually running kube proxy node programs iptables/ipvs rules implementing service virtual ips container runtime node actually pulls images and runs containers (containerd, cri o) via the cri [!key] the api server is the only component allowed to read or write etcd directly. every other component — scheduler, controllers, kubelet — interacts with cluster state exclusively through the api server's watch/list/update api. this is why the api server is the single point of truth for "is the cluster healthy right now." the declarative model and reconcili`,"devops/workloads-and-scheduling":"workload questions test whether you know which controller fits a given shape of problem and how kubernetes actually rolls out and schedules pods — not just whether you can name deployment, statefulset, and daemonset. the mechanics of rolling updates and scheduling are where most real interview depth lives. the pod: the unit of scheduling a pod is the smallest deployable unit in kubernetes — never a single container. it groups one or more containers that are always scheduled together on the same node, share a network namespace (same ip, can reach each other via ), and can share volumes. init containers run to completion, in order, before any main container starts — used for setup work (waiting for a dependency, running a migration, populating a shared volume). sidecar containers run alongside the main container for the pod's whole lifetime — a log shipper, a service mesh proxy, a config reloader. [!key] you never schedule a container directly — you always schedule a pod. multi container pods exist specifically for containers that must share a lifecycle, network namespace, and node — if two components scale independently or fail independently, they belong in separate pods (and usually separate deployments), not the same pod. workload controllers compared controller identity per replica ordering use case deployment interchangeable (no stable identity) no ordering guarantees stateless services, web apis statefulset stable, ordinal identity ( , ) + stable storage ordered create/scale/delete databases, anything needing stable network identity or per replica storage daemonset one ","devops/services-ingress-and-config":`services and probes are where kubernetes networking interviews concentrate, because the failure modes are subtle: a service with zero endpoints looks identical to a working one until you check, and a misconfigured probe causes restart loops that look like application bugs. this also covers securing configuration correctly, since "secrets are just base64" is one of the most commonly missed nuances. service types a service is a stable virtual ip and dns name that load balances traffic to a dynamic set of pods, selected by label selector. type behaviour typical use (default) stable virtual ip, reachable only inside the cluster internal service to service traffic opens the same static port on every node, forwarding to the service simple external access without a cloud lb, dev/test provisions a cloud load balancer pointing at the service (via cloud controller manager) production external access on a cloud provider pure dns cname to an external name, no proxying referencing an external service (e.g. a managed db) by an in cluster name headless ( ) no virtual ip — dns returns pod ips directly statefulsets, clients that need direct per pod addressing how kube proxy implements services kube proxy runs on every node and watches services/endpoints via the api server, then programs the node's packet forwarding rules — historically , increasingly for better performance at scale — so that traffic to a service's virtual ip is transparently dnat'd to one of the backing pod ips. this means a service isn't a running process or a proxy server in the traffic path at all; it's a set of kernel l`,"devops/autoscaling-and-resource-limits":"every container in kubernetes carries two numbers — requests and limits — and misunderstanding what each one actually does is the single biggest source of both wasted spend and mysterious production incidents. this page covers how the scheduler and kubelet use those numbers, the asymmetry between cpu throttling and memory oomkill, and the four autoscalers you are expected to reason about: hpa, vpa, keda and the cluster autoscaler. requests and limits do different jobs a request is a promise used only at scheduling time: the scheduler will not place a pod on a node unless the node has that much allocatable capacity free. a limit is a runtime ceiling enforced by the kubelet and container runtime through cgroups, long after the pod is already running somewhere. field enforced by when effect cpu request kube scheduler pod placement guarantees a cfs share; unused share can be borrowed by others cpu limit kubelet / cgroups (cfs quota) continuously at runtime throttles the process once quota for the period is used memory request kube scheduler pod placement reserved against node allocatable memory memory limit kubelet / cgroups (oom killer) continuously at runtime kills the process (sigkill) on breach [!key] requests decide where a pod runs. limits decide what happens to it once it's already there. they are not two ends of the same knob. cpu throttling vs memory oomkill — the crucial asymmetry cpu is a compressible resource: exceeding your cpu limit does not crash anything, it just slows you down. the kernel's cfs bandwidth controller lets you burn your quota within a 100ms period","devops/deployment-strategies":'"deployment" and "release" are not the same thing, and the strategy you pick determines how much blast radius a bad change gets before anyone notices. this page compares the standard deployment strategies on the axes interviewers actually probe — downtime, rollback speed, cost, risk — then goes deep on the two questions that separate a mid level answer from a senior one: how you migrate a database safely mid rollout, and how feature flags let you deploy without releasing. comparing the strategies strategy downtime rollback speed infra cost risk complexity traffic split recreate yes (full outage) slow (redeploy old) lowest high (all or nothing) lowest none — old stopped, new started rolling none (if healthy) slow (reverse rollout) low medium low by pod, gradually replaced blue green none instant (flip router) high (2x capacity) medium (all traffic at once) medium all or nothing, atomic cutover canary none fast (shift back to 0%) medium low high percentage based, gradual a/b none fast medium low (business risk, not availability) high split by user segment, not %random shadow / dark launch none n/a (no user impact) medium high (duplicate load) lowest (real traffic, no real responses) high mirrored, response discarded [!key] rolling, blue green and canary are about infrastructure risk . a/b is about business risk . shadow is about validating behaviour with zero user facing risk . conflating them in an interview answer is a tell. progressive delivery with automated analysis progressive delivery automates the canary judgment call: instead of a human watching a dashboard, a tool (',"devops/ci-cd-pipeline-design":`a ci/cd pipeline is a risk reduction machine, not a formality — every stage exists to catch a specific class of problem as cheaply and early as possible. this page covers pipeline shape, the "build once, promote everywhere" principle that most junior pipelines get wrong, secrets and identity in automation, and the practices (trunk based development, flaky test quarantine, dora metrics) that come up constantly in senior interviews. stages of a good pipeline each stage answers one question: build (does it compile?), unit tests (does the logic work in isolation?), static analysis (is it maintainable, does it violate known rules?), security scan (does it ship a known vulnerability or secret?), package (is the deployable artefact reproducible?), integration tests (does it work with real dependencies?), approval (should a human or policy sign off?), smoke test (did the deploy actually work?). [!key] order stages cheapest and most likely to fail first. a typo should fail at "build" in 30 seconds, not at "integration tests" in 20 minutes. fast feedback and the ten minute rule the ten minute rule: the feedback loop from commit to a pass/fail signal on the core checks (build, unit tests, static analysis) should stay under ten minutes, because beyond that developers context switch away and stop treating the pipeline as immediate feedback. slower stages (integration tests against real infra, security scans, load tests) are pushed later in the pipeline or run asynchronously/in parallel, so they don't block the fast signal. feedback loop target latency what lives here local (pre commit) `,"devops/gitops-and-platform-practices":`gitops takes the idea of "infrastructure as code" one step further: instead of a pipeline pushing changes to your cluster, an agent inside the cluster continuously pulls the desired state from git and reconciles reality to match it. this page covers the principles, the push vs pull trade off, repository structure, secrets, and the platform engineering practices that build on top of gitops — plus the honest case for when it's more process than a team actually needs. the four gitops principles 1. declarative — the entire system's desired state is expressed declaratively (kubernetes manifests, helm values, kustomize overlays), not as a sequence of imperative commands. 2. versioned and immutable — that desired state lives in git, giving you a full audit trail, diffable history, and the ability to check out any prior state exactly. 3. pulled automatically — an in cluster agent pulls changes from git; nothing outside the cluster needs standing write access to push into it. 4. continuously reconciled — the agent doesn't just apply once; it continuously compares live state to git and corrects any divergence, on a loop, forever. [!key] the principle people forget is 4. gitops isn't "we deploy via a git triggered pipeline" — that's still push based cd with extra steps. gitops means the cluster is continuously pulled back toward whatever git says, even hours after a manual change, with no new commit needed to trigger the correction. push vs pull deployment push based (traditional cd) pull based (gitops) who initiates ci pipeline, from outside the cluster an in cluster agent (argocd/fl`,"devops/terraform-fundamentals":`terraform's entire value proposition rests on one idea — you declare the state you want, and a plan/apply cycle figures out the diff — so most interview questions are really testing whether you understand how that diff is computed and what can go wrong when it's wrong. this page covers the desired state model, the hcl building blocks, and the plan reading skill that separates someone who's used terraform from someone who trusts it in production. declarative iac and the desired state model terraform is declarative : you describe the end state you want (a vm, a subnet, three replicas of a queue), not the steps to get there. terraform compares that desired state against the last known state file and the real infrastructure, computes a diff, and executes only the operations needed to close the gap. this is fundamentally different from an imperative script that runs the same commands every time regardless of current reality. [!key] the state file is terraform's memory of "what i last created." without it (or with a stale one), terraform cannot correctly compute a diff — this is why state management, covered on the next page, is not optional plumbing. providers and provider versioning a provider is a plugin that translates hcl resource blocks into api calls for a specific platform (aws, azure, github, datadog). providers ship independently of terraform core and evolve their own resource schemas, so pinning versions matters — an unpinned provider can silently change a resource's default behaviour between runs. [!warning] with no upper bound means a future major version bump can si`,"devops/terraform-state-and-modules":"the state file is the part of terraform most people ignore until it bites them — it is the single source of truth terraform uses to map your configuration onto real world resource ids, and mishandling it causes some of the worst outages in infrastructure as code. this page covers state mechanics, the operations you use to fix state when it drifts from reality, and how to structure modules so a growing codebase stays composable instead of becoming an unmaintainable monolith. what state is and why it exists terraform state is a json record mapping each resource block in your configuration to the real world object it created — a specific cloud resource id, its last known attribute values, and dependency metadata. terraform needs this because hcl alone doesn't identify which real object a block refers to; without state, every would have to guess or ask, or worse, assume nothing exists yet and try to recreate everything. [!key] state is not a cache you can casually regenerate. it is the only link between your configuration and the resources that actually exist — lose it, and terraform's next plan will not know those resources are yours. remote backends and state locking storing state as a local file works for a solo experiment and fails the moment a second person or a ci job runs terraform concurrently — two applies racing against the same local file corrupt it or silently overwrite each other's changes. a remote backend (azure storage, s3 + dynamodb, terraform cloud) centralises the state file and adds locking : while one run holds the lock, any concurrent or blocks or fails fa","dsa/complexity-analysis":`complexity analysis is the language interviewers use to decide whether your solution is "good". you will be asked for the complexity of every solution you write, so it needs to be automatic — not something you derive nervously at the end. why interviewers ask this stating complexity proves three things at once: 1. you understand what your own code actually does. 2. you can compare two designs without running them. 3. you know when to stop optimising — a senior signal. [!key] the phrase interviewers wait for is: "this is o(n log n) time and o(n) space because …" — the because matters more than the letters. the mental model big o describes how cost grows as input grows. it ignores constants and lower order terms, because those stop mattering as gets large. the three notations notation means interview usage upper bound — "grows no faster than" what you almost always state lower bound — "grows at least as fast as" rare; useful for proving a problem is hard tight bound — both say this when the bound is exact in practice, say o(...) and mean θ(...) . nobody will correct you. formally, means there exist constants and such that for all — i.e. beyond some point, dominates up to a constant factor. visually, the algorithm's actual cost curve stays under a scaled copy of once is large enough: the same three letters are often mapped onto the three "cases" of an algorithm's behaviour: best case is usually expressed as , worst case as , and average case as . insertion sort, for example, is best case (already sorted), worst case (reverse sorted), and on average over random inputs. the grow`,"dsa/arrays-and-strings":`arrays and strings are the first data structure in almost every interview, and the easiest place to lose points on details you already know. the bar is not "can you use an array" — it is "do you know why allocates, why is amortised o(1), and which of six near identical patterns fits this exact problem". memory layout an array is a contiguous block of memory : element lives at , which is why indexing is o(1) — it's arithmetic, not search. this contiguity is also why arrays are cache friendly: reading pulls a whole cache line, so is very likely already in l1 cache. a linked list gives up this contiguity for o(1) insert — nodes scatter across the heap, so traversal causes a cache miss per node. this is the real reason arrays beat linked lists in practice even when both are "o(n)": constant factors from cache locality dominate at interview sized inputs. [!key] array access is o(1) because it's a memory address calculation. everything else about arrays — insert cost, resize cost — follows from "contiguous and fixed size". fixed vs dynamic arrays a raw array ( in c ) has a size fixed at creation. a dynamic array ( ) wraps a raw array and reallocates when it runs out of room. aspect fixed array dynamic array ( ) size fixed at creation grows as needed append at end n/a — must pre size amortised insert at index shift shift random access extra memory none up to 2x used capacity (slack) amortised growth when a is full, it allocates a new backing array — typically double the size — and copies every element over. that single append is , but it only happens when capacity doubles, so it h`,"dsa/hashing-maps-and-sets":`hashing is the single most useful idea in interview coding: it turns an or scan into average lookup. interviewers expect you to reach for a / instinctively, and to be able to explain exactly why the "o(1)" claim is only true on average. how a hash table works a hash table stores key value pairs in an array of buckets . a hash function turns a key into an integer; the table takes that integer modulo the bucket count to pick a bucket index. lookup, insert and delete all start by computing the hash and jumping straight to that bucket — no scanning the whole table. two different keys can hash to the same bucket — a collision . what happens next is the part interviewers probe: strategy how it works trade off separate chaining each bucket holds a linked list (or small tree) of entries that collided simple, degrades gracefully, extra pointer overhead open addressing on collision, probe the next slot (linear, quadratic, or double hashing) until an empty one is found better cache locality, but clustering can hurt as load factor rises .net's uses a chaining like scheme (an array of entries with an internal linked "next" index per bucket), while some other languages' hash sets use open addressing. either way, the guarantee is the same: o(1) average , degrading toward if too many keys collide. [!key] a hash table is an array plus a function that maps keys to array indices. everything else — chaining, probing, resizing — exists to handle collisions cheaply. load factor and resizing load factor = . as it rises, collision chains get longer and lookups slow down. when load factor crosses a`,"dsa/linked-lists":"linked lists rarely appear in production code paths you'd write today, but they remain a staple interview topic because they test something specific: can you manipulate pointers correctly under pressure without an off by one or a lost reference. the good news is that almost every linked list question is one of five reusable techniques. node anatomy and list shapes a linked list is a chain of nodes, each holding a value and a reference to the next node (and, for doubly linked lists, the previous one too). variant structure notes singly linked each node points to next only simplest; no backward traversal doubly linked each node points to next and previous o(1) removal given a node reference; in c circular last node points back to head (or first) used for round robin buffers, never null [!key] almost every linked list bug is a pointer ordering bug: reassigning before you've saved the reference you needed. always capture what you need into a local variable before mutating pointers. insert and delete complexity operation array singly linked list insert/delete at front shift insert/delete at back (with tail pointer) amortised with tail, else insert/delete at arbitrary position shift to find + to splice access by index the headline trade: linked lists win at the front , arrays win at random access . everywhere else they're comparable once you count the search needed to reach an arbitrary linked list position. the dummy head technique inserting or deleting at the head of a list requires special casing, because the head has no predecessor to update — unless you invent one. a dummy (","dsa/stacks-queues-and-deques":`stacks and queues are simple to define — lifo and fifo — but the interview value is in the patterns built on top of them: monotonic stacks, min stacks, and deque based sliding windows all show up repeatedly across "hard" problems that are actually easy once you recognise the shape. lifo vs fifo a stack is last in first out: the most recently added element is the first one removed. a queue is first in first out: elements leave in the order they arrived. a deque (double ended queue) supports insert/remove at both ends, so it can act as either. structure add remove c type stack — top — top queue — back — front deque / / [!key] stack = "undo history" (last action is first to reverse). queue = "waiting line" (first come, first served). every use case maps back to one of those two mental models. array vs linked implementations both stacks and queues can be backed by an array or a linked list, with the same trade offs seen elsewhere: backing push/pop or enqueue/dequeue notes dynamic array amortised at the relevant end a naive array backed queue needs a circular buffer , or dequeue is (shifting everything left) linked list at head/tail (with tail pointer) no resizing, but per node pointer overhead and worse cache locality c 's and are array backed with internal circular buffer logic for the queue, so both give amortised operations without you needing to hand roll anything. [!warning] a naive queue built on using for dequeue is per operation — every remaining element shifts left. use (circular buffer internally) instead of reinventing one on top of . monotonic stack: next greater el`,"dsa/heaps-and-priority-queues":`a heap answers one question fast, repeatedly: "what's the current min (or max)?" that single capability — insert, peek, remove extreme — powers scheduling, top k problems, heap sort, graph algorithms (dijkstra's, prim's), and merging sorted data. interviewers expect you to reach for the moment a problem mentions "kth", "top", "smallest/largest k", or "merge sorted". binary heap as an array a binary heap is a complete binary tree (every level full except possibly the last, filled left to right) stored directly in an array — no explicit pointers needed, because a node's children and parent are computable from its index. node at index formula left child right child parent a min heap keeps every parent ≤ its children (root is the minimum); a max heap flips the inequality. this is a weaker ordering than a bst — siblings have no defined order relative to each other — which is exactly why heap operations are cheaper than bst ones for "give me the extreme value" but can't do full sorted traversal or arbitrary search in . [!key] a heap is not sorted — it only guarantees the root is the min (or max). that relaxed invariant is what makes insert and remove extreme instead of . sift up and sift down insert appends to the end of the array, then sifts up : swap with the parent while the heap property is violated. remove min swaps the root with the last element, shrinks the array, then sifts down : swap with the smaller child while violated. both operations move along a single root to leaf path, so both are — bounded by the tree's height. deleting an arbitrary element, not just the root re`,"dsa/binary-trees":'binary trees are where recursion stops being an abstract idea and becomes the natural tool. nearly every tree problem is a variation of "combine information from the left subtree and the right subtree at each node" — once that template is automatic, the hard part becomes identifying what exactly to combine. terminology a tree is a hierarchical structure of nodes connected by edges, with a single root and zero or more children hanging off each node — no cycles, and every node except the root has exactly one parent. general tree term meaning root the single topmost node with no parent parent / child a node that owns a child; the child it owns sibling nodes that share the same parent leaf a node with no children internal node a node with at least one child subtree a node together with all of its descendants degree the number of children a node has a binary tree restricts degree to at most 2 per node, and gives the two children fixed roles — left and right — which is what makes the recursive "combine left and right" template possible in the first place. binary tree specific term meaning depth of a node number of edges from the root to that node height of a node number of edges on the longest path from that node down to a leaf balanced for every node, the heights of its left and right subtrees differ by at most 1 complete every level is fully filled except possibly the last, which fills left to right full every node has 0 or 2 children (never exactly 1) perfect full and every leaf is at the same depth [!key] height and depth are measured in opposite directions: depth counts down',"dsa/binary-search-trees":`a binary search tree earns its keep by giving you search, insert and delete while also giving you sorted order for free via an inorder traversal — no other structure covered so far does both. the catch is that " " silently depends on the tree staying roughly balanced, which is where avl and red black trees come in. the bst invariant for every node, all values in its left subtree are smaller, and all values in its right subtree are larger — and critically, this must hold for the entire subtree, not just the immediate children. property guarantee left subtree of any node all values node's value right subtree of any node all values node's value inorder traversal produces values in ascending sorted order duplicates typically disallowed, or consistently routed one direction (e.g., always right) [!key] the invariant is about the entire subtree , not just parent vs child. a node with a smaller left child and larger right child isn't automatically a valid bst — a deep descendant could still violate the global ordering relative to an ancestor further up. search and insert both are the same idea: compare against the current node, and go left or right, discarding the other half of the tree at every step — the same principle as binary search on a sorted array, just realised as pointers instead of index arithmetic. both are where is the tree's height — if balanced, if degenerate. delete: the two child case deletion has three cases, and only one is genuinely tricky: node to delete has action no children simply remove it one child splice the child up to replace it two children replace its`,"dsa/tries":"a trie (prefix tree) is the structure interviewers reach for the moment a problem mentions prefixes — autocomplete, spell check, longest common prefix, word search on a board. its entire value proposition is sharing common prefixes across many strings so you never re scan them. structure each node represents one character position and holds a map (or fixed size array) of children, plus a flag marking whether a complete word ends there. the root represents the empty prefix. the diagram above encodes , , and — nodes marked are end of word markers. notice is shared structurally between and : that shared prefix is stored exactly once, which is the whole point. [!key] a trie's cost is proportional to the length of the key , not the number of keys stored. searching for a word of length l is , whether the trie holds ten words or ten million. node children: map vs fixed array representation lookup memory when to use average only allocates for characters actually present unicode, large or sparse alphabet (fixed array) , no hashing overhead fixed 26 slots per node regardless of usage known small alphabet (lowercase english letters) the array version is faster in practice (array indexing beats hashing) and is the standard choice when the problem guarantees lowercase letters only — a detail worth stating explicitly to show you're thinking about constants, not just asymptotic complexity. insert, search, startswith operation time space insert worst case (new path for a fully novel word) search startswith = length of the word/prefix. note that and share almost identical logic — just adds ","dsa/graph-representations-and-traversal":`almost every graph interview question is a traversal in disguise — connected components, cycle detection, bipartite checks and multi source shortest paths are all bfs or dfs with one extra bookkeeping trick layered on top. get comfortable with the representation choices and the bfs/dfs trade off first; everything else follows. terminology formally, a graph is a pair , where is the set of vertices (nodes) and is the set of edges connecting them. term meaning vertex a single node in the graph edge a connection between two vertices degree the number of edges touching a vertex in degree / out degree in a directed graph, the number of edges entering / leaving a vertex weight a cost, distance, or capacity value attached to an edge path a sequence of edges connecting a sequence of vertices cycle a path that starts and ends at the same vertex directed, undirected, weighted type edge meaning example undirected connection goes both ways friendship graph, road network (two way street) directed connection has a direction follows on social media, task dependencies, one way streets weighted edge carries a cost/distance/capacity road network with distances, network with bandwidth unweighted every edge costs the same (implicitly 1) most traversal only problems directed edges ( ) mean "a points to b, not necessarily the reverse". an undirected edge ( ) is equivalent to two directed edges, one each way — that equivalence is exactly how you'd implement an undirected graph on top of a directed representation. [!key] "graph problem" almost always decomposes into: pick a representation, pick bfs`,"dsa/union-find":`union find (disjoint set union, dsu) answers one question extremely fast, repeatedly: are these two elements in the same group? it quietly underlies cycle detection, kruskal's mst, and any problem phrased as "merge these into groups and query them later." what it models union find maintains a forest — one tree per component. every element points to a parent; the root of a tree is that component's representative. two elements are connected exactly when they share a root. it supports two operations: find(x) — walk parent pointers up to the root, i.e. the representative of x's component. union(a, b) — merge the components containing and by attaching one root under the other. a naive implementation can degrade to per operation if unions always build a long chain. two independent optimisations fix that. union by rank / size always attach the smaller (or shallower) tree under the larger one's root, instead of the reverse. this keeps tree height bounded at — you never let a big tree hang off a small one. path compression during , re point every node visited on the way up so it hangs directly off the root . the path only needs walking once; after that, lookups on it are . [!key] combined, operations on elements cost total, where is the inverse ackermann function. for any that fits in the observable universe, — so this is "near o(1)" in practice, not just asymptotically. implementation returning is the whole trick behind cycle detection: if two endpoints of an edge already share a root, that edge would create a cycle rather than connect anything new. complexity picture optimisation `,"dsa/topological-sort":`topological sort answers one question: given a set of tasks with "must happen before" dependencies, in what order can you actually run them? it only exists for a directed acyclic graph (dag) — the moment a cycle appears, no valid ordering exists, and detecting that is half of what this topic tests. the dag prerequisite a topological order is a linear ordering of vertices such that for every directed edge , appears before in the ordering. this is only possible when the graph has no directed cycle — a cycle would force some node to come before itself. a valid order here is (or — multiple valid orders can exist whenever two nodes have no dependency relationship between them). [!key] a topological order is generally not unique . if the interviewer asks for "a" topological order, any valid one is acceptable; if they ask for "the lexicographically smallest" one, that constrains you to a specific algorithm choice — see below. kahn's algorithm (bfs on in degrees) compute each node's in degree (number of incoming edges). start a queue with every node that has in degree 0 — nothing blocks them. repeatedly pop a node, add it to the order, and decrement the in degree of its neighbours; whenever a neighbour's in degree hits 0, it becomes unblocked and joins the queue. at the end is the cycle signal: some nodes never reached in degree 0 because they were stuck waiting on each other in a cycle. dfs based ordering with post order reversal alternative approach: run dfs from every unvisited node, and record each node after all of its descendants have been fully explored (post order). reversi`,"dsa/shortest-path-algorithms":"every shortest path question is really a question about the shape of the weights : are they all equal, all non negative, or can they go negative? answer that first and the algorithm picks itself. interviewers use this family to test whether you reach for the simplest correct tool rather than always defaulting to dijkstra. choosing the right algorithm n (vertices) weighted? negative edges? pick any no — bfs ≤ 400–500 yes maybe, all pairs floyd warshall up to 10⁵ yes no dijkstra up to 10³–10⁴ edges yes yes bellman ford [!key] dijkstra is a greedy algorithm: it commits to the shortest distance to a node the moment it is popped, assuming nothing later can make it shorter. negative weights break that assumption — that is the whole story of why dijkstra fails on them. bfs: shortest path in unweighted graphs when every edge costs the same, the fewest number of edges is the shortest path, so a plain level order bfs suffices — no weights, no priority queue. dijkstra: non negative weights dijkstra maintains a min heap of and repeatedly finalises the closest unvisited node, relaxing its outgoing edges — updating a neighbour's distance if a shorter path was just found. [!warning] dijkstra fails with negative edges because it never revisits a finalised node. a negative edge discovered later could shorten a path to a node already popped, but the algorithm has no mechanism to reopen it — it can silently return a wrong, too large answer instead of erroring. bellman ford: negative weights and cycle detection bellman ford relaxes every edge , times. after rounds, all shortest paths (which us","dsa/minimum-spanning-trees":"a minimum spanning tree connects every vertex of a weighted, undirected graph with the smallest possible total edge weight and no cycles. interviewers use mst questions to check two things: can you apply union find correctly, and can you justify why a greedy choice is safe here when greedy usually needs proof. what makes something a spanning tree a spanning tree of a graph with vertices has exactly edges, touches every vertex, and contains no cycle. a minimum spanning tree is the spanning tree (there can be several) whose edge weights sum to the least possible value. msts are only defined for connected graphs; a disconnected graph instead has a minimum spanning forest , one tree per component. [!key] both kruskal and prim are greedy, and greedy is normally suspicious — but mst is one of the few problems where a local greedy choice is provably globally optimal, thanks to the cut property . the cut property and the cycle property these two properties are why the greedy algorithms below are correct, not just heuristics. property statement used by cut property for any partition of vertices into two sets, the minimum weight edge crossing the cut is in some mst prim (grows one side of a cut at a time) cycle property for any cycle, the maximum weight edge in that cycle is not in any mst (unless tied) kruskal (skips the edge that would close a cycle, which is always the heaviest one added last) [!tip] if asked to prove kruskal is correct, cite the cycle property: adding edges in increasing weight order and rejecting anything that closes a cycle means the rejected edge is always the","dsa/two-pointers":"two pointers is the simplest pattern that consistently turns an nested loop brute force into . the trick is not the code — it is recognising when moving two indices together is valid, which depends on the array being sorted or the condition being monotonic. two families there are two distinct shapes, and interviewers expect you to name which one you are using. variant movement typical use requires sorted input? opposite ends starts at 0, at end, they close inward pair sum, container with most water, palindrome check usually yes same direction (slow/fast) both start at 0, scans ahead, marks a write position in place removal, partitioning, cycle detection no [!key] two pointers work only when moving one pointer provably cannot miss the answer . on a sorted array, moving right when the pair sum is too small is safe because every pair involving the old and anything smaller than the current was already too small. that argument, not the code, is what you should say out loud. sorted array pair sum given a sorted array, find two numbers that sum to a target. this is versus brute force, or time / space with a hash set if the array is unsorted and you cannot sort it (sorting destroys original indices). three sum with de duplication fix one element, then two pointer the rest. sorting first makes both the search and the de duplication trivial. [!tip] the de duplication trick is always the same: after recording a match, skip past every equal neighbour before moving on. say this explicitly — it is the detail that separates a working solution from one with duplicate triplets. container wi","dsa/sliding-window":`sliding window is what two pointers becomes when the thing you are tracking is a range , not just two positions. any time a problem asks about the best contiguous subarray or substring, and a brute force would re scan overlapping ranges, this pattern collapses it to . fixed vs variable windows window type size typical trigger phrase example fixed constant , given up front "of size k", "every k elements" max sum subarray of size k variable grows and shrinks based on a condition "longest/shortest substring such that...", "at most k distinct" longest substring without repeating characters [!key] every variable window follows the same template: expand right to include new data, then contract left while some invariant is violated (or satisfied, if you're minimising) . the invariant — not the code — is what changes between problems. the expand/contract template each element enters the window exactly once (when reaches it) and leaves exactly once (when passes it), so total work across the whole run is , even though it looks like a nested loop. longest substring without repeating characters note this variant jumps directly instead of a loop — both are valid; jumping is an optimisation once you know exactly how far to move. minimum window substring the classic hard variant: find the smallest window in containing every character of (with multiplicity). [!tip] say the invariant out loud before coding: "formed == required means the window currently satisfies the frequency requirement for every needed character — i only shrink while that stays true, recording the best window each time i`,"dsa/prefix-sum-and-difference-arrays":`prefix sums trade a one time (or ) build for range queries afterward. difference arrays are the mirror image — they trade range updates for a single rebuild at the end. together they cover most "range" questions that would otherwise be re summed from scratch every time. 1 d prefix sums holds the sum of everything up to and including index . a range sum is then one subtraction. [!key] the identity to memorise: . almost every prefix sum problem is a variation on subtracting two running totals to isolate a range. 2 d prefix sums same idea extended to a grid, using inclusion exclusion to avoid double subtracting the overlapping region. subarray sum equals k (with a hash map) range sum queries need the array to be static (immutable) once built. a different family of problems — "how many subarrays sum to exactly k" — uses the same prefix sum identity but combines it with a hash map, because the array is scanned once rather than queried repeatedly. the trick: a subarray sums to exactly when , i.e. . instead of storing the whole prefix array and scanning it for matches ( ), keep a running count of every prefix value seen so far and look up — . [!tip] the seed is the detail interviewers watch for. it accounts for subarrays that start at index 0 — without it, you silently miss every subarray whose sum from the very beginning equals . count of subarrays divisible by k same identity, mod arithmetic instead of exact match: two prefixes with the same remainder mod k bound a subarray divisible by k, because exactly when . [!warning] c 's operator can return a negative result for negative `,"dsa/binary-search":`binary search is simple in concept and notoriously easy to get subtly wrong in practice. the fix is to memorise one exact template per use case rather than reinventing the loop bounds each time — interviewers notice when the boundary handling is automatic. the template that avoids off by one and overflow [!warning] can overflow in languages with fixed width integers when and are both large — never adds two large numbers together, so it cannot overflow. in c , overflow wraps silently instead of throwing, making this bug very easy to miss in testing. write this iteratively, not recursively. a recursive version reads slightly cleaner but costs stack space for no benefit — the iterative form is space and is what every production binary search implementation uses. lower bound / upper bound semantics most real binary search problems are not "find this exact value" but "find the boundary of a region" — these use a half open template instead, which is what makes lower/upper bound clean. function returns formula first index first index — [!key] the half open template with and (never ) is the one template to memorise for every "find the boundary" problem — rotated arrays, peaks, and binary search on answer all reduce to it. search in a rotated sorted array at every step, at least one half of or is guaranteed sorted — identify which half is sorted, then check whether the target lies inside that half's range. a close relative asks only for the minimum of the rotation, not a specific target — that needs just the half open template compared against the rightmost element, since the minimu`,"dsa/sorting-algorithms":"you will rarely implement a sort from scratch in an interview, but you will be asked to pick the right one, explain stability and worst case, and reason about why and behave differently. this page is the reference table plus the handful of algorithms worth being able to write from memory. the comparison table algorithm best average worst space stable? in place? use case bubble sort ✅ ✅ teaching only insertion sort ✅ ✅ small or nearly sorted input selection sort ❌ ✅ minimising number of swaps merge sort ✅ ❌ stability required, linked lists, external sort quicksort ❌ ✅ general purpose, best average constant factor heapsort ❌ ✅ guaranteed , space counting sort ✅ ❌ small integer range k bucket sort ✅ ❌ uniformly distributed values across a known range radix sort ✅ ❌ fixed width integers/strings, k = digit count [!key] comparison based sorting has a proven lower bound of — you cannot beat it without exploiting structure in the data (like a bounded integer range, which is what counting and radix sort exploit). insertion sort: the small input specialist insertion sort builds the sorted portion one element at a time, shifting larger elements right until it finds the new element's correct slot — exactly how you'd sort a hand of playing cards. its worst case is , the same as bubble and selection sort, but its best case — an already sorted or nearly sorted array — is , because the inner loop never fires. that adaptive behaviour, a tiny constant factor, and good cache locality on small arrays are exactly why introsort and timsort both fall back to insertion sort once a partition or run","dsa/recursion-and-backtracking":`backtracking is recursion with a discipline: try a choice, recurse as if it were correct, then undo it and try the next one. almost every "generate all x" or "find if some arrangement of x is possible" question reduces to this one template, plus a pruning rule specific to the problem. base case, recursive case, and the call stack every recursive function needs a base case (a condition that returns directly, without recursing) and a recursive case (a step that reduces the problem and calls itself). the call stack tracks every pending call's local variables and the point it needs to resume from — this is what makes recursion "remember" the state of the outer call while the inner one runs. [!warning] every recursive call adds a stack frame. a recursion depth of costs stack space — a naive recursive traversal of a 10⁶ element linked list will stack overflow in most languages, c included. if depth can be large and unbounded by tree balance, convert to iteration with an explicit stack. recursion to iteration any recursive algorithm can be rewritten iteratively using an explicit stack that mimics what the call stack was doing — this is worth demonstrating if asked to avoid recursion (e.g. for stack depth safety). the choose explore unchoose template backtracking generalises recursion for exploring a search tree of decisions: at each node, try every valid choice, recurse into it, then undo the choice before trying the next one — leaving shared state exactly as you found it. [!key] the single most common bug is forgetting the "unchoose" step, or adding the reference to instead of a `,"dsa/greedy-algorithms":`a greedy algorithm builds a solution one locally best choice at a time, never reconsidering earlier decisions. that is either the fastest possible solution to a problem, or a subtly wrong one — the entire skill is knowing which, and proving it before you commit to the approach out loud. what makes a greedy choice safe two properties must both hold for a greedy strategy to be provably correct, not just "seems to work on the examples". property meaning greedy choice property a locally optimal choice at each step leads to a globally optimal solution — you never need to revisit it optimal substructure an optimal solution to the whole problem contains optimal solutions to its subproblems optimal substructure alone is not enough — dynamic programming problems have it too, but still require exploring multiple choices per state because the locally best choice isn't always globally safe. greedy additionally requires that committing early never closes off the true optimum . [!key] "it passed the examples i tried" is not a proof. the interview safe way to justify greedy is the exchange argument : assume an optimal solution does not make the greedy choice, then show you can swap it in without making the solution worse — therefore some optimal solution always includes the greedy choice. the exchange argument, concretely take activity/interval scheduling: pick the maximum number of non overlapping intervals from a set. the greedy rule is "always pick the interval that finishes earliest among those still compatible." proof sketch: suppose an optimal solution's first pick is some interval `,"dsa/intervals-and-sweep-line":`interval problems all reduce to one decision: sort by start or sort by end, then sweep once. interviewers use this family to check whether you can pick the right sort key for the question actually being asked, and whether you handle touching but not overlapping endpoints correctly. sort by start vs sort by end sort key used for why start time merge intervals, insert interval you need to know, in order, when each interval begins to decide if it extends the current merge end time max non overlapping intervals, min arrows to burst balloons, activity selection greedily picking the interval that frees up time soonest leaves the most room for what follows [!key] sorting by end time for "maximum non overlapping subset" is the one greedy choice in this whole topic that needs a proof: picking the interval that ends soonest always leaves at least as much room for future picks as any other choice — the classic exchange argument for interval scheduling. merge intervals sort by start. walk the list, keeping a "current" merged interval; if the next interval's start is current end, extend the merge; otherwise close it out and start a new one. insert interval a specialised merge where the array is already sorted, so a single linear pass suffices instead of a full sort: append intervals strictly before the new one, merge everything overlapping it, then append the rest. non overlapping intervals: sort by end to find the minimum number of intervals to remove so the rest don't overlap, sort by end time and greedily keep any interval whose start is the last kept interval's end. meeting rooms i `,"dsa/matrix-and-simulation":`matrix problems are rarely about a clever algorithm — they are about careful index bookkeeping: which direction you're walking, which boundary you've hit, and whether you can avoid an extra buffer. interviewers use them to see whether your code is correct on the first try, not just eventually. row/column traversal and the direction vector technique most grid problems (flood fill, number of islands, rotting oranges, word search) move in four directions. encoding directions as an array of pairs avoids four copy pasted if blocks and is the single most reusable idiom in this topic. [!key] always bounds check before indexing, not after: must short circuit before touching , or you risk an index out of range exception on the boundary. spiral order track four shrinking boundaries — , , , — and walk each side in turn, shrinking the relevant boundary after each side. the two "if" guards before the final two sides handle single row/single column remainders correctly. [!warning] skipping the two guard checks ( , ) causes the last row or column to be visited twice when the matrix isn't square — a classic bug that only shows up on non square test cases. rotate image in place rotating a square matrix 90° clockwise decomposes into two simpler, well known operations: transpose , then reverse each row . rotation operations 90° clockwise transpose, then reverse each row 90° counter clockwise transpose, then reverse each column (or reverse rows, then transpose) 180° reverse each row, then reverse each column (or reverse rows top bottom and reverse each row) set matrix zeroes with o(1) space th`,"dsa/bit-manipulation":"bit manipulation questions are less about cleverness and more about recognising a small set of idioms — check a bit, isolate the lowest set bit, use xor to cancel duplicates. once memorised, they turn into some of the fastest, cleanest solutions you can write in an interview. the five operators operator symbol effect and 1 only where both bits are 1 — used to test or clear bits or 1 where either bit is 1 — used to set bits xor 1 where bits differ — used to toggle bits and cancel duplicates not flips every bit shift , moves bits left/right — equivalent to multiply/divide by powers of 2 [!key] every bit trick below is built from just three ideas: produces a mask with only bit set, reads or clears against that mask, and / write against it. common idioms goal expression notes check bit returns or set bit idempotent — safe to call twice clear bit has every bit set except toggle bit flips just that bit lowest set bit (isolated) relies on two's complement; see below clear lowest set bit used to count set bits in iterations check power of two a power of two has exactly one set bit count set bits built in in .net; avoid hand rolled loops when available [!tip] isolates the lowest set bit because in two's complement is : every bit below the lowest set bit becomes , the lowest set bit itself stays (since it was in and the carries up to it), and everything above flips. anding with the original keeps only that one bit. xor properties and single number problems xor is its own inverse: and . it is also commutative and associative, so the order of xor ing a list doesn't matter — every value","dsa/dynamic-programming-foundations":`dynamic programming is not a separate universe of algorithms — it is brute force recursion with a cache, applied to problems that recompute the same subproblem many times. interviewers use dp to test whether you can decompose a problem into states and transitions methodically, rather than pattern matching a memorised solution. overlapping subproblems and optimal substructure dp applies when a problem has both properties below. miss either one and dp either doesn't help or gives a wrong answer. property meaning test overlapping subproblems the same smaller input recurs many times during naive recursion draw the recursion tree — do nodes repeat? optimal substructure the optimal answer to the whole problem is built from optimal answers to subproblems can you write ? and are each recomputed from scratch multiple times — that duplication is overlapping subproblems . caching each distinct collapses calls to . [!key] if a problem has optimal substructure but no overlapping subproblems (e.g. binary search, quicksort's partitioning), memoisation buys you nothing — that's plain divide and conquer, not dp. how to spot a dp problem certain phrasings are strong signals: "count the number of ways", "find the minimum/maximum cost to reach", "can you partition/reach a target", "longest/shortest subsequence satisfying a property". a brute force recursive or backtracking solution that clearly re explores the same pair is the tell. [!tip] say out loud: "i'll first write the brute force recursion, identify what repeats, then memoise it." this narrates a defensible path to the optimal solution `,"dsa/dp-patterns":"most dp interview questions are a variation on a small number of well known shapes. recognising which shape you're looking at — not solving from scratch — is the actual skill being tested. this page is a lookup table: given a problem description, find the matching pattern, state and recurrence. how to use this catalogue read the problem for these signals: does an item get used once or unlimited times? is order fixed (subsequence) or free (subset)? is the answer over a contiguous range (interval) or a full assignment (bitmask)? those questions narrow the field to one or two rows below. [!key] the recurrence almost always encodes a choice : take vs. skip, extend vs. start new, split at position . naming the choice out loud is 80% of deriving the recurrence correctly. knapsack family pattern state recurrence notes 0/1 knapsack = best value using first items, capacity each item used at most once; space optimise by iterating backward unbounded knapsack = best value with capacity item reusable; iterate forward subset sum = can first items make sum boolean version of 0/1 knapsack partition equal subset sum subset sum with same as subset sum reject immediately if is odd coin change (min coins) = min coins to make amount unbounded reuse of each coin coin change (count ways) = number of ways to make loop coins outer , amount inner, to avoid counting permutations as distinct longest increasing subsequence (lis) version state recurrence time o(n²) = length of lis ending at for where o(n²) o(n log n) = smallest tail of an increasing subsequence of length binary search for the first elem","dsa/coding-interview-playbook":`the coding round tests more than whether your solution compiles — it tests whether you can be trusted to work through an ambiguous problem methodically, under time pressure, while explaining your thinking. this page is a rehearsable script for that 45 minutes, not another algorithm to learn. the shape of a 45 minute round phase time budget goal clarify 3–5 min pin down input shape, constraints, edge cases, ambiguous wording brute force 3–5 min state a correct, even if slow, approach and its complexity optimise 5–8 min narrate the bottleneck and the trick that removes it code 18–22 min write clean, working code, narrating non obvious lines test 5–7 min dry run on a small example, then check edge cases wrap up 2–3 min state final complexity, ask your own question if time remains [!key] the single biggest signal in the round is narration — an interviewer who has to guess what you're thinking cannot score you on it. say the plan before you type it. clarifying questions to always ask what are the constraints on input size ( )? this tells you the target complexity before you start. can the input be empty, contain duplicates, negative numbers, or be unsorted? is there a canonical tie breaking rule if multiple valid answers exist? can i modify the input in place, or must it be preserved? what's the expected return value on "not found" — , , throw, empty list? [!tip] ask 2–3 targeted questions, not ten generic ones. "can nums contain negatives, and should i treat an empty array as an error or return 0?" signals you're thinking about edge cases already — a stronger signal than a chec`,"dsa/pattern-recognition":`the hardest part of a coding interview is rarely writing the code — it's the first ninety seconds, deciding which of a dozen plausible techniques actually fits. this page is a recognition index, not a tutorial: for each family it gives the trigger phrase or constraint that should make the pattern come to mind, plus a one line reminder of the core idea. the mechanics — invariants, proofs, full walkthroughs — live on the dedicated pattern pages; this page exists so you can go from "what is this problem really asking" to "which page do i need" in seconds. the trigger table when you notice... reach for one line idea "contiguous subarray/substring sum" on a static array prefix sum precompute cumulative sums so any range query is many range updates , one final read of the whole array difference array mark only the boundaries; reconstruct with one prefix sum pass point updates interleaved with range queries fenwick tree / segment tree trade array simplicity for update and query array is sorted, or two values must trade off from opposite ends two pointers converge inward, or run same direction slow/fast pointers "longest/shortest substring or subarray satisfying a condition" sliding window expand right, shrink left while the window stays valid "next greater/smaller element", "span", histogram shaped question monotonic stack keep indices in increasing/decreasing order; each pushed and popped once sorted data, or a yes/no predicate that flips exactly once binary search search on index directly, or binary search on the answer "kth largest/smallest", "top k", "median of a stream" heap `,"dsa/advanced-patterns":"the core patterns pages cover the structures you reach for in almost every interview. this page is the next tier: the data structures that show up when a problem's constraints quietly rule out anything simpler — range updates that must stay , a set so large that exact membership is too expensive to store, or a storage engine question hiding inside what looked like a plain coding round. none of these are exotic; each one is the standard, well known answer to a specific constraint, and recognizing that constraint is most of the battle. [!key] pick the structure by the exact pair of operations you need, not by familiarity. a prefix sum array wins on simplicity if the data never changes; a fenwick tree wins the instant point updates and range sum queries interleave; a segment tree wins the moment you need a non sum aggregate (min, max, gcd) or range updates via lazy propagation. segment trees a segment tree is a binary tree laid over array intervals, where each node stores an aggregate (sum, min, max, gcd — anything associative) of the range it covers. both query and update walk a single root to leaf path plus a bounded number of siblings, giving for each, at the cost of space for the array backed implementation below. lazy propagation for range updates a range update (add a delta to every element in ) applied node by node costs . lazy propagation gets it back to : every node holds , a pending delta not yet pushed to its children. before a query or update descends into a node's children, push the pending delta down first , then clear it on the parent. only fully covered ranges ","dsa/csharp-for-coding-interviews":"knowing the algorithm is only half the coding round — the other half is not losing five minutes to versus , or to a constructor that quietly reverses your input. this page is a fast lookup for the c syntax, collection apis and linq methods that come up constantly while solving dsa problems, organized so you can scan straight to the type you need instead of re deriving it mid interview. [!key] arrays, and use ; every other collection uses . this single rule causes more compile time friction under pressure than anything else on this page. setup, primitives and casting type width range notes 32 bit ( 2.1e9) default integer type 64 bit ( 9.2e18) use once a sum can overflow 64 bit ieee 754 15 17 significant digits default floating type 128 bit exact for base 10 fractions rarely needed in dsa problems 16 bit utf 16 — arithmetic works directly on it [!warning] overflows silently before the result is ever assigned to a . cast one operand to before the multiplication ( ), not after — casting the finished product is too late. control flow, tuples and methods classes, structs and records arrays and strings collections remember by shape add read / remove list / set — general bag / stack — top / queue — line / linkedlist — two ends / dictionary — key → value / priorityqueue — priority / [!tip] every method ( , , , , ) exists specifically so you never have to wrap a collection operation in a to test for empty or missing — check the return value instead. note that enumerates and pushes each element, which means the last element of ends up on top — iterating the resulting stack does not re","dsa/must-solve-problem-list":"solving problems at random produces the illusion of progress without the pattern recognition interviews actually test. this list groups roughly 120 problems by the technique they teach, in the order most candidates should learn them, so each new problem reinforces a pattern instead of feeling like a fresh puzzle. how to use this list work pattern by pattern, not top to bottom by difficulty. solve 2–3 problems per pattern, notice what's shared, then move on — depth on every pattern beats exhaustive drilling of one. [!key] the goal of any problem list is pattern recognition under time pressure , not memorising 120 individual solutions. if you can't name the pattern within 60 seconds of reading a new problem, that's the gap to close — not more volume. arrays, strings and hashing pattern problem difficulty key idea hash complement two sum easy store seen values, check kadane's algorithm maximum subarray medium reset running sum when it goes negative prefix/suffix product product of array except self medium two passes, no division one pass greedy best time to buy and sell stock easy track minimum price seen so far in place swap rotate array medium reverse whole, then reverse in two parts two pointers on sorted 3sum medium sort, fix one, two pointer the rest frequency map group anagrams medium sorted string or char count as map key frequency map valid anagram easy character count comparison prefix sum + hash map subarray sum equals k medium store running sum counts, check hash set of starts longest consecutive sequence medium only start counting from a number with no left neighbo","dsa/standard-problems":"this page is the problem library companion to the pattern pages. instead of teaching a technique in isolation, it shows the canonical interview problem where that technique earns its keep, with complete c implementations you can compare against your own. the fastest way to use it is to read a prompt, name the pattern family first, then jump to the matching section and check whether your invariant, state, and complexity story line up with the worked version here. [!key] in interviews, say the wasted work before you say the trick. once you can name the bottleneck as repeated scanning, repeated branching, or repeated re sorting, the right pattern usually becomes obvious. [!tip] use this page after you can already state a brute force solution. interviewers usually score the transition from correct but slow to optimal almost as highly as the final code. arrays, strings and hashing problem pattern technique complexity maximum subarray arrays kadane running sum best time to buy and sell stock arrays reverse scan with suffix max majority element arrays boyer moore vote product of array except self arrays prefix and suffix products maximum product subarray arrays prefix and suffix products with zero reset find all duplicates in an array arrays index marking by negation find the duplicate number arrays floyd cycle detection sort colors arrays dutch national flag partition rotate array arrays triple reverse next permutation arrays pivot swap and suffix reverse spiral matrix arrays boundary simulation set matrix zeroes arrays first row and first column markers rotate image arrays trans","dsa/advanced-problems":"this page is the harder tier of the problem library. these are the questions where a correct brute force solution is usually easy to say out loud, but the interview signal comes from finding the one state definition, ordering trick, or data structure invariant that makes the optimal version click. use it as a walkthrough reference: read the prompt, name the family, then check whether your recurrence, sweep, heap, or stack invariant matches the worked solution here. problem pattern technique time space car pooling arrays difference array for bounded coordinates burst balloons dynamic programming interval dp with last burst framing regular expression matching dynamic programming 2d dp over string and pattern prefixes partition to k equal sum subsets dynamic programming bitmask dp over used elements cheapest flights within k stops graphs bellman ford over edge relaxations critical connections in a network graphs tarjan low link bridge detection reconstruct itinerary graphs hierholzer eulerian path traversal partition labels greedy last occurrence greedy partitioning letter combinations of a phone number backtracking choose one letter per digit palindrome partitioning backtracking backtracking with palindrome dp precompute meeting rooms iii heap available room heap plus busy room heap lfu cache design linked list hash maps plus frequency buckets of doubly linked lists average per op median of two sorted arrays binary search partition binary search on the smaller array split array largest sum binary search binary search on the answer count of smaller numbers after self binary se","dsa/contest-practice-one":`a timed contest is a different skill from an untimed interview: four problems, a ticking clock, and partial credit for whatever you finish. this page is a set of write ups from one such round, kept in the format that's actually useful to revisit later — what the problem asked, what approach got picked and why, the working solution, and the one thing worth remembering next time a similar shape shows up. [!tip] in a scored contest, a correct solution submitted in five minutes almost always beats a correct solution submitted in twenty. optimize only after the naive version is banked, unless the constraints make outright impossible. problem difficulty technique complexity count rotations with exactly k equal adjacent pairs easy circular equal pair count, adjust for the one boundary that changes count good cyclic rotations medium fixed size sliding window over the doubled array count robot groups medium interval merge, then a monotonic stack over speed minimum cost path with at most k turns hard dijkstra over an expanded state on the expanded graph count rotations with exactly k equal adjacent pairs problem. given a string of length and an integer , a cyclic rotation moves some prefix of to the end. each rotation has a score: the number of adjacent index pairs where the characters are equal. count how many of the cyclic rotations have a score of exactly . approach. rotating the string never changes which characters are adjacent to each other around the circle — it only changes which single pair sits at the "seam" (the join between the old end and the old start). so instead of sc`,"dsa/contest-practice-two":`not every contest problem gets solved cleanly inside the time limit — sometimes the honest write up is "here's the working but too slow solution i submitted, and here's what i should have done." this page keeps that honesty: four problems, the approach taken during the round, the solution as submitted, and a lesson for each, including one where the lesson is "the constraints needed a smarter data structure than the one i reached for." [!key] "all occurrences equally spaced" for an unbounded count reduces to a fixed size sliding window of the last three occurrence indices — three consecutive terms of a sequence are equally spaced if and only if every consecutive triple is, so you never need to remember more than three positions per value. problem difficulty technique complexity count values with equally spaced occurrences i easy track the first three occurrence indices per value count values with equally spaced occurrences ii medium slide a 3 index window per value; poison it on the first violation minimum days to score exactly n points medium precompute streak totals, combine two of them with a skip day join count subarrays with distant sums hard submitted brute force; fenwick tree over compressed prefix sums is the optimal fix submitted, optimal count values with equally spaced occurrences i problem. an integer in is special if it occurs exactly three times, at indices , with . count how many distinct values in are special. approach. one pass with a dictionary keyed by value, storing how many times it's been seen and its first, second and third occurrence index. a value wi`,"frontend/scope-closures-and-this":'scope, closures and are the three ideas that separate "can write javascript" from "understands javascript". a backend engineer who has internalised static scoping in c or java will find lexical scope familiar, but and closures have sharp edges that show up constantly in interviews and in real bugs. lexical scope and the scope chain javascript uses lexical (static) scoping — where a variable resolves is determined by where it is written in the source, not by who calls the function. every function captures a reference to the scope it was defined in, and lookups walk outward through that chain until they hit the global scope. each function, block ( for / ), and module creates a new lexical environment. lookup is o(depth of nesting), which is why deeply nested closures can be a little slower — never a real interview concern, but worth knowing. [!key] scope is decided at write time , not call time. a function always sees the variables of the scope where it was defined , even if it is invoked from somewhere else entirely. var vs let vs const, and the temporal dead zone is function scoped and hoisted with an initial value of . and are block scoped and hoisted too, but they sit in the temporal dead zone (tdz) — a region where the binding exists but touching it throws a . feature scope function block block hoisted yes, initialised to yes, but in tdz yes, but in tdz re declarable yes no no re assignable yes yes no (binding is frozen, not the value) attaches to /global object yes (in scripts) no no freezes the binding , not the object it points to — is legal because you never reassign',"frontend/prototypes-and-inheritance":`javascript's object model is not classical inheritance underneath — it is prototypal , where objects delegate to other objects. es6 syntax hides this well enough that many engineers use classes daily without knowing what they compile down to, which is exactly the gap senior interviewers probe. the prototype chain and property lookup every object has an internal link to another object (its prototype). when you read a property, the engine checks the object itself first, then walks up the chain of prototypes until it finds the property or hits . [!key] inheritance in javascript is delegation at read time , not copying at creation time. if you mutate later, every object delegating to it sees the change immediately. vs these two are constantly confused and are a favourite "explain the difference" question. (aka ) exists on functions (used as constructors) every object purpose the object that becomes the prototype of instances created with the actual link used during property lookup access or should you use it directly? yes, to add shared methods prefer / ; is a legacy accessor sets the new object's internal to — that single assignment is the entire link between a constructor and its instances. constructor functions and before classes, shared behaviour was built with a plain function and its object: builds the same relationship without a constructor at all — useful when you want an object that delegates to another object directly, with no and no function involved. es6 classes: syntactic sugar, precisely does not introduce a new inheritance model — it compiles to the same construc`,"frontend/the-event-loop":`the event loop is the mechanism that lets a single threaded language handle timers, network requests, and user input without blocking. it is also the single most reliable way an interviewer separates candidates who have memorised "javascript is async" from those who can actually predict output order. the single threaded model javascript itself executes on one call stack — one thing at a time, run to completion. anything that looks concurrent (timers, network calls, file reads) is handled by the host environment (the browser's web apis, or node's libuv), which runs that work outside the js thread and hands a callback back to javascript only when the stack is empty. [!key] the call stack, the web apis, and the queues are three separate things. javascript code only ever runs on the call stack — queues just decide what runs next once the stack is empty. queues: microtasks vs macrotasks (tasks) macrotask (task) queue microtask queue examples , , i/o callbacks, ui events , , when drained one task per loop tick entire queue , fully drained, before the next task priority vs the other lower higher — always runs first can starve the other? no yes — recursively queuing microtasks blocks tasks and rendering indefinitely the rule that decides almost every "predict the output" question: after each single macrotask finishes, drain the entire microtask queue before picking the next macrotask — even if that means running hundreds of callbacks in a row. worked example walkthrough: synchronous code runs first end to end ( , then — the and calls only register callbacks, they don't run yet). th`,"frontend/promises-and-async-await":`promises are the standard abstraction for a value that will exist later, and / is how most modern code consumes them. interviewers use this topic to check two things at once: do you understand the underlying contract, and do you write async code that is actually correct under failure and concurrency, not just in the happy path. promise states and the then/catch/finally contract a promise is always in exactly one of three states, and a transition out of pending is permanent — a settled promise never changes state again. method runs when receives returns fulfilled (first arg) or rejected (second arg) the value or the reason a new promise rejected — sugar for the reason a new promise either — no argument passed nothing a new promise, forwarding the original result unless it throws [!key] every / / call returns a new promise , never the same one. this is what makes chaining possible, and it's why forgetting to inside a silently breaks the chain. chaining and flattening returning a promise from inside a flattens automatically — you never end up with a promise of a promise. returning a plain value wraps it in an already fulfilled promise for the next link. this flattening is exactly what makes chains readable instead of nesting ever deeper ("callback hell" with promises instead of callbacks — the classic mistake of forgetting to flatten). error propagation and the unhandled rejection trap a rejection skips every until it finds a (or a second argument) — exactly like a skipping frames until a block. [!danger] a rejected promise with no anywhere in its chain triggers an event (brow`,"frontend/events-debounce-and-throttle":`dom events and their rate limiting cousins, debounce and throttle, are the bread and butter performance topic of front end interviews. a backend engineer already understands rate limiting conceptually — this section maps that intuition onto the browser's event model and the two implementations you are expected to be able to write from memory. event propagation: capture, target, bubble a dom event does not just fire at the element it happened on — it travels through the tree in three phases. phase direction triggered by default? capture root down to the target only if listener registered with target at the element the event actually occurred on always bubble target back up to the root yes, for most events (a few, like / , don't bubble) [!key] almost all real code listens on the bubble phase (the default). capture is mainly for intercepting an event before a child gets a chance to handle or stop it — a common use is a top level "close all open menus" handler. stoppropagation vs preventdefault vs stopimmediatepropagation these three are frequently confused, and interviewers like to ask for all three in one breath. method stops the event from stops the default browser action? stops sibling listeners on the same element? nothing — propagation continues yes (e.g., form submit, link navigation, checkbox toggle) no continuing to bubble/capture past the current element no no continuing to propagate, and any other listeners on the same element no yes [!warning] does not stop the default action, and does not stop propagation — they are independent axes. confusing them is the classic m`,"frontend/typescript-type-system":`typescript's type system is structural, not nominal, which is the single biggest mental adjustment for engineers coming from c or java. interviewers use this topic to check whether you can reason about types as compile time contracts that get erased at runtime, and whether you write types that actually catch bugs rather than just satisfying the compiler. structural typing vs nominal typing typescript compares types by shape — if two types have compatible members, they are compatible, regardless of name or declared hierarchy. c /java use nominal typing — compatibility requires an explicit declared relationship (implements/extends). [!key] "does it have the right shape?" replaces "does it declare the right interface?" — this is why typescript can type plain object literals and json responses without wrapping them in classes. type vs interface both describe object shapes, and for plain objects they are close to interchangeable — but the differences matter in specific situations interviewers probe. object shapes yes yes unions / intersections no yes — declaration merging (reopen and add members) yes — same name merges automatically no — duplicate name is an error extending (multiple) intersection primitives, tuples, mapped/conditional types no yes typical convention public api shapes, class contracts unions, utility/derived types, function types [!tip] "i default to for object shapes that might need to be extended or merged, and for everything else — unions, tuples, mapped types" is a defensible, common team convention to state out loud. union and intersection types a union res`,"frontend/typescript-generics-and-utility-types":`generics let you write a function or class once and have typescript compute the specific types for every call site, instead of choosing between duplicated code or falling back to . this is where typescript interviews shift from "do you know the syntax" to "can you design a type safe api" — the utility types are the vocabulary, generics are the grammar. generic functions, classes and constraints a generic parameter ( ) is a placeholder filled in per call site, inferred from the arguments unless you specify it explicitly. constrains what is allowed to be, so you can safely use members that only exist on a subset of all types: [!key] a constraint ( ) narrows what a generic can be, without pinning it to one concrete type — you get both safety (you can use 's members) and flexibility (any subtype still works). default type parameters a default kicks in only when the caller doesn't supply or can't infer the type argument — useful so a generic type is still usable "as is" without forcing every consumer to specify it. conditional types and infer a conditional type ( ) branches at the type level, based on whether one type is assignable to another. captures a type from inside that check to reuse on the right hand side. is how the compiler "reaches into" a generic shape — a function signature, an array, a promise — and pulls out one of its type parameters without you having to know it up front. mapped types a mapped type builds a new object type by iterating over the keys of an existing one, optionally transforming each key or value. combined with key remapping ( ), you can rename key`,"frontend/react-components-and-state":`react's mental model rewards precision about a small number of ideas: what a component actually is, why state must be updated immutably, and why keys exist at all. interviewers use component and state questions to see whether you understand the model or have just memorised patterns that happen to work. function components and jsx a function component is a plain javascript function that returns a description of ui — jsx compiles to calls, which produce plain objects, not real dom nodes. react calls this function on every re render, diffs the returned tree against the previous one (reconciliation), and applies the minimal set of real dom mutations needed. [!key] a component re rendering does not mean the dom changes — it means react re runs the function and compares the result. only actual differences touch the real dom. props vs state props state owner passed in by the parent owned by the component itself mutability read only from the child's perspective mutable, via a setter ( / ) who can change it only the parent, by re rendering with new props the component itself triggers re render on change? yes, of the child yes, of the component that owns it (and its subtree) typical use configuration, data flowing down anything that changes over time and affects rendering immutability and why mutating state doesn't re render react decides whether to re render by comparing the reference of the new state to the old one ( ), not by deep inspecting its contents. mutating an object or array in place keeps the same reference, so react sees "no change" and skips the re render entirely. [!da`,"frontend/react-hooks":`hooks let function components hold state and side effects without classes, but they work through a fragile mechanism — call order — that trips up almost every candidate at some point. knowing why the rules exist, not just what they are, is what separates "used hooks" from "understands hooks". why the rules of hooks exist react does not know hook names — it stores state in a linked list per component instance , indexed purely by call order . on every render, react walks that list and hands out the next slot to the next hook call. if a hook is called conditionally, the slot index shifts and react hands the wrong stored value. [!key] hooks are not magic — they are entries in an ordered array keyed by call position. never call a hook inside a condition, loop, or after an early return. call them in the same order on every render. the two rules of hooks, and why each exists: rule reason only call hooks at the top level preserves call order across renders so react's slot indexing stays valid only call hooks from react functions (components or custom hooks) ties hook state to a fiber; plain functions have no fiber to attach state to usestate: functional updates and batching returns a snapshot value that is fixed for the lifetime of that render — it is not live like an instance variable. reading inside a closure after calling still gives the old value until the next render. react batches state updates that happen inside event handlers and (since react 18) inside promises, timeouts, and native event listeners too — all calls in one tick are merged into a single re render. this is why`,"frontend/rendering-and-reconciliation":`"re render" is one of the most misused words in front end interviews. candidates conflate it with "the dom updated" or "the screen changed", and interviewers use that confusion to test whether you actually understand react's render → reconcile → commit pipeline. what triggers a render a component re renders when react decides its output might have changed. there are exactly four triggers: trigger example state changes props change (from a parent that itself re rendered) parent passes a new prop context value changes a 's value prop gets a new reference parent re renders by default, a child re renders whenever its parent does, even with unchanged props [!key] a re render is react calling your component function again to get a new tree description. it says nothing about whether the dom actually changes — that decision happens later, in reconciliation. render phase vs commit phase react splits work into two phases: render phase is pure computation — calling component functions, building the new virtual tree, diffing. it can be paused, thrown away, or restarted by react (this is what makes concurrent features possible) and must have no side effects. commit phase is where react actually mutates the real dom, attaches refs, and runs synchronously, followed by paint, followed by asynchronously. this phase is not interruptible . [!warning] because the render phase can be thrown away and re run, it must be pure — no mutating variables outside the function, no direct dom writes, no side effects. that is the deeper reason side effects belong in , not in the component body. the virtual`,"frontend/react-performance":`most react performance problems are self inflicted by premature optimisation or a misunderstanding of referential equality — not by react being slow. the senior move is to measure before touching a single , and to know the handful of patterns that actually cause wasted work. diagnose first: the profiler, not guessing react devtools' profiler tab records a render and shows which components rendered, how long each took, and — critically — why each one rendered (props changed, state changed, parent re rendered, hooks changed). guessing which component is "slow" and wrapping it in blind is a common wasted effort; the profiler tells you definitively. [!key] "it feels slow" is not a diagnosis. record it, identify whether the problem is too many renders or one render doing too much work , and fix that specific thing. chrome's performance tab complements this for actual paint/layout cost. referential equality: the root cause of most wasted renders , , , and dependency arrays all compare values with (or a shallow prop comparison for ) — reference equality for objects/arrays/functions. a new literal created every render is "different" even if its contents are identical, defeating any memoisation downstream. [!warning] alone does nothing if the props you pass are new references every render. memoising the child without stabilising what the parent passes down is the single most common wasted effort pattern in interviews and in real codebases. memo, usememo, usecallback — used correctly tool wraps prevents a component re render when props are shallowly equal to last time a computed valu`,"frontend/state-management":`"where should this state live" is one of the highest signal front end architecture questions, because the wrong answer causes cascading complexity — over centralised state, stale caches, or unnecessary re renders. most of the answer comes from correctly classifying what kind of state you're dealing with before picking a tool. a taxonomy of state kind example lifetime server cache a list of orders fetched from an api owned by the server; the client holds a stale copy that needs invalidation global client state current theme, logged in user, feature flags client only, shared across many unrelated components local ui state is this dropdown open, which tab is active owned by one component/subtree, disposable url state current page number, filters, selected item id should be shareable/bookmarkable/back button able form state values, validation errors, dirty/touched flags transient, usually local, sometimes needs persistence on navigation [!key] most state in a typical app is server cache, not global client state — and the two need fundamentally different tools. server cache needs fetching, caching, invalidation and background refresh; a redux store gives you none of that for free. comparing the tools tool best for weakness / local component state, simple to moderately complex doesn't scale across distant components without lifting/prop drilling context low frequency global values (theme, auth, locale) re renders every consumer on any value change; no built in caching or selectors redux (with redux toolkit) large apps with complex, cross cutting client state and a need for strict`,"frontend/csr-ssr-and-code-splitting":`where a page's html gets generated — client, server at request time, server at build time, or some hybrid — is one of the most consequential architecture decisions a front end team makes, and interviewers use it to check whether you can reason about trade offs instead of naming buzzwords. csr vs ssr vs ssg vs isr vs streaming ssr strategy when html is built ttfb fcp seo server cost data freshness complexity csr (client side rendering) in the browser, after js downloads and runs fast (static shell) slow (waits for js + data fetch) poor without extra work low (static hosting/cdn) always fresh (fetched on load) low ssr (server side rendering) on the server, per request slower (server does work first) fast (html has content) good high (compute per request) always fresh medium ssg (static site generation) at build time, once fastest (pure static file) fastest good lowest (served from cdn) stale until next build low isr (incremental static regeneration) at build time, then regenerated on a schedule/on demand fast (serves stale, revalidates in background) fast good low medium eventually fresh (configurable) medium streaming ssr on the server, sent in chunks as they're ready fast (first chunk arrives quickly) fast (progressive) good medium high always fresh high [!key] there is no universally "best" strategy — the right choice depends on how often the data changes, how much seo matters, and how much server cost/complexity the team can afford. state the trade off, not a single winner. hydration and the hydration cost/mismatch problem hydration is the process of react attaching event`,"frontend/frontend-security":`front end security questions test whether you understand why a defence works, not just its name — interviewers routinely ask "what does cors actually protect against" and most candidates get it backwards. this page covers the defences that come up most in senior interviews, with the mechanism behind each one. xss: stored, reflected, dom based cross site scripting means an attacker gets their javascript to execute in another user's browser , in the context of your site — able to read cookies (unless ), make authenticated requests, or modify the page. type how it happens example stored malicious input is saved server side and served to other users later a comment field stores , rendered unescaped for every viewer reflected malicious input is echoed back in the immediate response, usually via a url parameter a search page renders directly into the results html dom based the vulnerability is entirely client side — js takes untrusted input and writes it into the dom unsafely executed with attacker controlled url fragment [!danger] is named that way on purpose. if you must render user supplied html (a rich text field), sanitise it server side and client side with a library like dompurify — never trust that "we validate on the way in" is enough on its own. defences, layered: defence what it does contextual output encoding escape data differently depending on where it's inserted — html body, html attribute, url, js string — a single "escape everything the same way" approach misses attribute/js context injections avoid / frameworks escape text content by default; bypassing that remo`,"frontend/frontend-testing":`front end testing interviews rarely ask "do you write tests" — they ask what kind , at what level , and why , because a suite full of brittle implementation detail tests is worse than no tests at all. the testing trophy reframes where the effort should actually go. the testing trophy for front end the classic "testing pyramid" (many unit tests, few e2e) doesn't map cleanly onto front end apps, where most bugs live in how components integrate — a form talking to validation logic talking to state talking to an api call — not in isolated pure functions. kent c. dodds' testing trophy reflects this: layer what it checks speed confidence per test static types, lint rules, obvious mistakes instant low, but catches whole bug classes for free unit a single pure function or hook in isolation very fast moderate — narrow scope integration a component tree rendering, interacting, and talking to a mocked network, as a user would experience it fast high — closest to how the app is actually used e2e a real browser driving the real (or near real) app end to end slow highest, but expensive and slower to diagnose failures [!key] integration tests give the best return on effort: they exercise real component composition and real user interaction without the cost, flakiness, and slow feedback loop of a full browser e2e run. spend the bulk of your testing budget here. testing behaviour, not implementation react testing library (rtl) is built around one principle: tests should resemble how users interact with your app , not reach into component internals (state, instance methods, prop values). its`,"fundamentals/clean-code-and-principles":`"clean code" questions test judgement, not trivia. interviewers want to see that you can name things well, keep functions small and honest, and know when a principle like dry should bend rather than be obeyed blindly. naming that removes the need for comments a good name answers "what" and "why" so a comment doesn't have to. if you feel the urge to write , rename the boolean to and delete the comment — the code now says the same thing and cannot go stale. rules that hold up in review: use intention revealing names ( , not ), avoid disinformation (don't call a a ), and make names searchable — is unfindable, is not. booleans should read as a yes/no question ( , ); avoid negatives ( ). [!key] a comment that explains what the code does is a sign the code should be renamed instead. a comment that explains why (a business rule, a workaround for a bug) earns its place. function size and a single level of abstraction small functions are easier to name, test and reuse — if you struggle to name a function, it is probably doing more than one thing. the related discipline is single level of abstraction : a function should either orchestrate calls to other functions, or do low level work, not mix both in one body. a function reading top to bottom at one abstraction level is scannable in seconds; a function mixing "call a service" with "manually build a json string" forces the reader to context switch line by line. guard clauses over nested conditionals nested blocks force the reader to hold multiple conditions in their head before reaching the actual logic. guard clauses (early returns)`,"fundamentals/code-review":"code review is one of the few engineering practices interviewers can probe without a whiteboard — they just ask you to describe how you review, or hand you a diff and watch what you flag. it reveals judgement about risk, communication style, and whether you understand what review is actually for . what a review is for, and what it isn't review exists for four things: catching correctness issues before production, sanity checking design decisions while they're still cheap to change, protecting long term maintainability , and spreading knowledge so more than one person understands each part of the system. it is explicitly not for enforcing formatting, brace placement, or import ordering — that is what linters and formatters are for. a team that argues about tabs vs spaces in review has a tooling gap, not a review culture problem. review should catch review should not need to catch logic errors, missed edge cases, race conditions indentation, brace style wrong abstraction, leaky interface, missing test import ordering, trailing whitespace security issue (injection, missing auth check) line length unclear naming that will confuse the next reader semicolons, quote style [!key] if a comment could be produced by a linter, it shouldn't be produced by a human. automate style; spend human attention on correctness, design and risk. the reviewer checklist a useful mental checklist, roughly in priority order: category questions to ask correctness does it do what the pr description claims? are edge cases (null, empty, concurrent) handled? tests do tests exist for the new behaviour and th","fundamentals/git-and-branching":"git questions in interviews rarely test command memorisation — they test whether you understand the underlying object model well enough to reason about a strange state, and whether you can justify a branching strategy for a specific team rather than reciting one you've memorised. centralized vs distributed version control git is a distributed version control system (dvcs) : every clone is a complete copy of the repository's history, not just a checkout of the current state, which is what makes branching, committing, and browsing history all work offline. centralized (cvcs) distributed (dvcs) repository copies one central copy; clients check out a working copy every clone has the full history server dependency most operations need the server only push/fetch/pull need the server examples svn, cvs, perforce git, mercurial, bazaar [!tip] this is why never touches the network and works on a plane: the entire history already lives on disk. only , , and need a remote at all. the object model everything in git is one of four object types, and every command you run is really just moving a pointer or creating one of these objects. object contains analogy blob raw file contents (no filename) a file's bytes tree a directory listing — names + modes pointing to blobs/trees a folder commit a tree pointer, parent commit(s), author, message, timestamp a snapshot + metadata ref a named pointer to a commit (branch, tag, ) a label a branch is nothing but a mutable pointer to a commit — creating one is instant because it writes 41 bytes, not a copy of the repository. usually points at a branch ","fundamentals/versioning-and-compatibility":"versioning questions test whether you can distinguish a change that's safe to ship silently from one that requires a major version bump, a deprecation notice, and months of lead time. getting this wrong in production means broken integrations and angry consumers; getting it right in an interview signals real operational experience. semantic versioning semantic versioning (semver) encodes intent in a version number: . segment bumped when consumer expectation major a breaking change may require consumer code changes to upgrade minor a backward compatible addition safe to upgrade, new features available patch a backward compatible bug fix always safe to upgrade the entire value of semver is that a consumer can pin and trust that any release won't break them — which only works if the maintainer is disciplined about what counts as breaking. pre 1.0.0 ( ) is explicitly exempt from these guarantees under the semver spec — anything can break at any point, which is why libraries should reach 1.0.0 quickly once there's a real consumer depending on stability. [!key] semver is a promise to consumers , not a description of effort. a one line change that removes a field from a response is a major bump even if the code diff is tiny — the size of the change to the contract is what matters, not the size of the diff. what actually counts as breaking this is the question that separates people who've memorised the rule from people who've been burned by getting it wrong. change apis libraries events databases add optional field/parameter ✅ safe ✅ safe ✅ safe (consumer ignores unknown) ✅ safe (n","fundamentals/schema-and-api-evolution":"schema and api evolution questions probe whether you can change a live system's data shape without a maintenance window — the expand contract pattern is the single idea that answers most of them, so understanding it deeply is worth more than memorising a dozen individual migration recipes. the expand contract pattern expand contract (also called parallel change) splits a schema change into safe, independently deployable steps so that at every point in time, both the old and new code can run against the database simultaneously. renaming a column is the canonical example — you can never just rename it, because that's an instantaneous breaking change no rolling deploy can survive. walking through a rename of to : 1. expand — add as a new nullable column; old code is untouched and still works. 2. dual write — deploy code that writes to both and on every insert/update, but still reads from . this version can run alongside the old version during rollout. 3. backfill — copy existing rows' into in batches (see below), for rows written before dual write went live. 4. migrate reads — deploy code that reads instead of . verify correctness in production. 5. contract — once nothing reads or writes , stop writing it, then drop the column in a later, separate deploy. [!key] every step in expand contract is individually backward and forward compatible — that's what makes it safe under a rolling deploy where old and new instances run side by side for minutes to hours. adding and dropping columns safely adding a column is safe only if it doesn't force existing rows into an invalid state. a c","fundamentals/zero-downtime-deployments":`zero downtime deployment questions test whether you understand that "no downtime" is an emergent property of several disciplines working together — statelessness, compatibility, health checks, and draining — not a feature you turn on. interviewers usually push on what happens during the window when two versions run at once. preconditions zero downtime is impossible without these already in place; skipping any one of them means the deploy sequence below can't actually protect users. precondition why it's required stateless services any instance can be killed or added without losing session state backward compatible changes old and new code (and schema) must coexist during rollout health checks (readiness + liveness) the load balancer must know when an instance is actually ready for traffic graceful shutdown an instance must finish in flight work before it stops accepting new work and exits [!key] zero downtime deployment is really "zero downtime given backward compatibility and statelessness" — most of the hard work happens in change design (see schema/api evolution), not in the deploy tool itself. connection draining and graceful shutdown when an instance is marked for removal, it must stop receiving new requests immediately (removed from load balancer rotation) while being given time to finish requests already in flight. this is connection draining : the load balancer stops routing new traffic to the instance, and the instance itself, on receiving , stops accepting new connections but lets active ones complete before exiting. [!warning] a (hard kill, no grace period) inste`,"fundamentals/migration-strategies":"migration questions separate people who've read about the strangler fig pattern from people who've actually lived through a multi quarter legacy replacement — the follow up questions target verification, rollback and the political reality that old systems rarely get decommissioned on schedule. the strangler fig pattern named after the strangler fig vine that grows around a host tree until the tree can be removed, this pattern replaces a legacy system incrementally by routing traffic through a facade that sends each request to either the old or new system, one capability at a time, instead of a risky big bang rewrite. the facade is the key piece of infrastructure — an api gateway, a reverse proxy rule, or a feature flagged branch in the calling code — that can redirect traffic per capability without either system knowing about the other. each migrated capability shrinks the legacy system's surface area a little further, until what remains is small enough to retire outright. the pattern's real value is that every step is independently shippable and reversible: if capability a's new implementation misbehaves, the facade routes it back to the old system without touching anything else. [!key] strangler fig succeeds or fails on the facade's routing granularity. route by coarse module and you inherit big bang risk anyway; route by fine grained capability (a single endpoint, a single business rule) and each cutover becomes small enough to verify and roll back independently. branch by abstraction branch by abstraction is strangler fig applied inside a single codebase rather than acr","fundamentals/performance-profiling":`"it's slow" is not a diagnosis. performance work is an investigation with a hypothesis, a measurement, and a fix — in that order. interviewers use this topic to see whether you reach for tools and data or start randomly changing code. measure first, and amdahl's law the single biggest mistake is optimising the part of the system you assume is slow. profile first, form a hypothesis from the data, fix the biggest contributor, then re measure. anything else is guessing with extra steps. amdahl's law formalises why this matters: the speed up from optimising one part of a system is capped by how much time that part actually consumes. where is the fraction of total time spent in the part you're speeding up, and is how much faster you make it. if a component is only 10% of total time, making it infinitely fast saves at most 10% overall — no matter how proud you are of the fix. [!key] optimise the largest bar in the profiler, not the ugliest piece of code. a perfectly tuned function that consumes 2% of runtime is a waste of a sprint. the four resources every performance problem eventually traces back to contention on one of four resources. knowing the symptom table cold lets you triage in seconds. resource symptom common tools cpu high cpu%, requests queue up, latency scales with load , flame graphs, , task manager memory growing working set, frequent gc pauses, eventual , , memory profiler disk io high queue length, latency spikes on read/write, stalls near file/db calls , resource monitor, db wait stats network latency proportional to round trips, timeouts under load, works fine `,"fundamentals/processes-threads-and-scheduling":`every concurrency bug and every "why is this slow under load" question eventually traces back to how the os models processes, threads, and scheduling. interviewers use this topic to check whether you understand what's actually happening below your keyword. process vs thread a process is an isolated unit of execution with its own virtual address space; a thread is a unit of scheduling within a process, sharing that address space with its sibling threads. property process thread address space own, isolated shared with sibling threads creation cost high (100s of µs — allocate address space, tables) low ( 10s of µs — reuse existing address space) isolation strong — one process can't corrupt another's memory weak — a bad pointer in one thread can crash the whole process communication ipc required (pipes, sockets, shared memory) direct — shared heap, shared globals failure blast radius contained to the process contained to the process — one thread crashing takes down all threads context switch cost higher (address space/tlb flush) lower (registers + stack pointer only) [!key] threads are cheap to create and communicate through shared memory, but that same sharing is exactly what causes races, and one crashed thread kills every other thread in the process. context switching and its real cost a context switch is the cpu saving one thread's state (registers, program counter, stack pointer) and loading another's. between threads of the same process this is relatively cheap — shared address space means the memory management unit (mmu) mappings stay valid. between processes , the switc`,"fundamentals/memory-and-virtual-memory":`almost every "why is this slow" or "why did this crash" question at the systems level comes back to memory: how it's addressed, how it's cached, and what happens when you run out of it. this is one of the few topics where a concrete, numeric answer beats a hand wavy one. why virtual memory exists every process sees its own private, contiguous virtual address space , translated by hardware into physical ram addresses. this buys three things: isolation (one process can't read or corrupt another's memory), simplicity (every process can assume it owns address upward, regardless of what's physically free), and the ability to run programs larger than physical ram by paging parts to disk. [!key] virtual memory is an indirection layer. the cpu never lets a normal instruction touch a physical address directly — it always goes through translation, which is what makes isolation and overcommit possible. pages, page tables, and the tlb physical and virtual memory are divided into fixed size pages (commonly 4kb). a page table maps virtual page numbers to physical frame numbers, maintained per process by the os and consulted by the cpu's memory management unit (mmu) on every memory access. walking a multi level page table on every access would be far too slow, so the cpu caches recent translations in the tlb (translation lookaside buffer) — a small, extremely fast hardware cache. a tlb hit resolves a virtual address in about a cycle; a tlb miss requires a full page table walk, costing tens to hundreds of cycles. [!warning] frequent context switches between processes can flush or partially`,"fundamentals/networking-tcp-and-ip":`almost every backend interview eventually touches the network layer — sockets that won't close, port exhaustion in production, or "why is tcp slower than udp here". you don't need to reimplement a network stack, but you do need to reason precisely about what each layer guarantees. network building blocks a computer network connects two or more devices so they can exchange data and share resources, and the internet is simply the largest instance of one — a global mesh of lans (local networks), mans (city scale networks), and wans (wide area links) stitched together by isps. how devices are wired together ( topology ) and how responsibility is split between them ( architecture ) both shape what a network can do. a star or mesh topology trades cabling and configuration cost for fault tolerance; a client server architecture centralises data and control, which is how most production systems are built, while a peer to peer architecture spreads both across every node instead. for a quick side by side comparison rather than the full pros and cons breakdown above, cost, scalability and reliability trade off differently across the same seven shapes: delivery itself comes in four shapes worth naming precisely: unicast (one sender, one receiver), broadcast (one sender, every host on the segment), multicast (one sender, a specific subscribed group), and anycast (one address routed to whichever member of a group is topologically nearest — how dns root servers and many cdns answer from the closest edge location without the client ever knowing multiple servers exist). three numbers describ`,"fundamentals/http-versions-and-protocols":`http questions in interviews split into two flavours: "what changed between versions and why" and "which real time protocol would you pick for this feature". both reward knowing the trade offs cold rather than reciting a version history. http/1.1 vs http/2 vs http/3 property http/1.1 http/2 http/3 transport tcp tcp quic (over udp) multiplexing no (one request in flight per connection without pipelining) yes, multiple streams per connection yes, independent streams header compression none hpack qpack server push no yes (rarely used, mostly deprecated in practice) yes (same caveat) head of line blocking yes, at the application level reduced at app level, but reintroduced at tcp level (one lost packet stalls all streams) eliminated — quic streams are independent even at the transport level connection setup tcp handshake only tcp + tls handshake quic combines transport + tls 1.3 handshake, supports 0 rtt [!key] http/2 fixed head of line blocking at the http layer by multiplexing streams over one tcp connection, but a single lost tcp packet still stalls every stream on that connection, because tcp itself only knows one ordered byte stream. http/3 fixes this properly by moving multiplexing into quic, where each stream's loss is independent. persistent connections and pipelining http/1.0 opened a new tcp connection per request — enormously wasteful given the handshake cost. http/1.1 introduced persistent connections ( by default) so multiple requests could reuse one tcp connection sequentially. pipelining tried to let a client send several requests without waiting for each respons`,"fundamentals/dns-and-connection-setup":`"what happens when you type a url into a browser and press enter" is one of the most reliable senior interview questions because it forces you to connect dns, tcp, tls, and http into one coherent timeline. getting the order and the cost of each step right is what separates a strong answer from a vague one. the full journey of a request each step is a real, measurable cost. a cold connection to a new host pays for all of it; a warm, reused connection skips straight to the last step. before dns: joining the network and resolving local addresses long before a browser resolves , the device itself needs an address and a way to find other devices on its own local segment — two jobs dns doesn't do at all. dhcp (dynamic host configuration protocol) automatically assigns a device's ip address, subnet mask, default gateway, and dns servers when it joins a network, through a four step exchange remembered as dora : discover (the client broadcasts asking for an address), offer (a dhcp server proposes one), request (the client asks to actually use it), acknowledge (the server confirms the lease). once a device has an ip, sending anything on the local segment still requires a mac address, not just an ip — that's what arp (address resolution protocol) is for: a device broadcasts "who has this ip?" on the local segment, and the owner replies with its mac address, which gets cached briefly so the lookup isn't repeated for every packet. if the destination isn't on the local segment at all, the packet goes to the default gateway learned via dhcp, and if that gateway is the edge of a private ne`,"fundamentals/serialization-and-data-formats":'serialization decisions look small in the moment — "just use json" — but they quietly determine how easily two services can evolve independently, how much a payload costs to move over the wire, and whether a deserialiser becomes an attack surface. interviewers use this topic to see if you think about data contracts, not just syntax. comparing the major formats format size speed schema human readable evolution support tooling json medium medium optional (json schema) ✅ yes manual discipline (additive fields) universal xml large slow optional (xsd) ✅ yes manual discipline, verbose universal, mature protobuf small (binary) fast required ( ) ❌ no built in (field numbers, optional/reserved) strong (grpc ecosystem) avro small (binary) fast required, schema travels with data or registry ❌ no strong (reader/writer schema resolution) strong in big data/kafka ecosystems messagepack small (binary) fast optional ❌ no manual discipline, like json good, less universal than json csv small (text) fast to parse, naive format none ✅ yes none — brittle to column changes universal but primitive [!key] there is no universally "best" format — the right choice depends on whether you need human debuggability (json/xml), wire efficiency (protobuf/avro/messagepack), or neither and just tabular simplicity (csv). naming this trade off explicitly is the senior answer. text vs binary trade offs text formats (json, xml, csv) are debuggable with your eyes — you can open a payload in a text editor, an endpoint and read the response, or diff two versions in source control. they pay for this with larger size',"hld-problems/url-shortener":'a url shortener turns a long link into a compact, shareable code and redirects visitors back to the original destination. it looks trivial but is a great vehicle for testing key generation under concurrency, redirect semantics, and read heavy scaling. requirements functional shorten a long url into a unique short code (e.g. ). redirect a short code to the original long url. let users request a custom alias instead of a generated code. support an optional expiration date per link. track basic click analytics (count, timestamp, referrer, rough location). non functional redirect latency low, target p99 under 100ms. redirects must stay available even if the write path degrades — read availability over write availability. short codes must be globally unique, no collisions. analytics can be eventually consistent; redirects cannot be. design for billions of stored urls with a read:write ratio of roughly 100:1. out of scope full user account and billing system. malware/phishing url scanning. a rich analytics dashboard ui (only the ingestion pipeline matters here). [!tip] pin down scope with the interviewer before designing: is this "url management" only, or does it also expect account/auth and a real analytics product bolted on? a bitly style prompt often bundles all three, and clarifying up front stops you from over building the wrong tier. scale estimation metric assumption working result new urls created 500m/month given 500m/month write qps (avg) 500m / (30 × 86,400s) 500,000,000 / 2,592,000 193 writes/sec write qps (peak) 3× average burst 193 × 3 600 writes/sec read:write rati',"hld-problems/rate-limiter":"a rate limiter rejects requests once a client exceeds an allowed quota, protecting backends from abuse and overload. the interesting parts are algorithm choice, keeping counters correct across many nodes, and deciding what happens when the limiter itself fails. requirements functional enforce limits per identity (user id, api key, or ip) and optionally per endpoint. support multiple limit tiers (e.g. free vs paid) and burst allowances. return with headers describing the limit, remaining quota, and reset time. allow rules to be added or changed without redeploying the service (hot reload). non functional add minimal latency to every request, target under 5ms p99 for the rate limit check itself. stay accurate under concurrent requests from the same identity across many gateway nodes. remain highly available — a rate limiter outage should not be able to take down the whole platform. scale horizontally with request volume, independent of backend service scaling. out of scope billing/metering and quota systems (this is throttling, not invoicing). ddos mitigation at the network/waf layer. a ui for managing rules (only the config propagation mechanism matters here). scale estimation metric assumption working result global request rate peak gateway traffic given 200,000 req/sec rate limit checks needed one per request 200,000 req/sec 200,000 ops/sec active rate limited identities concurrent users/keys given 50m bytes per counter (key + count + ttl) small fixed size record 50 bytes 50 bytes counter store memory 50m × 50 bytes 50,000,000 × 50 2.5 gb redis ops per node typical single ","hld-problems/distributed-cache":`a distributed cache stores frequently accessed key value data in memory across many nodes, sitting in front of a slower database to absorb read traffic and cut latency. the interesting engineering is in the parts that make it distributed rather than just "redis on one box" — how keys are routed to nodes, what happens when a node joins, leaves, or dies, and how eviction stays fast at millions of operations per second. requirements functional , , with millisecond latency. automatic eviction of least recently used entries when a node is at capacity. data is distributed across many nodes; no single node holds the whole dataset. nodes can be added or removed with minimal data movement and no downtime. optional ttl based expiration alongside capacity based eviction. non functional p99 latency under 1 ms for an in memory hit. horizontally scalable to terabytes of cached data across hundreds of nodes. tolerates individual node failure without losing availability (some staleness or a small blip is acceptable; a full outage is not). adding/removing a node should remap only a small fraction of keys, not the whole keyspace. cache is best effort — losing a node's data is not "data loss" in the durability sense, since the source of truth is the underlying database. out of scope being a system of record (this is a cache, not a database) — durability guarantees are intentionally weaker than the backing store's. complex query capabilities beyond key based access (no secondary indexes, no joins). the application level cache aside vs. write through decision for every specific use case (touche`,"hld-problems/online-judge":"an online judge lets engineers browse coding problems, write and submit a solution in the language of their choice, get instant pass/fail feedback, and compete on a live leaderboard during timed contests. the defining challenge is running arbitrary, untrusted user code safely and quickly, at a volume that spikes hard the moment a popular contest starts. requirements functional users can browse a paginated list of coding problems. users can view a problem's statement and write a solution in their chosen language. users can submit a solution and receive pass/fail feedback against test cases quickly. users can view a live leaderboard during a coding competition. non functional user submitted code must run in complete isolation — no access to the host, network, or other users' data. submission feedback should feel close to instant (seconds, not minutes). the system must scale to competitions with 100,000 concurrent participants. leaderboard reads must stay fast even as a competition's submission volume climbs. out of scope problem authoring/editorial tooling for content creators. payment/subscription tiers. discussion forums and solution sharing features. scale estimation metric assumption working result dau given given 5m problem views / user / day 5 avg 5m × 5 25m views/day submissions / user / day 3 avg 5m × 3 15m submissions/day read qps (avg) 25m / 86,400s 25,000,000 / 86,400 290/sec read qps (peak) 4× average 290 × 4 1,160/sec write qps (avg, submissions) 15m / 86,400s 15,000,000 / 86,400 175/sec write qps (peak, contest burst) 5× average 175 × 5 875/sec read : write rati","hld-problems/news-feed":`a news feed shows each user a ranked stream of posts from the people they follow. the core tension is that writing a post is cheap, but delivering it to millions of followers instantly is not — and the answer changes completely once one account can have tens of millions of followers. requirements the product surface here is deceptively simple to sketch on a whiteboard — post, follow, and a ranked feed: functional users can create posts (text, image, video) and follow/unfollow other users. users can view a feed of posts from people they follow, ranked or chronological. the feed supports pagination as the user scrolls. new posts should appear in followers' feeds within a reasonable delay (seconds, not real time critical). non functional feed reads should be fast, target under 200ms p99. read heavy: users check their feed far more often than they post. eventual consistency is acceptable — a post appearing a few seconds late is fine; availability matters more than strict consistency. must handle a small number of accounts with tens of millions of followers ("celebrities") without degrading the system for everyone else. out of scope direct messaging/chat. stories/ephemeral content. ads ranking and auction logic. scale estimation metric assumption working result dau given given 500m posts per user per day 0.5 avg 500m × 0.5 250m posts/day write qps (avg) 250m / 86,400s 250,000,000 / 86,400 2,900 writes/sec write qps (peak) 3× average 2,900 × 3 8,700 writes/sec feed reads per dau/day 10 checks/day 500m × 10 5b reads/day read qps (avg) 5b / 86,400s 5,000,000,000 / 86,400 58,000 rea`,"hld-problems/chat-system":`a chat system delivers messages between users in real time, whether they're online now or come back hours later. the hard parts are not the database — they're routing a message to whichever server holds a recipient's live connection, and doing it reliably at the scale of billions of messages a day. requirements the functional shape maps directly onto a diagram worth sketching early — who talks to whom, and what has to survive a disconnect: functional one to one and group chat (groups up to 100 members). real time delivery to online recipients; store and forward delivery to offline ones, including a push notification. delivery status per message: sent, delivered, read. presence ("online" / "last seen at ..."). media messages (images, video, files). non functional low latency delivery to online users, target under 100ms within a region. no message loss — durability matters even if a server or connection drops mid send. scale to billions of messages/day and hundreds of millions of concurrent connections. strict global ordering is not required; per chat ordering should be best effort and visibly consistent to users. out of scope voice/video calling. chat backup/export. spam and abuse detection pipelines. scale estimation metric assumption working result dau given given 500m messages sent per user/day 40 avg 500m × 40 20b messages/day write qps (avg) 20b / 86,400s 20,000,000,000 / 86,400 230,000 msgs/sec write qps (peak) 3× average 230,000 × 3 700,000 msgs/sec concurrent websocket connections 20% of dau online at once 500m × 0.2 100m concurrent connections per ws server typical `,"hld-problems/notification-system":"a notification system is the shared pipeline other services use to reach users through push, sms, email, and in app messages. the central challenge is that a single login code and a million user marketing blast go through the same pipeline, and the blast must never be allowed to delay the code. requirements the requirements sketch is worth drawing before anything else, since the whole design hinges on keeping two very different traffic shapes apart: functional send notifications across push, sms, email, and in app channels through a common api. support templates with variables and per locale localization. respect user channel preferences and quiet hours. deduplicate notifications and support batching/digests for non urgent content. track delivery status per notification and retry on transient failures. non functional critical, low volume traffic (otps, security alerts) must never be delayed by bulk campaign traffic. at least once delivery with idempotency to avoid duplicate sends on retry. durable: a notification accepted by the api must not be silently lost. tolerate individual third party provider outages without failing the whole channel. out of scope campaign authoring ui / marketing segmentation tools. deep analytics/bi dashboards on engagement. in app chat (a different, bidirectional real time system). scale estimation metric assumption working result total notifications/day mixed critical + bulk given 2b/day avg send qps 2b / 86,400s 2,000,000,000 / 86,400 23,000/sec peak qps (campaign burst) 5× average 23,000 × 5 115,000/sec critical/transactional share 5% of volume","hld-problems/photo-sharing-platform":"a photo sharing platform lets users post photos and videos with a caption, follow other users, and scroll a chronological feed of everyone they follow. the interesting problems are the same two that dominate every social product: fanning out new posts to millions of followers without melting the database, and moving very large media files in and out of the system fast enough that uploads and playback feel instant. requirements functional users can create a post containing a photo or video plus a short caption. users can follow and unfollow other users. users can view a chronological feed of posts from the people they follow. users can open a post and see its media render immediately. non functional feed delivery must be low latency, under 500ms, even for a user who follows thousands of accounts. media (photos up to 8mb, videos up to 4gb) must render instantly for the viewer regardless of network conditions. the system must scale to 500m daily active users. availability over strict consistency — a post that appears in a follower's feed a few seconds late is acceptable. out of scope comments, likes, and direct messaging. content moderation and recommendation/explore ranking. stories or ephemeral content. scale estimation metric assumption working result dau given given 500m posts created / user / day 0.2 avg 500m × 0.2 100m posts/day feed views / user / day 15 scroll sessions 500m × 15 7.5b feed reads/day write qps (avg) 100m / 86,400s 100,000,000 / 86,400 1,160 writes/sec write qps (peak) 5× average (evening spike) 1,160 × 5 5,800 writes/sec read qps (avg) 7.5b / 86,400s 7,5","hld-problems/live-video-comments":`live video comments let viewers post messages on a live stream and see a continuous, near real time scroll of everyone else's comments while watching. the problem looks like chat at first glance, but the read side is wildly asymmetric — one popular stream can have millions of viewers passively watching a handful of people type, so the design has to treat "fan out to viewers" as the central problem, not comment storage. requirements functional viewers can post comments on a live video feed. viewers see new comments appear while they continue watching, without refreshing. viewers who join mid stream can see comments posted before they joined. non functional new comments should reach viewers within a couple of seconds, not milliseconds — this is not a trading system. the system must scale to a single live video with millions of concurrent viewers. a single popular stream can generate thousands of comments per second; delivery must not degrade for viewers as that rate climbs. prefer availability and simplicity (http based delivery) over a heavier bidirectional protocol. out of scope comment moderation, spam filtering, and reactions/likes on comments. video ingestion, transcoding, and playback itself. direct messaging between viewers. scale estimation metric assumption working result platform daily viewers given given 50m peak concurrent viewers on one "mega stream" given given 10m peak comment write rate on a mega stream given given 5,000 comments/sec naive per viewer polling (every 5s) 10m viewers / 5s 10,000,000 / 5 2,000,000 reads/sec cdn snapshot reads (1 refresh/sec, cache`,"hld-problems/ecommerce-platform":"an e commerce platform lets users browse a catalogue, hold items in a cart, and check out. the catalogue is a massive read, tolerant of staleness problem; checkout is the opposite — low volume, but it must never oversell inventory or double charge a customer. requirements functional browse and search a product catalogue. add/remove items in a cart, for both guests and logged in users. check out: reserve inventory, charge payment, create an order, hand off to shipping. apply promotions/coupons and compute final pricing. view order status after purchase. non functional product pages and search are extremely read heavy; must be fast (under 100ms) even if slightly stale. checkout must be strongly consistent for inventory and payment — no overselling, no double charges. must survive flash sale traffic spikes (an order of magnitude burst on a subset of skus). search indexing can lag the source of truth by seconds; inventory availability cannot. out of scope recommendation/personalization engine. full returns/refunds workflow (briefly touched, not designed in depth). seller onboarding and marketplace multi tenant concerns. scale estimation metric assumption working result product page views/day 50m dau × 20 views 50,000,000 × 20 1b/day page view qps (avg) 1b / 86,400s 1,000,000,000 / 86,400 11,600/sec page view qps (peak) 3× average 11,600 × 3 35,000/sec search queries/day given given 500m/day search qps (avg) 500m / 86,400s 500,000,000 / 86,400 5,800/sec orders/day 2% conversion of dau visits 2m/day 2m/day checkout qps (avg) 2m / 86,400s 2,000,000,000 / 86,400,000 23/sec checkout","hld-problems/payment-system":"a payment system lets merchants accept money from customers without building their own connection to card networks and banks. the entire design exists to answer one question with certainty: for every dollar that moves, can you prove exactly where it went, exactly once? requirements the functional scope is small on paper — create an intent, charge it, refund it, notify the merchant — which is exactly why it's worth sketching explicitly, since almost all the real complexity lives in the non functional column next to it: functional merchants create a payment intent and charge a customer's payment method. support refunds against a prior charge. notify merchants of payment events via webhooks. expose transaction status and history for lookup. non functional no transaction is ever lost, even across process crashes or network failures — durability and auditability are non negotiable. effectively once money movement: never double charge, never silently drop a charge. high throughput, target 10,000+ tps at peak. security: minimize the scope of raw card data ever touching your own servers (pci dss). out of scope fraud detection ml models. currency conversion/fx rate logic. tax calculation. scale estimation metric assumption working result average tps steady state given 2,000 tps peak tps requirement given 10,000 tps transactions/day 2,000 tps × 86,400s 2,000 × 86,400 173m/day ledger entries/day double entry, 2 entries/txn 173m × 2 346m entries/day ledger storage/day 200 bytes/entry 346m × 200b 69 gb/day ledger storage (7yr audit retention) 69gb × 365 × 7 69gb × 2,555 176 tb webhook e","hld-problems/ticket-booking":`a ticket booking system lets users find an event, pick seats, and pay before someone else takes the same seat. the defining challenge is contention: at the moment a popular show goes on sale, far more people want a seat than there are seats, all within the same few seconds. requirements the core loop — browse, hold, pay, confirm — is worth sketching before diving into the contention problem it creates: functional browse and search events; view a seat map with real time availability. hold selected seats temporarily while the user completes payment. confirm the booking once payment succeeds; release the hold if it doesn't. prevent any seat from being sold to two different users. non functional correctness is paramount: zero double bookings, even under extreme concurrent load. handle a massive burst of demand the instant a popular event goes on sale (a "thundering herd"). seat map views must reflect near real time availability so users don't pick a seat that's already gone. browsing/search latency should stay low even while a flash sale is in progress elsewhere on the platform. out of scope dynamic/surge pricing algorithms. venue and seller onboarding tooling. secondary market/resale marketplace. scale estimation metric assumption working result seats in a popular venue given given 50,000 concurrent buyers at on sale moment flash sale demand given 10m within first 10 min naive booking attempt rate 10m / 600s 10,000,000 / 600 16,700 req/sec success rate of those attempts 50,000 seats / 10m buyers 50,000 / 10,000,000 0.5% platform wide page views/day general browsing given 200m/`,"hld-problems/auction-system":`an auction system lets sellers list an item with a starting price and end date, and lets buyers bid against each other, with only strictly higher bids accepted. the core tension is consistency versus scale: every bid must be checked against the current highest bid without a race condition, yet the system also has to broadcast that highest bid to every interested viewer in real time, across potentially millions of auctions running at once. requirements functional sellers can post an item for auction with a starting price and end date. buyers can bid on an item; a bid is only accepted if it is strictly higher than the current highest bid. anyone can view an auction, including its current highest bid, in near real time. non functional strong consistency on bid acceptance: two simultaneous bids must never both be accepted as "the highest." fault tolerance and durability: an accepted bid must never be silently lost, even during partial failures. the current highest bid should update for viewers in real time, not on manual refresh. scale to 10m concurrent auctions. out of scope payment processing and escrow once an auction closes. seller/item verification and fraud detection. search and discovery/browsing of active auctions. scale estimation metric assumption working result concurrent live auctions given given 10m avg auction duration assumption given 2 days new auctions started/day concurrency ÷ duration (little's law) 10,000,000 / 2 5m auctions/day avg bids per auction assumption given 15 total bids/day 5m auctions × 15 bids 5,000,000 × 15 75m bids/day write qps (avg) 75m / 86,`,"hld-problems/ride-sharing":`a ride sharing service matches a rider who wants to go somewhere with a nearby available driver, tracks the trip live, and settles payment at the end. the hard parts are all about location at scale — ingesting a firehose of gps pings, finding the nearest free driver in milliseconds, and making sure two riders never get matched to the same driver. requirements the product loop is simple to sketch even though the engineering underneath it isn't: estimate, request, match, ride: functional rider gets a fare estimate for a pickup and destination before booking. rider requests a ride and is matched with one nearby, available driver. driver receives the request and can accept or decline within a short window. both parties see each other's live location during the trip. fare is computed and charged automatically when the trip ends, including any surge multiplier. both sides can view trip history. non functional match latency: p99 under 5 seconds in a dense city. location ingestion must scale to hundreds of thousands of writes/second. strong consistency on the invariant "a driver has at most one active ride" — this cannot be eventually consistent. location display can be eventually consistent — a driver marker lagging by a second is fine. trip and payment records must be durable and auditable. out of scope driver onboarding, kyc, background checks. in app chat, ratings, and review moderation. multi modal trip legs (walking + transit + car). deep fraud/anti abuse modeling (mentioned only where it touches matching). [!key] everything in this design is downstream of one fact: driver lo`,"hld-problems/video-streaming":"a video streaming platform lets users upload videos, processes them into a form that plays smoothly on any device and network, and serves them to a massive audience through a cdn. the interesting problems sit at two extremes — a slow, heavy write path (transcoding a large file) and a blisteringly fast, huge fan out read path (millions of concurrent viewers). requirements the functional shape — upload, process, watch, count — is simple enough to sketch as a single picture before the scale numbers make clear why each step is hard: functional users can upload a video with title/description metadata. the platform transcodes the video into multiple resolutions/bitrates. users can play a video that adapts quality to their network in real time. view counts and basic engagement metrics are tracked. users can browse a home feed with recommended videos (mentioned, not deep dived). non functional upload must support large files (multi gb) and resume after network failure. playback start latency under 2 seconds; no visible rebuffering on typical connections. storage is cost sensitive at scale — petabytes of video, mostly rarely accessed after the first weeks. view count must be near real time but doesn't need to be exact to the second. high read availability — a playback outage is worse than a stale metadata field. out of scope content moderation pipeline (covered separately). detailed recommendation ranking model. monetization/ads insertion logic. live streaming is discussed briefly for contrast, not designed in full. [!key] almost every hard decision in this system comes down to do t","hld-problems/search-autocomplete":`search autocomplete suggests completions as a user types, and it has one unforgiving constraint that shapes the whole design — every keystroke is a query, and the response has to land in well under 100 ms or the feature feels broken. the system is a good showcase for offline build plus online serve architecture. requirements functional as a user types a prefix, return the top k most likely completions. suggestions should reflect real search popularity, not just alphabetical order. new trending terms should surface within minutes to hours, not days. basic typo tolerance (one edit distance away) is nice to have. personalization (recent searches, location) is a stretch goal. non functional p99 latency under 100 ms per keystroke, ideally under 50 ms. extremely read heavy: essentially every keystroke of every search is a read. freshness can lag — a term trending in the last few minutes doesn't need to appear instantly. highly available — a suggestion outage should degrade to "no suggestions," never to a slow or broken search box. out of scope the full text search/ranking engine behind the actual search results page. query understanding/nlp beyond simple prefix and edit distance matching. ads or sponsored suggestions. [!key] autocomplete is an offline heavy, online light system: almost all the real work — counting queries, ranking, building the data structure — happens in a batch/streaming pipeline well before any user types a key. the online path just does a fast lookup against something already computed. scale estimation metric estimate arithmetic searches/day 1 billion given, `,"hld-problems/file-sync-and-storage":"a file sync and storage platform lets a user upload a file from one device, have it automatically appear on every other device they own, download it back, and share it with other users — all while treating multi gigabyte files and unreliable networks as the normal case, not the exception. requirements functional users can upload a file from any device. users can download a file from any device. users can share a file with other users. files automatically sync across all of a user's devices. non functional uploads, downloads, and sync must stay fast even for very large files. the system must support large files reliably over unreliable networks (mobile, flaky wi fi). file data must be secure: encrypted in transit and at rest, with access control on sharing. sync should be near real time — a change on one device should reach other devices within seconds to a couple of minutes. out of scope real time collaborative editing of the same file (like a shared document editor). full version history/diffing ui. payment/billing for storage tiers. scale estimation metric assumption working result dau given given 50m uploads / user / day 2 avg 50m × 2 100m uploads/day downloads / user / day 5 avg 50m × 5 250m downloads/day write qps (avg) 100m / 86,400s 100,000,000 / 86,400 1,160/sec write qps (peak) 3× average 1,160 × 3 3,500/sec read qps (avg) 250m / 86,400s 250,000,000 / 86,400 2,900/sec read qps (peak) 3× average 2,900 × 3 8,700/sec read : write ratio 2,900 : 1,160 — 2.5 : 1 avg file size (blended) 4mb assumption given 4mb daily upload storage (raw) 100m × 4mb 100,000,000 × 4mb 400tb","hld-problems/proximity-search":"a proximity search platform lets users find local businesses by location, category, and free text query, view a business's details and reviews, and leave a review of their own. unlike most systems in this track, the data volume here is modest — the actual challenge is that a single search query mixes three fundamentally different kinds of filtering (geospatial, categorical, full text) that a naive relational query handles very badly. requirements functional users can search for businesses by location, free text query, and category. users can view a business's details, including its reviews and average rating. users can leave a review (rating plus optional text) for a business. a user can leave at most one review per business. non functional search queries combining location, text, and category must stay fast as the business/review corpus grows. a business's average rating shown in search results must be accurate and reasonably fresh, not stale. read heavy: searches and business views vastly outnumber review submissions. support searching by informal location names (a city or neighborhood), not just raw coordinates. out of scope business claiming/verification workflows for owners. photo uploads attached to reviews. recommendation/personalized ranking of search results. scale estimation metric assumption working result dau given given 30m searches / user / day 3 avg 30m × 3 90m searches/day business views / user / day 5 avg 30m × 5 150m views/day reviews / user / day 0.02 avg (rare) 30m × 0.02 600k reviews/day read qps (avg) (90m + 150m) / 86,400s 240,000,000 / 86,400 2,780/s","hld-problems/distributed-job-scheduler":"a distributed job scheduler runs work — cron style recurring jobs or one off delayed tasks — reliably across a fleet of workers, without a single machine's cron table as a bottleneck or a single point of failure. the core tension is between running every job exactly once conceptually and the reality that distributed systems can only promise at least once delivery cheaply. requirements functional users define jobs with a schedule (cron expression, fixed interval, or one off delay) and a payload/handler. the scheduler triggers each job at the right time and dispatches it to a worker for execution. failed jobs are retried with backoff, up to a configurable limit. long running jobs report progress via heartbeats; a stalled job is detected and retried. multiple tenants (teams/services) share the scheduler with fair capacity allocation. job status and history are queryable (succeeded, failed, running, pending). non functional no duplicate scheduling of the same job occurrence, even with multiple scheduler instances running. at least once execution guarantee, with the expectation that job handlers are idempotent. scales to millions of scheduled jobs and tens of thousands of triggers per second at peak. scheduling precision within a few seconds of the target time is acceptable; sub second precision is not a goal. scheduler control plane must survive individual node failure without missing triggers. out of scope the business logic inside individual jobs. a general purpose workflow/dag engine (multi step orchestration) — this is single job scheduling. ui/dashboard for authoring jobs.","hld-problems/content-moderation-pipeline":`a content moderation pipeline decides whether user generated content (posts, images, comments) is safe to publish, using a mix of automated checks, machine learning classifiers, and human reviewers. it's a good showcase for asynchronous, multi stage pipeline design, because no single stage can be allowed to block or lose content, and the stages have wildly different latencies and failure modes. requirements functional every piece of submitted content passes through automated checks before publishing. content flagged as likely violating is routed to a human review queue. reviewers can approve, reject, or escalate content, and their decision is recorded. rejected content can be appealed, and appeals are tracked. moderation rules can be updated without a code deployment. non functional no content is silently lost between stages, even under partial outages. each stage can scale and fail independently — a slow ml model shouldn't block ingestion. automated only content gets a decision within seconds; human reviewed content within a defined sla (e.g., hours). every decision is auditable — who/what made it, when, and why. the pipeline must be resumable: a stage restart shouldn't reprocess already completed work or drop in flight work. out of scope the training/labeling pipeline for the ml models themselves. legal/policy definition of what counts as violating content. payment/billing implications of moderation decisions. [!key] this is fundamentally a pipeline of independently scalable, independently failing stages connected by a durable message broker — not a single service that "c`,"hld-problems/realtime-analytics-pipeline":"a realtime analytics pipeline ingests a huge volume of discrete events — ad clicks, game events, metric samples — and produces aggregated counts and rollups that a dashboard or a billing system can query with low latency. the central tension is between processing speed and correctness: events arrive out of order, late, and sometimes duplicated, but the aggregate numbers still have to be trustworthy enough to bill an advertiser. requirements the shape of the problem — clicks in, aggregated metrics out — is simple to state and worth sketching before the correctness concerns pile on: functional ingest a high volume stream of events (e.g., ad clicks) tagged with an entity id (ad id) and timestamp. aggregate counts/sums over fixed time windows (per minute, per hour, per day). serve near real time aggregate queries to a dashboard. support backfill/reprocessing of historical data when logic changes. handle late arriving events correctly within a bounded lateness window. non functional end to end latency from event to visible aggregate: seconds, not minutes. aggregation must be idempotent — replays or retries must not double count. must survive a burst of 10x normal traffic without losing events. must tolerate uneven load across keys (a viral ad gets far more clicks than an average one) without one hot key stalling the whole pipeline. cost must scale sub linearly with volume where possible (via pre aggregation). out of scope fraud/click abuse detection logic itself (only idempotency related prevention is covered). the dashboard's ui/visualization layer. long term data warehousing/b","hld-problems/web-crawler":`a web crawler is a program that automatically traverses the web — fetching pages, extracting their content and links, and following those links to discover more pages — to index content for search, gather research data, or monitor sites for changes. at the scale of billions of pages, the interesting problems stop being "how do i parse html" and become fault tolerance, politeness, and raw throughput engineering. requirements functional given a set of seed urls, the crawler fetches each page, extracts its text content, and discovers linked urls to crawl next. extracted text content is stored for downstream use (indexing, analysis). the crawler respects each site's rules and crawl delay directives. non functional scale to crawling 10 billion pages within 5 days. fault tolerant: a crashed worker must not lose in flight progress or duplicate completed work. polite: the crawler must not overwhelm any single site with request volume. avoid redundant work: the same url or the same content should not be processed twice. out of scope ranking/ordering search results built from crawled content. rendering javascript heavy single page applications in full (beyond a noted extension). natural language processing of extracted text. scale estimation metric assumption working result target corpus given given 10b pages target crawl duration given given ≤5 days avg page size given given 2mb network throughput budget (per machine class) given given 200 gbps theoretical fetch rate 200gbps ÷ 8 bits/byte ÷ 2mb/page 200,000,000,000 / 8 / 2,000,000 12,500 pages/sec effective fetch rate (30% real worl`,"hld-problems/metrics-and-monitoring":"a metrics monitoring platform collects performance data (cpu, memory, throughput, latency) from servers and services, stores it as time series data, visualizes it on dashboards, and triggers alerts when thresholds are breached — think datadog, prometheus/grafana, or aws cloudwatch. it's infrastructure engineers rely on to understand system health and respond to incidents, which makes its own reliability and latency non negotiable: the monitoring system going down during an incident is the worst possible failure mode. requirements functional services can ingest metrics (name, labels, value, timestamp) into the platform. users can query and visualize metrics on dashboards over arbitrary time ranges. users can define alert rules with thresholds against a metric query. users receive notifications (slack, pagerduty, etc.) when an alert rule's condition is met. non functional ingest millions of metric data points per second without becoming the bottleneck for the services being monitored. dashboard queries over weeks of historical data must stay low latency. alert latency — time from a threshold breach to a firing notification — should be under a minute. the monitoring system itself must remain highly available, including during the traffic spikes that accompany incidents. out of scope distributed tracing and log aggregation (related but distinct systems). anomaly detection/ml based alerting (only threshold based alerting is in scope). long term (multi year) data archival and compliance retention policies. scale estimation metric assumption working result reporting hosts/services","lld/oop-fundamentals":`every lld round starts by probing whether you actually understand objects or just know the vocabulary. the four pillars are table stakes — what separates a senior answer is naming the trade off, spotting the liskov trap, and knowing exactly when composition beats inheritance. classes and objects a class is a blueprint — it defines the attributes and methods that every instance built from it will share, but it occupies no memory on its own. an object is a concrete instance of that blueprint, created at runtime with the keyword, holding its own independent state in memory. many objects can come from one class, each with a different state but identical behaviour. [!key] think classes → objects → applications : you design the blueprint once, instantiate it as many times as the runtime needs, and build the system out of how those instances collaborate. a constructor's only job is initializing that per instance state — it runs exactly once, at creation, and (aside from static members) every other member operates on the state it set up. encapsulation encapsulation bundles data and the behaviour that operates on it into one unit, and restricts direct access to that data from outside. the interview definition: "hiding internal state and forcing all interaction through a controlled interface." it is achieved with access modifiers, not just getters and setters — a public getter that returns a mutable list is not encapsulated at all. [!warning] returning a private field directly from a getter leaks a mutable reference — the caller can mutate internal state without going through your va`,"lld/composition-over-inheritance":`"prefer composition over inheritance" is the single most quoted design rule in lld interviews, and also the most shallowly answered. a senior response explains why hierarchies break, shows the refactor, and still names the narrow cases where inheritance is correct. why deep inheritance hierarchies break three failure modes show up repeatedly once a hierarchy grows past two levels. fragile base class problem — a change to a base class (even a seemingly safe one, like adding a call inside an existing method) can silently break every subclass that overrides related behaviour, because subclasses depend on the base's implementation , not just its contract. rigid taxonomy — real world categories don't stay clean. modelling works until you need , and now every bird either fakes flight or the taxonomy has to be redesigned. combinatorial explosion — if behaviour varies along two independent axes (say, "can fly" × "can swim"), subclassing needs one class per combination : , , , . add a third axis and the class count multiplies again. [!key] inheritance couples a subclass to the base class's implementation . composition couples an object only to a contract (an interface). contracts change far less often than implementations, so composition is structurally more stable over the life of a codebase. the classic duck/bird example the textbook illustration of the taxonomy problem: modelling flight as an inherited method. is forced to override just to break the promise the base class made. any code that treats a polymorphically and calls on each one can now blow up at runtime for a perfectly`,"lld/solid-principles":'solid is asked in nearly every lld round, usually as "tell me about a time you applied one" rather than "define srp". the principles are simple to recite and easy to misstate — the interview signal is in the subtleties: srp is about reasons to change , not "one method", and dip is not the same thing as dependency injection. single responsibility principle a class should have one reason to change — not "one method" or "does one thing", which is a common oversimplification. "reason to change" means one axis of responsibility owned by one stakeholder or concern. smell: a class name that needs "and" to describe ( ), or a code review where every unrelated feature touches the same file. real system: a that validates input, applies business rules, and writes audit logs is the single most common srp violation in real backends. [!warning] srp is not "a class should have one method." a class can have ten methods and still have a single responsibility if they all serve one cohesive purpose (e.g. ). the test is: "who asks for a change, and how many different stakeholders would touch this file?" open/closed principle software entities should be open for extension, closed for modification — you add new behaviour by adding new code, not editing tested code. this is achieved through abstraction: define an interface, and let new behaviour arrive as new implementations. smell: a / chain on a type string or enum that grows every sprint. how di enables it: dependency injection is the delivery mechanism — receives an through its constructor rather than constructing it, so the set of available p',"lld/uml-for-interviews":`you will never be asked to reproduce full uml from a textbook in an interview — you'll be asked to sketch a class diagram on a whiteboard or in a shared doc while you talk. the skill being tested is picking the right small diagram fast and using the correct arrowhead, not academic completeness. the uml diagram family uml splits into structural diagrams (what exists) and behavioural diagrams (what happens). you'll use two of these routinely in an lld round and the rest only when asked; recognising all of them by sight is still worth having. diagram category shows example class structural classes, attributes, methods, relationships {height=120px} object structural a snapshot of actual instances and their links at one moment {height=120px} component / deployment structural how modules or services are packaged and deployed — rarely hand drawn in an lld round use case behavioural actors and the goals they achieve with the system {height=120px} sequence behavioural messages between objects, in time order {height=120px} activity behavioural workflow steps, branches, and parallel paths {height=120px} state behavioural an entity's lifecycle and legal transitions {height=120px} [!key] you need class diagrams cold, sequence diagrams for "walk me through the flow", and state diagrams whenever an entity has real lifecycle rules — those three cover the overwhelming majority of an lld round. component, deployment, and object diagrams matter more for system design than for a single class model. class diagrams: notation a class box has three compartments: name, attributes, methods. visibili`,"lld/creational-patterns":`creational patterns exist to answer one question: "how does this object get built, and does the caller need to know?" interviewers use them to check whether you reach for the right amount of ceremony — a simple factory when that's enough, a builder when a constructor would be unreadable, and a healthy suspicion of singleton. factory method problem: a class needs to create an object, but the exact type to create should be decided by a subclass or a configuration value, not hardcoded. real world use: style code, orm connection creation based on a config driven provider name, notification channel selection. when not to use it: if there's only ever one concrete type and no plausible second one on the roadmap — a factory around a single implementation is needless indirection. abstract factory problem: you need to create families of related objects that must stay consistent with each other — e.g. a ui toolkit where buttons and checkboxes must all match one visual theme. real world use: cross platform ui toolkits, database provider abstraction layers that must produce a matched / / set. when not to use it: when there's only one product to create (that's just factory method) or the "families" never actually need to vary together — added complexity for a guarantee nobody needs. [!key] factory method vs abstract factory vs a simple factory, clarified: a simple factory is one static method with a switch — not a gof pattern, just a convenience. factory method defines a virtual creation method that subclasses override to decide one product's type. abstract factory is a factory of factor`,"lld/structural-patterns":"structural patterns are about composing classes and objects into larger structures without making that structure fragile. adapter, decorator and proxy look nearly identical on paper — same shape, one wrapped object behind one interface — so the interview test is whether you can state the intent difference, not just draw the diagram. adapter problem: you have an existing class with an incompatible interface (often third party or legacy) and need it to work where a different interface is expected, without modifying its source. real world example: wrapping a legacy soap client or an older sdk behind your application's modern / interface so the rest of the codebase never sees the legacy shape. .net example: adapting a raw (byte oriented) into a text oriented reading api. decorator problem: you need to add optional, combinable behaviour to an object at runtime without subclassing for every combination of features. real world example: asp.net core chains on — each handler wraps the next, adding retry, logging, or auth headers before forwarding the request, all implementing the same handler contract. .net example: wrapping — wraps a , which could itself be wrapped in a , each adding behaviour while remaining a . facade problem: a subsystem has many interacting classes with a complex protocol between them, and most callers only need a simple, high level operation. real world example: a in an e commerce backend that hides inventory reservation, payment charging, and shipping scheduling behind one call. .net example: itself is a facade over the much lower level /socket/dns machinery ","lld/behavioural-patterns-part-1":`behavioural patterns are about who does what and when — they distribute responsibility for a task across objects instead of hard coding it into one giant method. this page covers the four you will actually write in an interview: strategy (swap an algorithm), observer (notify many listeners), command (turn a request into an object), and chain of responsibility (pass a request along a pipeline until someone handles it). strategy pattern problem: a class needs one of several interchangeable algorithms — pricing rules, payment methods, compression formats — and today that logic lives in a growing or inside the class itself. every new variant means editing code that already works, which violates the open/closed principle and risks regressions in unrelated branches. strategy pulls the algorithm out into its own interface. the context class holds a reference to that interface and delegates to it; it never knows which concrete implementation it is running. real world usage: payment gateways ( , , behind ), file compression ( vs ), tax calculators that vary by region, and sorting implementations passed into . anywhere you see "the algorithm varies but the caller shouldn't care which one", reach for strategy. [!key] strategy is composition over inheritance : instead of subclassing for every pricing rule, you inject the behaviour. the client (or a factory) decides which strategy to use. strategy vs state both patterns look identical in uml — a context holding an interface reference — which is why interviewers ask you to tell them apart. strategy state who picks the implementation the `,"lld/behavioural-patterns-part-2":`part 1 covered strategy, observer, command and chain of responsibility. this page finishes the behavioural family: state (an object that transitions through a lifecycle), template method (a fixed algorithm skeleton with pluggable steps), iterator (uniform traversal), mediator (centralised coordination between objects), memento (snapshot and restore), and a brief look at visitor and interpreter, which show up far less often but are worth recognising by name. state pattern problem: an object's allowed behaviour depends entirely on what phase of its lifecycle it's in — an order can be shipped only if it's paid, a vending machine can dispense only after payment — and that logic is currently a block duplicated across every method that touches the object. every new status means editing several methods, and it's easy to miss one. state gives each phase its own class implementing a shared interface; the object holds a reference to its current state object and delegates behaviour to it. transitions happen by swapping which state object is currently held — often the state itself decides the next state. compare this to the alternative every candidate reaches for first: the version puts all lifecycle knowledge in one method that keeps growing; the state version puts each phase's rules in its own class, so adding "refunded" is a new file, not an edit to , , and every other method that happened to check . [!key] state removes a giant by giving each branch its own class. the tell in an interview: "the object's behaviour changes as a direct result of what happened to it before" — that's a `,"lld/design-patterns-cheatsheet":`you will not have time to reason a pattern up from first principles in the middle of a round — you need instant recall of what each pattern is for , then a sharp sentence justifying it. this page is a dense reference: all 23 gang of four patterns with a one line intent, a real .net/production example, and a "use when" trigger, followed by a symptom to pattern lookup you can scan under pressure. creational patterns pattern intent in one line real .net/production example use when factory method delegate object creation to a method subclasses can override the exact type to construct depends on a condition or subclass abstract factory produce families of related objects without naming concrete types (sql/postgres/sqlite families) you need a whole family of related objects to stay consistent (all sql or all postgres) builder construct a complex object step by step, same process, different representations , , ef core's construction has many optional parameters or must happen in steps prototype create new objects by cloning an existing instance , creating an object from scratch is expensive; copying a template is cheap singleton ensure exactly one instance, globally accessible reused per app, di container's singleton lifetime exactly one instance must coordinate shared state (config, connection pool) structural patterns pattern intent in one line real .net/production example use when adapter convert one interface into another the client expects wrapping a third party sdk behind your own you must integrate a class whose interface you can't change bridge decouple an abstraction from`,"lld/thread-safety-in-design":`thread safety questions in lld rounds are rarely "implement a mutex" — they're "look at this class diagram and tell me where it breaks under concurrent access." every concurrency problem in a design round reduces to one of three shapes: state getting corrupted under concurrent access, work needing coordination between producers and consumers, or demand exceeding a scarce resource. this page covers all three, spending the most time on correctness since it's what interviewers probe deepest, using a booking system as the running example. three shapes of concurrency problems shape what breaks default fix typical examples correctness shared state is read and written concurrently, corrupting an invariant lock the critical section, or make the state immutable seat booking, bank balances, inventory counts coordination threads need ordering, handoff, or to wait on each other a bounded queue between producer and consumer background jobs, bursty request handling, async email/notifications scarcity demand exceeds a limited resource (connections, memory, external api quota) a semaphore (permits) or a resource pool (reusable objects) connection pools, rate limited api clients, worker capacity [!key] when an interviewer describes a system, classify the problem first: is state being corrupted, is work waiting on other work, or is a resource running out? that single question narrows the entire toolbox to one row of this table. shared mutable state is the root cause every concurrency bug traces back to one thing: two or more threads reading and writing the same mutable memory without coordin`,"lld/extensibility-and-testability":`extensibility and testability are the same underlying skill wearing two hats: both come from not hard wiring decisions that vary . a class that's easy to extend without editing is almost always also easy to unit test in isolation, because both properties come from depending on interfaces instead of concrete, hard coded collaborators. designing for change identifying axes of variation before adding any abstraction, name what's actually likely to vary: payment method, notification channel, pricing rule, storage backend, region specific tax logic. an axis of variation is something the business changes, not something you imagine might change — resist adding an interface for a dimension nobody has asked to vary yet. axis of variation interface to introduce concrete implementations how a fee is calculated , , where a notification is sent , , where data is persisted , what counts as "now" , (for tests) programming to interfaces, not implementations the dependency should be declared as the abstraction the caller needs, not the concrete class that happens to provide it today. the second version can add sms or push notifications as new classes with zero changes to , and a unit test can inject a fake that just records calls instead of actually sending email. open/closed in practice "open for extension, closed for modification" sounds abstract until you have a concrete rule: if adding a requirement means editing a class that already works and is already tested, the design isn't open . adding should mean writing a new class, not adding a branch to an existing method that every other pri`,"lld/lld-interview-framework":`most candidates who "know" design patterns still fail lld rounds because they jump straight to code with no shared understanding of scope. interviewers are grading your process as much as your final class diagram — the goal every round is the same: clean, maintainable code that visibly applies oop, solid, and design patterns, arrived at through a process the interviewer can follow. this page gives a repeatable structure for a 45–60 minute round, a time budget, a reusable class skeleton for the coding portion, and what to do when the interviewer throws a curveball requirement halfway through. the shape of the round every stage produces something the interviewer can see — a list, a diagram, a spoken walkthrough. silence while you think is the single biggest score killer; narrate constantly, even "i'm considering whether payment should be an entity or a value object" is worth more than silence followed by a diagram. time budget for a 45–60 minute round phase time (45 min round) time (60 min round) output clarify requirements & scope 5 min 7 min written list of in scope/out of scope features actors & use cases 3 min 5 min 3–6 use cases, one sentence each nouns → classes, verbs → behaviours 3 min 4 min rough candidate class list core entities & value objects 5 min 7 min named classes with 2–4 key fields each interfaces & responsibilities 5 min 6 min interfaces for anything that varies relationships (class diagram) 5 min 7 min drawn or described class diagram walk a use case through the model 5 min 6 min verbal trace, catches missing methods extensibility / concurrency / errors 4`,"lld-problems/parking-lot":"a parking lot is the classic low level design warm up: it looks trivial but hides a real type hierarchy, a pluggable pricing engine, and a genuine race condition the moment two cars go for the last free spot at once. requirements functional support multiple vehicle types (motorcycle, car, truck/bus) and matching spot types. on entry, automatically find and assign a compatible spot and issue a ticket recording spot, vehicle type and entry time. on exit, validate the ticket, compute the fee from time parked, free the spot, and invalidate the ticket. reject entry when no compatible spot is free; reject exit on an unknown, expired or already used ticket. support multiple floors, and a display board showing free spot counts per type per floor. non functional and assumptions payment processing and physical gate hardware are out of scope — assume they call into this system as a black box. peak load means many simultaneous entries at multiple gates; spot assignment must not double book a spot. pricing must be configurable per vehicle type and, later, per time of day, without touching the core allocation logic. single process, in memory design for the interview; the strategy boundaries should make sharding across processes later a config change, not a rewrite. clarifying questions to ask which vehicle types and spot types do we need — is a car allowed to occupy a truck spot? is pricing flat, hourly, or does it vary by vehicle type or duration? do we need multi floor support and a live display board in scope? what happens on a lost ticket — is there a manual override/admin flow? is p","lld-problems/elevator-system":'an elevator system is a scheduling problem wearing a state machine costume. moving one car up and down is trivial; the interview worthy part is deciding which of several cars answers a hall call, and how a car reorders its stops without starving a request forever. requirements functional accept external hall calls : a floor button press with a direction (up or down), before anyone is inside a car. accept internal car calls : a destination floor pressed from inside a car, with no direction. given multiple elevators, decide which one answers each hall call. advance all elevators through discrete time steps ( / ), each servicing its own queue of stops. reject invalid floor numbers; treat a call for the current floor as already served. support taking a car out of service (maintenance/emergency) and reassigning its pending calls. non functional and assumptions floor count and elevator count are configuration, not hardcoded constants. door timing, weight limits and sensor hardware are abstracted away — assume the controller is told "car arrived" and "call placed" as discrete events. no request should starve: a car committed to a direction should not reverse until it has serviced every stop in that direction. the dispatch algorithm must be swappable without touching how a single elevator manages its own queue. clarifying questions to ask how many floors and elevators, and are they fixed or configurable at runtime? are hall calls directional (separate up/down buttons), or just "an elevator is wanted here"? is this a tick based simulation, or should it react to real asynchronous eve',"lld-problems/vending-machine":"a vending machine is the textbook case for the state pattern: the same button press means something different depending on whether the machine is idle, holding money, or mid dispense, and a hand rolled switch statement tracking that combination rots fast. requirements functional let a user select a product, then insert money (coins and notes) toward its price. dispense the product and any change once the inserted amount covers the price. support cancelling mid transaction with a full refund of inserted money. reject selection of an out of stock product and surface that clearly. support an admin restock operation that adds inventory without disrupting an active transaction. non functional and assumptions one physical transaction is active at a time — the machine serializes the whole select→insert→dispense flow. the machine holds a bank of coins for change and must refuse to dispense if it cannot make correct change. restocking can happen concurrently (an admin refilling a live machine) and must not corrupt inventory counts mid vend. payment is coins/notes for this design; card/contactless should be addable without touching the state machine. clarifying questions to ask what payment methods are in scope — coins and notes only, or card too? can the user insert money before selecting a product, or only after? what happens if the machine cannot make exact change for the selected product? should cancelling refund exact inserted coins, or just the total value? is admin restock part of this design, or assumed to happen offline? do we need to support multiple simultaneous machines s","lld-problems/atm":"an atm combines three patterns in one machine: a state pattern for the session lifecycle from card insert to card eject, a command pattern for transaction types, and a hard boundary to an external account service that must never be trusted to always succeed. requirements functional authenticate a card via pin, locking/retaining the card after a fixed number of failed attempts. support withdraw, deposit, balance inquiry and transfer as distinct transaction types. dispense cash using available denominations, refusing the withdrawal if it cannot be made exactly. call out to an external account/core banking service to check balance and post debits/credits. log every transaction attempt and outcome for audit and dispute resolution. abstract hardware (card reader, cash dispenser, receipt printer) behind interfaces for simulation and testing. non functional and assumptions a single physical machine serves one card session at a time; there is no concurrent multi card session to reason about locally. the core banking call may be slow, time out, or fail independently of whether cash was dispensed — every call must be idempotent via a transaction/idempotency key. physical cash inventory per denomination is finite and must be tracked precisely; running low on one denomination should not block withdrawals that another mix of notes could satisfy. a crash or jam mid dispense must never leave the customer's account debited with no cash delivered, or vice versa. clarifying questions to ask which transaction types are in scope — withdraw, deposit, balance inquiry, transfer, all of them? is c","lld-problems/package-locker":"a package locker is a self service pickup system: a carrier deposits a parcel into a free compartment, the system mints a one time access code, and the customer later keys that code in to reclaim it. it looks trivial until you have to reason about expiry, staff overrides and two drivers racing for the last medium slot at the same instant. requirements functional a carrier deposits a package by declaring a size (small, medium, large); the system finds a free compartment of that size, opens it, and returns a single use access code, or a clear error if none is free. a customer retrieves a package by presenting the access code; the system validates it, opens the matching compartment, and invalidates the code so it cannot be reused. access codes expire after a fixed window (7 days). an expired code must be rejected with a specific, distinct error — not treated the same as an unknown code. staff can trigger a sweep that opens every compartment whose code has expired, so they can manually remove and return stale packages to the sender. every rejection path — wrong code, already used code, expired code, no compartment of the requested size — surfaces a distinct, actionable error message. non functional and assumptions delivery logistics (how the package physically arrives) and notification delivery (sms/email of the code to the customer) are out of scope — assume external systems call into this one. single locker bank, single process, in memory state for the interview; nothing in the design should prevent adding more banks later. compartments are a scarce physical resource: two car","lld-problems/tic-tac-toe-and-chess":`board games are the classic "start simple, then generalize" interview arc: design tic tac toe cleanly enough — a generic board, pluggable win/move rules — and the follow ups "now make it chess" and "now make it connect four" become extension exercises instead of rewrites. requirements functional a generic board of configurable dimensions that every game shares. tic tac toe: two players alternate placing x/o on a 3×3 grid; detect a win (row, column or diagonal) or a draw after every move, in o(1) per move rather than rescanning the board. chess: 8×8 board, full piece hierarchy (pawn, rook, knight, bishop, queen, king) each with its own movement rule; alternating turns; detect check, checkmate and stalemate; support undoing the last move. connect four: 7 column, 6 row grid; players drop discs that fall to the lowest empty row in the chosen column (gravity) instead of placing on any empty cell; detect four in a row (horizontal, vertical or diagonal) or a full board draw. reject illegal moves in every game: occupied cell (or full column, for connect four), wrong turn, out of bounds, moving after the game is over, or (for chess) a move that leaves the mover's own king in check. non functional and assumptions single local two player game per instance; no networking or matchmaking in this scope. chess legality is computed as "pseudo legal moves per piece rule, filtered to those that don't leave your own king in check" — full fide rules (castling, en passant, threefold repetition) are treated as extensions, not core scope, unless the interviewer says otherwise. undo must be efficie`,"lld-problems/library-management":"a library system's whole interview value hinges on one distinction candidates routinely miss: a is a catalogue entry — title, author, isbn — while a is one physical, barcoded copy of it. conflate the two and checkout, reservations and per copy state all break. requirements functional search the catalogue by title, author, isbn or subject. track member accounts, each with a lending limit and a list of active loans. check out and return a specific physical copy, tracking due dates and accruing fines on late return. queue reservations when every copy of a title is checked out, and notify the next member in line when a copy becomes available. support looking up a copy by barcode/isbn, and admin operations to add new titles or retire damaged copies. non functional and assumptions single branch for the core design; the catalogue vs copy split should make multi branch an additive change, not a rewrite. two members must never be able to check out the same physical copy at once. fines accrue daily past the due date, typically capped at some maximum — flat rate schemes should also be supportable without redesigning the loan model. notification (email/sms/push) is abstracted behind an interface; the actual delivery channel is out of scope. clarifying questions to ask is this a single branch, or multiple branches sharing one catalogue with independent inventories? do lending limits and fine rates vary by member type (student vs faculty vs guest)? should reservations be strict fifo, or can priority holds exist (e.g., faculty jump the queue)? is full text search in scope, or exact/prefix","lld-problems/question-and-answer-site":`a question and answer site needs a content model for questions, answers and comments, a fair voting and reputation system, and search over all of it — the real design work is keeping voting, reputation and search each isolated so tightening one rule never risks breaking another. requirements functional users post questions (with tags), post answers to questions, and comment on both questions and answers. users vote (upvote/downvote) on questions and answers; a user cannot vote on their own post, and each user gets exactly one active vote per post. the question's author can mark exactly one answer as accepted. users search by keyword (title/body), by tag, or by author. reputation is a derived score, updated automatically from vote outcomes and accepted answers — never set directly by a caller. non functional and assumptions single process, in memory storage for the interview; the design should not preclude swapping any one piece (search, storage) later. many users vote and post concurrently — the system must never double count a vote or lose a comment under concurrent writes. reputation changes are a side effect of voting/acceptance, not a separate api surface a client calls directly. moderation, edit history, spam detection and notification delivery are out of scope for this pass. clarifying questions to ask [!tip] ask "can a user change or retract a vote?" early — it decides whether votes are an append only log or a keyed, replaceable map, which changes both the data structure and the reputation math (a retraction has to reverse the exact delta it applied). can a vote be c`,"lld-problems/music-streaming-service":'a music streaming design is really two smaller problems wearing one trenchcoat: a catalog/playlist content model, and a playback engine whose queue must support shuffle, repeat and "play next" without turning into a wall of conditionals. the state management version — no actual audio bytes — is what interviewers ask, and the queue/player split is the whole game. requirements functional a catalog of songs (title, artist, album, duration); search by title, artist or album, case insensitive substring match. users create playlists: add, remove, and reorder songs. player operations: play (a playlist, album, or single song), pause, resume, next, previous, seek, stop. shuffle on/off — a new random order each pass, without interrupting the currently playing song. repeat modes: (stop at the end), , . "play next" inserts a song ahead of the normal queue, consumed before the queue resumes. playback state changes (song changed, state changed) notify subscribers — ui, scrobbler, history — without the player knowing who they are. track recently played songs per user. non functional and assumptions this is a state management design — audio decoding, streaming, and buffering are explicitly out of scope. one active playback session per user for this pass; multi device sync is discussed as an extension. recommendations, radio, offline downloads and drm are out of scope. the catalog can be large; search must not scan every song per keystroke. clarifying questions to ask [!tip] ask "does shuffle reshuffle every loop, or shuffle once and repeat that order?" before writing . the answer decides w',"lld-problems/movie-ticket-booking":"movie ticket booking is the interview problem where concurrency isn't a bonus round — it's the whole point. everything else in the design (catalogue browsing, pricing, cancellation) exists in service of answering one question: how do you stop two people from paying for the same seat. requirements functional model cinema → screen → show (a movie at a specific time on a specific screen) → seat. let users search for movies by title (simple substring match) and browse shows by movie, cinema or time, viewing a seat map for a chosen show. let a user select seats and place a temporary hold on them while completing payment. confirm the booking on successful payment; release held seats back to available on payment failure or hold expiry. support cancelling a confirmed booking with a refund, releasing the seats. price seats by class (standard, premium, recliner) and potentially by showtime. non functional and assumptions exactly one booking may succeed per seat per show, even under many simultaneous attempts. a seat hold is short lived (5–10 minutes) and must auto expire back to available if payment never completes. payment is an external, untrusted boundary — it can succeed, fail, or time out independently of seat state. contention on one popular show must not slow down bookings for unrelated shows. clarifying questions to ask do users pick specific seats, or does the system auto assign best available? is movie search full text/fuzzy, or is simple substring matching on title enough? how long should a seat hold last before it expires back to available? does every seat in a show cost ","lld-problems/shopping-cart":"a shopping cart looks like crud on a list until the pricing pipeline shows up: stacking discounts, tax and currency all have to compose predictably, inventory can't be reserved too early or too late, and checkout has to survive a double click or a network retry without charging twice. requirements functional add, remove and update the quantity of line items in a cart. compute a price via a pipeline: subtotal → discounts → tax → total. support multiple discount rules with a defined precedence for stacking vs. mutual exclusion. reserve inventory at checkout time, not the moment an item is added to the cart. merge a guest cart into a logged in user's cart on login. persist the cart with an expiry so abandoned carts are cleaned up. make checkout idempotent — a retried checkout request must never double charge or double reserve stock. non functional and assumptions currency and tax rules vary by region and must be pluggable, not hardcoded. the same cart may be edited from multiple devices/tabs for one user; concurrent edits should not silently drop an item. checkout calls external payment and inventory services that must be treated as untrusted boundaries, exactly like the atm's account service. cart data has a ttl — an abandoned cart is not kept forever. clarifying questions to ask can discounts stack (e.g., 10% off and free shipping), or are some mutually exclusive? is inventory reserved when an item is added to the cart, or only at checkout? do we need guest cart support with merge on login? what's the expected ttl / abandoned cart cleanup behaviour? is multi currency and reg","lld-problems/order-matching-engine":`an order matching engine question is really a data structure question wearing a finance costume — the entire design lives or dies on choosing the right structure for "give me the best price, in arrival order, fast" under heavy concurrent load. requirements functional supports and , and , for a given symbol. each symbol has its own independent order book with separate buy (bid) and sell (ask) sides. matching uses price time priority : best price first, ties broken by earliest arrival (fifo). a matches a resting when ; the execution price is the resting (maker) order's price, not the incoming (taker) order's price. partial fills are supported — an unfilled remainder of a order rests in the book; an unfilled order remainder is cancelled, never rests. removes a resting order; every match publishes a to subscribers. modifying an order (price or quantity) is implemented as cancel then replace, not an in place update — the replacement order gets a new timestamp and forfeits the original's time priority. non functional and assumptions many symbols, each with an independent book — aapl and tsla orders must never contend for the same lock. matching must be deterministic and reproducible for the same sequence of inputs (important for audit and replay). settlement, clearing, margin, and network/fix protocol are explicitly out of scope. cancellation should ideally be o(1); with real order flow, cancels vastly outnumber fills. clarifying questions to ask [!tip] ask about the threading model early — "one thread per symbol, or shared locking?" — because the answer changes the entire concur`,"lld-problems/ride-hailing-service":'a ride hailing system chains three orderly problems: match a rider to a nearby available driver, walk that ride through a strict lifecycle, and price it. the same "find the nearest available resource, hand it a payload, track it to completion" machinery underneath generalizes well past rides — the local delivery variant at the end of this page reuses it almost unchanged. requirements functional a rider requests a ride with ; the system matches the nearest available driver of that ride type within a max radius. if no driver is found within radius, the request fails with a clear, specific reason. ride lifecycle: ; any pre start state can move to . invalid transitions are rejected outright — you cannot complete a ride that never started, or re assign a completed one. fare = , floored at a minimum fare, then multiplied by a surge factor; each ride type has its own pricing configuration. a driver becomes available again the instant a ride completes or is cancelled. non functional and assumptions single process, in memory design for the interview; straight line distance is an acceptable stand in for real routing/eta. payment gateway integration, driver ratings, and a real pooling algorithm are explicitly out of scope — fare calculation only. assume single threaded matching as a starting point, then be ready to defend it under concurrent load. persistence and distributed matching across regions are named as future work, not built here. clarifying questions to ask [!tip] ask "how do we find nearby drivers — a real geo index or simple distance?" early. answering "simple straight lin',"lld-problems/lru-and-lfu-cache":'an lru cache is the single most common data structure design question because it forces you to combine two structures to beat the o(n) cost of either one alone. lfu is the natural follow up that tests whether you can generalise the same skeleton to a different eviction rule. requirements functional returns the value or a miss signal, and counts as a "use" of that key. inserts or updates; if the cache is full, evict according to policy before inserting. both operations must be o(1) average time. lru evicts the least recently used key; lfu evicts the least frequently used key, breaking ties by recency. non functional and assumptions fixed capacity, set at construction, does not grow dynamically. single process, in memory — no persistence, no cross node replication in the base design. reads and writes may come from multiple threads; correctness under concurrency is a first class requirement, not an afterthought. values are reasonably small; we are bounding by entry count first, with memory size bounding as a named extension. clarifying questions to ask [!tip] asking these up front is what separates a candidate who "knows lru" from one who can design a cache. it also lets you scope the 30 minutes you have. is the eviction policy fixed, or should it be swappable (lru vs lfu vs ttl) without rewriting the cache? do entries need to expire on a timer (ttl) independent of eviction? is thread safety required, and what is the concurrency level — light contention or a hot path from hundreds of threads? should capacity be counted in number of entries or bytes of memory ? is this cache lo',"lld-problems/rate-limiter":"a rate limiter question is really two questions in one interview: can you name and implement the classic throttling algorithms, and can you design a class around them that stays clean when a sixth algorithm shows up next sprint. requirements functional returns whether a request for (e.g. a client id or api key) is allowed right now. support at least token bucket, leaky bucket, fixed window and sliding window algorithms, selectable per endpoint. return enough information to build a response: allowed/denied, remaining quota, and a retry after hint. each key (client, endpoint, or client+endpoint pair) is limited independently. configuration (which algorithm and its parameters, per endpoint) is loaded once at startup; an endpoint with no explicit entry falls back to a default limiter rather than being rejected outright. non functional and assumptions single process, in memory state, loaded from configuration at startup — no external coordination service in the base design. must be thread safe: many request handling threads call concurrently. memory must not grow unbounded as new client keys appear; idle keys need to be reclaimed. must be testable without real wall clock sleeps, which means the clock is a dependency, not a static call. clarifying questions to ask [!tip] a senior answer starts here, not with code: rate limiting has at least four well known algorithms with different trade offs, and picking one before understanding the burst tolerance requirement is a mistake worth avoiding out loud. is bursty traffic acceptable (token bucket) or must the rate be perfectly smooth (","lld-problems/logging-framework":`every backend engineer has used a logging framework, which makes this lld question deceptively easy to start and easy to get shallow on — the interesting parts are async delivery, backpressure, and what happens when a sink goes down. requirements functional records a structured entry with a timestamp, level, message and key value properties. multiple output sinks simultaneously: console, file, network (e.g. shipping to a log aggregator). a configurable minimum level per logger or per sink (e.g. console at , file at ). structured logging: attach arbitrary properties, plus ambient context (correlation id, request scope) that flows automatically without being repeated at every call site. non functional and assumptions logging must never block the calling thread on i/o — the hot path is returning immediately. thread safe: many application threads log concurrently. bounded memory: a burst of log volume must not grow the process's memory without limit. must degrade gracefully if a sink is slow or unavailable — one broken sink must not stop others or crash the app. extensible to new sinks and formats without changing the core class. clarifying questions to ask [!tip] naming the producer consumer shape early — "logging is a producer consumer problem, the app thread produces, a background thread consumes and writes" — signals you already know where this design is going. should sinks share one minimum level, or can each sink have its own threshold? is exactly once delivery required, or is "best effort, may drop under extreme load" acceptable? do we need structured properties (key val`,"lld-problems/task-scheduler":'a task scheduler question tests whether you can build a correct producer consumer system around time itself — jobs are not "ready" until a clock says so, and the queue has to wake workers at exactly the right moment without busy spinning. requirements functional , , , and cron style recurring schedules. a fixed worker pool executes jobs at or after their due time — never early. prevents future runs; a run already in flight is allowed to finish. a job that throws is logged and does not kill its worker or its future schedule. non functional and assumptions single process, in memory — persistence and distributed coordination are named extensions, not core requirements. thread safe under concurrent / calls from many callers while workers are executing jobs. the same job instance must never run concurrently with itself, even if one execution overruns its next scheduled time. stops accepting new jobs and drains in flight work cleanly. clarifying questions to ask [!tip] ask about self overlap early — "if a recurring job takes longer than its interval, does the next run wait, skip, or run in parallel?" — because the answer changes whether you need a per job "is running" flag at all. how many schedule types are needed: one time delay, fixed rate, fixed delay, full cron expressions? what is the worker pool size, and is it fixed or does it need to scale with load? what is the "missed run" policy if the scheduler was busy or paused — fire immediately, skip to the next slot, or fire once and catch up? does cancellation need to interrupt a running job, or only prevent future runs? is exa',"lld-problems/rule-engine":`a rule engine question is fundamentally about representing arbitrarily nested logic as a data structure instead of code, so business rules can change in configuration without a deployment. requirements functional load rules from configuration: a name, a priority, a condition tree, and an action. conditions compose: comparison leaves ( , , , , ...) combined via / / , nested to any depth. facts are addressed by dot path ( ) against a nested structure. evaluate rules in descending priority order; support both (stop after one) and modes. a missing fact makes its condition evaluate to rather than throwing. report which rules matched and why, for debugging. non functional and assumptions single pass evaluation — no forward chaining (an action does not re trigger evaluation) in the base design. rules are loaded at startup but must support a hot reload path without restarting the process. evaluation must be fast enough to run per request against dozens to thousands of rules. adding a new operator or action type must not require touching the evaluation engine's core loop. clarifying questions to ask [!tip] the best opening line for this problem: "a condition is a tree, and every node answers the same question — evaluate(facts) → bool. that's what lets nesting be unlimited with zero special casing." say this before writing any code. do all matching rules fire, or only the highest priority match? can an action's effect feed back into evaluation (forward chaining), or is it strictly single pass? what happens when two rules of equal priority both match and their actions conflict? are ru`,"lld-problems/notification-service":'a notification service question tests whether you can fan one event out to many channels and many users asynchronously, while still surviving a flaky sms provider without losing or duplicating messages. requirements functional publishers emit events ( ); the service fans out to every user subscribed to that topic, across every channel they opted into (email, sms, push, in app). is fire and forget — it must return immediately, before any delivery happens. each channel renders its own template (sms short, email rich html, push a short title/body). failed deliveries retry with backoff up to a configured limit, then move to a dead letter store. delivery status callbacks (sent, failed, bounced) are observable by other parts of the system (analytics, support tooling). non functional and assumptions in memory queue for the base design, explicitly built to be swappable for kafka/sqs later. at least once delivery is acceptable, provided sends are idempotent — a retried send must not become a duplicate notification to the user. one slow or down channel provider must not delay or block delivery on other channels. user preferences (opted in channels, quiet hours, do not disturb) must be checked before a message is queued, not after. clarifying questions to ask [!tip] ask "is publish synchronous or fire and forget" first — the entire architecture (queue, workers, retry) only exists because the answer is fire and forget. if it were synchronous, this would just be a fan out loop with no scheduling concerns at all. is delivery ordering required, or is best effort acceptable across channels',"lld-problems/authentication-service":'an authentication service question is really a security decisions question wearing an oop costume: the class diagram is almost incidental, and what interviewers actually listen for is whether you store passwords safely, make sessions genuinely revocable, and treat "mfa required" as a normal outcome rather than an exception. requirements functional rejects duplicate emails and weak passwords; passwords are stored as salt plus a slow hash, never reversible plaintext. returns a session token with an expiry; login methods are pluggable — password today, oauth providers later, with no change to the flow. if mfa is enabled for the user, login returns a pending challenge instead of a token; a valid totp code completes it. returns the user, or fails if the token is expired or revoked. revokes one session; revokes every session for that user. the account locks for n minutes after k consecutive failed logins. password reset uses a single use, time bound token. performs a basic role check. non functional and assumptions opaque, server side session tokens rather than jwts — simpler to revoke instantly, at the cost of a store lookup per request. real oauth handshakes with providers, a distributed session store, device fingerprinting/risk scoring, and email/sms delivery mechanics are all out of scope. single process, in memory storage for the interview; nothing should prevent swapping the session store for redis later without touching the flow. multiple concurrent sessions per user are allowed and must be individually revocable. clarifying questions to ask [!tip] ask "session tokens or j',"messaging/queues-topics-and-streams":'"messaging" is not one thing. a competing consumer queue, a publish subscribe topic and a partitioned append only log solve different problems, and interviewers use this question to check whether you actually understand the systems you have operated or just know the product names. the three consumption models every messaging technology is fundamentally one of these three shapes, sometimes with a second shape layered on top (service bus topics are pub sub built out of per subscription queues; kafka is a log that can emulate a queue with a single partition topic). property competing consumer queue publish subscribe topic partitioned append only log consumption model one consumer per message, load shared every subscriber gets its own copy every consumer group gets its own copy, ordered by offset retention message deleted on ack (or ttl) deleted per subscription on ack time/size based, independent of consumption replay not possible once acked not possible once acked (per subscription) first class — rewind offset, re read history ordering fifo only with sessions/message groups fifo only within a session, per subscription strict within a partition, none across partitions scaling unit add consumers up to prefetch limits add subscriptions (fan out), consumers per subscription add partitions; consumers per group ≤ partitions fan out none — one logical consumer group native — n subscribers, n independent reads native — n consumer groups, independent offsets typical products azure service bus queue, sqs, rabbitmq queue service bus topics, sns+sqs, rabbitmq exchange kafka, event hubs, ',"messaging/delivery-semantics":`every messaging interview eventually asks "does your system guarantee exactly once delivery?" the correct senior answer is that delivery cannot be exactly once over an unreliable network — only processing can, and only through a specific mechanism you should be able to name. the three delivery guarantees, precisely guarantee definition what can go wrong cost at most once message sent/attempted zero or one times, no retry on failure message loss on any error cheapest, no dedup needed at least once message delivered one or more times until acknowledged duplicate processing requires idempotent consumers exactly once message delivered and processed exactly one time, no loss, no duplicates — (the goal) only achievable at the processing layer, not the network layer [!key] exactly once delivery is impossible to guarantee over a network that can drop, delay or duplicate packets. exactly once processing is achievable — by making the effect of processing idempotent, or by wrapping the read process write in a single atomic transaction. why exactly once delivery is impossible: the two generals problem two generals must coordinate an attack by sending messengers across enemy territory where any messenger might be captured. general a sends "attack at dawn." did it arrive? a can't know unless b sends an acknowledgement — but that ack can also be lost, so b can't know if a got the ack, and so on forever. there is no number of messages that produces mutual certainty. this maps directly onto producer to broker and broker to consumer hops: any ack itself can be lost, so the sender can never d`,"messaging/ordering-and-partitioning":`"does your queue guarantee ordering?" is a trick question — the honest answer is always "ordering within some scope," never globally. this section covers how that scope is chosen, what it costs you, and how to design systems that don't need ordering at all. why global ordering does not scale global ordering requires a single, serial stream that every producer writes to and every consumer reads in lockstep — which caps throughput at whatever one thread/partition/queue can sustain, and turns every consumer into a bottleneck for every other consumer's unrelated work. the moment you want to process more messages in parallel than one strictly serial log can produce, you must give up ordering across the whole system and settle for ordering within a smaller scope: a partition, a session, or a single queue. [!key] ordering and parallelism are in direct tension. the only way to get more parallelism is to shrink the scope within which you promise order — from "everything" to "per partition" to "per entity" to, in the extreme, "no ordering at all." per partition and per session ordering instead of ordering the whole stream, systems order within a key — messages that share a partition key (kafka, event hubs) or session id (service bus sessions) are processed in the order they were produced, but messages with different keys have no ordering relationship and can be processed fully in parallel. the choice of key is the whole design decision: pick the entity whose event history must be strictly ordered — usually the aggregate/entity id (customer id, order id, account id) — and route every `,"messaging/idempotency-and-deduplication":`at least once delivery is the practical default for messaging, which means every consumer you write will eventually receive a duplicate. idempotency is the discipline that makes that survivable, and it's one of the most reliably asked topics for anyone claiming production messaging experience. idempotent operations vs idempotent handlers an idempotent operation produces the same end state no matter how many times it's applied — is idempotent, is not. an idempotent handler is a consumer that produces the same end state no matter how many times the same message is delivered, even if the underlying operation isn't naturally idempotent — it achieves this by explicitly detecting and skipping duplicates. natural idempotency engineered idempotency mechanism operation is mathematically safe to repeat handler tracks "have i seen this message id" example dedup table + unique constraint before charging a card cost free — just write operations this way extra storage, extra write per message works for state setting writes any side effect, including non idempotent ones like sending email [!key] prefer natural idempotency wherever the operation allows it — instead of , upsert instead of insert, "mark as paid" instead of "add to balance." it's free and needs no supporting infrastructure. reach for engineered deduplication only for effects that can't be expressed that way. set vs increment style operations are the single most common source of duplicate processing bugs: a redelivered "add 50 to balance" message silently doubles the credit. whenever you can express the target state instead of`,"messaging/retries-and-backoff":`retrying is the easy part; retrying correctly is what gets tested. this section covers classifying failures, spacing retries so they don't cause the outage they're meant to prevent, and knowing when to stop retrying and dead letter a message instead. transient vs permanent failures the first decision on any failure is whether retrying could possibly help. failure type examples retry? transient network blip, downstream 503, connection pool exhaustion, deadlock victim, throttling (429) yes — likely to succeed shortly permanent malformed payload, missing required field, business rule violation (e.g., insufficient stock), unauthorized no — will fail identically every time ambiguous timeout (did the write happen or not?), downstream 500 with no detail retry cautiously, prefer idempotent operations [!key] retrying a permanent failure doesn't just waste time — it delays dead lettering, hides a real bug behind a wall of retry logs, and, in an ordered partition, blocks every message behind it. classify the exception before deciding to retry, don't retry everything by default. immediate, delayed and scheduled redelivery strategy when mechanism immediate in process retry very short transient blip, cheap operation (e.g., a single db call) retry loop inside the handler, 1 3 attempts, small fixed delay delayed broker redelivery downstream dependency needs recovery time abandon the message; broker redelivers after a lock/visibility timeout, or you explicitly schedule it scheduled future redelivery known recovery time (e.g., "retry after maintenance window ends") (service bus) / delay queu`,"messaging/dead-letter-queues":"a dead letter queue (dlq) is where messages go when the system has given up on normal processing — and how a team treats that queue says more about their operational maturity than almost any other messaging metric. this section covers what lands there, how to triage it safely, and why replay is more dangerous than it looks. the shape is simple even when the failure modes behind it aren't: a producer sends and forgets, a consumer pulls and processes, and only after the nth retry does a message get diverted into the dlq instead of back onto the main queue. what lands in a dlq, and why reason trigger max delivery count exceeded message abandoned/failed n times (service bus default 10) ttl expiry message sat in the queue longer than its time to live without being consumed header/message size exceeded message or its properties exceed the broker's size limit explicit dead letter consumer code calls deliberately (e.g., detected a permanent/business failure) subscription filter evaluation error a sql filter on a topic subscription throws while evaluating a message session lock lost repeatedly (sessions) a session enabled queue can't maintain a lock long enough to process [!key] a dlq message always carries a dead letter reason and description (service bus sets and as system properties). never dead letter without setting these explicitly in your own code — an unexplained dlq message is nearly untriageable months later. monitoring dlq depth as a primary slo signal dlq depth (and its rate of growth) should be a first class alert, not an afterthought — it directly measures messages tha","messaging/consumer-groups-and-scaling":`scaling a messaging consumer is not "add more instances and it goes faster" — beyond a hard ceiling set by partitions or sessions, extra consumers just idle, and settings like prefetch, concurrency and lock duration interact in ways that silently cause message loss if tuned in isolation. competing consumers vs consumer groups a competing consumer pool (a plain queue with multiple readers) has no partition concept — any consumer can pick up any message, and adding consumers scales throughput smoothly up to the point where the queue itself or a downstream resource becomes the bottleneck. a consumer group (kafka, event hubs) is a named set of consumers that jointly own a topic's partitions, with each partition assigned to exactly one consumer in the group at a time — parallelism is capped at the partition count, not open ended. competing consumers (queue) consumer group (log) parallelism ceiling prefetch/concurrency settings, downstream capacity number of partitions adding an instance beyond the ceiling marginal gains shrink, but no instance is fully idle extra instance gets zero partitions — fully idle message ownership any consumer can get any message exactly one consumer per partition, per group rebalancing not a concept — no fixed assignment explicit protocol reassigns partitions when membership changes [!key] the single most commonly missed fact in this space: a kafka/event hubs consumer group can never have more effective parallelism than partitions. scaling a deployment from 10 to 20 pods against a 10 partition topic does not double throughput — it just leaves 10 pods w`,"messaging/backpressure-and-flow-control":`backpressure is what happens when a fast producer meets a slow consumer and something has to give. every messaging system eventually hits this, and how you handle it — bound it, shed it, or let it explode — is one of the fastest ways an interviewer tells a senior engineer from someone who has only read about queues. what backpressure actually is backpressure is a signal that flows backward through a pipeline: "i cannot keep up, slow down." it exists at every layer — tcp has flow control windows, a has a bounded capacity, a database connection pool has a max size. the point is not to prevent overload; it is to make overload visible and controllable instead of silently absorbed by an ever growing buffer. [!key] backpressure is a control signal, not a queue. a queue without a bound is not backpressure — it is a landmine with a delay timer. what happens without it an unbounded queue between a producer and a slow consumer looks fine for the first ten minutes of an incident. then: 1. memory grows unbounded — every enqueued message is held in memory (or disk) until processed. 2. latency grows unbounded — a message enqueued now waits behind everything ahead of it; "freshness" collapses. 3. the process ooms or the host swaps , which slows the consumer further, growing the queue faster — a feedback loop. 4. the failure cascades upstream : the producer's outbound buffer fills, its own callers time out and retry, multiplying load on an already drowning system. [!danger] "the queue will just absorb the spike" is the sentence that precedes most messaging outages. a queue only absorbs a s`,"messaging/outbox-and-inbox-patterns":`any service that writes to its own database and then publishes an event faces a problem with no atomic fix at the infrastructure level: the write and the publish are two separate systems, and either can fail independently. the outbox and inbox patterns are the standard, interview expected answer to this — and one of the most reliable ways to tell if a candidate has actually operated a messaging system in production. the dual write problem, precisely a service commits a database transaction (say, "order created") and then calls a message broker to publish . there is no distributed transaction spanning a relational database and a broker in the general case, so exactly one of these can go wrong independently: failure point result db commit succeeds, publish fails (broker down, network blip) state changed, no event ever sent — downstream systems never learn about the order publish succeeds, db commit fails or rolls back after event sent for a state change that never actually happened process crashes between the two calls either of the above, non deterministically [!key] the dual write problem exists because "write to db" and "publish to broker" are two different systems with no shared transaction coordinator. any solution either makes them share one (2pc), or restructures the problem so only one atomic write is needed (outbox). the transactional outbox the fix: don't publish directly. instead, write the event to an outbox table in the same database, in the same local transaction as the business write. a separate relay process reads the outbox and publishes to the broker, retryi`,"messaging/saga-and-compensations":`a saga is how you get transaction like behaviour across services that each own their own database, without a two phase commit spanning all of them. instead of one atomic commit, a saga is a sequence of local transactions, each with a defined compensating action to undo it if a later step fails. this is one of the most reliably asked distributed systems topics in senior interviews because it forces you to reason about partial failure honestly. why sagas exist a single business operation — "place an order" — often touches inventory, payment, and shipping, each owned by a different service with its own database. a classic acid transaction across all three isn't available (no shared database, and 2pc across services is operationally painful and blocks on the slowest participant). a saga instead runs each step as a local transaction in its own service, and if a later step fails, runs compensating transactions for every step that already succeeded, in reverse. [!key] a saga trades atomicity for availability: nothing is ever locked across services, but the system passes through intermediate, real states that must be designed for, not just assumed away. choreography vs orchestration aspect choreography orchestration control flow each service reacts to events and emits the next one; no central coordinator a central orchestrator explicitly calls each step and decides what happens next coupling loose — services only know event contracts, not each other orchestrator knows about every participant; participants stay simpler visibility hard to see the "whole" saga — it's implicit in event`,"messaging/schema-evolution-and-versioning":"an event, once published, can outlive the code that produced it by years — it might sit in a topic with long retention, be replayed months later, or be read by a consumer that hasn't been redeployed since before the schema changed. schema evolution is about designing events so that producers and consumers can change independently, on their own schedules, without either side breaking the other. why event schemas outlive code a request/response api call and its handler deploy together, roughly, so a breaking change can be coordinated in one release. an event is different: it is written once and may be read by many consumers, deployed at different times, some of which don't exist yet when the event is published, and some of which read it from long retained storage well after the producer has moved on to a newer schema version. the schema is effectively a public contract with the future , not just with today's consumers. [!key] treat every published event schema as permanent. you are not just changing a struct — you are changing a contract that unknown future readers, and possibly a replay of past data, both depend on. compatibility modes, defined mode rule example who can evolve safely backward compatible a new schema can read data written with the old schema consumer upgraded to v2 can still read v1 events producers can be upgraded ahead of consumers forward compatible an old schema can read data written with the new schema consumer still on v1 can read v2 events (ignoring new fields) consumers can be upgraded after producers, or lag behind full compatible both directions hol","messaging/replay-and-reprocessing":`replay is deliberately re feeding events that were already produced — sometimes already consumed — back through a consumer. it is one of the most powerful recovery tools in an event driven system and one of the easiest ways to cause a second incident on top of the first if you don't design for it in advance. when you need replay scenario why replay is the fix a bug in a consumer processed events incorrectly fix the bug, then replay the affected time range to reprocess correctly a new consumer needs historical context it didn't exist when past events were published — replay brings it up to date rebuilding a projection/read model the read model is derived state; rebuild it by replaying the events that produce it a data correction is needed an upstream data error is fixed at the source, and downstream state must reflect the fix [!key] replay only works if the events are still available and consumers can safely process the same event more than once. everything else in this page follows from those two requirements. offsets and retention: the enabler replay is only possible because most brokers don't delete a message the instant it's consumed. kafka retains messages for a configurable period (or forever, with compaction/tiered storage) and tracks consumer offsets separately from the log itself — resetting a consumer group's offset backward causes it to reread already seen messages. sqs, by contrast, deletes a message once acknowledged, so "replay" there means pulling from a separate archive (s3, a data lake sink), not rewinding the queue itself. source system retention model how `,"messaging/designing-a-production-pipeline":`this page pulls together everything from queueing to backpressure to schema evolution into one worked design: a producer publishes to a queue, a consumer processes and writes to a database. it sounds simple until you're asked what happens when the broker goes down at 3am, a message poisons your consumer, or traffic spikes 10x during a sale — which is exactly what a design review interview is testing. the full request path every arrow here is a place something can go wrong, and a senior design answer walks the interviewer through each one deliberately rather than describing only the happy path. what to instrument metric what it tells you why it matters throughput (msgs/sec, in and out) whether the pipeline is keeping pace with demand a sustained gap between in rate and out rate predicts a growing backlog before it's visible elsewhere consumer lag / queue depth how much unprocessed work is backed up early warning of a slow or stalled consumer age of oldest unprocessed message how stale the backlog actually is, in time more honest than depth alone — ties directly to freshness slas processing duration (p50/p95/p99) per message handling cost and tail latency a rising p99 often precedes a lag spike by minutes failure rate (per consumer, per error type) whether errors are transient noise or a real regression distinguishes "retry and move on" from "stop and page someone" dlq depth volume of messages that exhausted retries a non zero, growing dlq means real work is silently not happening [!key] throughput tells you the system is busy. lag, age of oldest, and dlq depth tell you wheth`,"observability/logs-metrics-and-traces":`"production latency suddenly increased — what do you do?" is answered with these three signals in a specific order: metrics tell you something is wrong and roughly where, traces tell you which request path is slow, and logs tell you why . knowing which tool answers which question, and why you cannot fake one with the other, is the foundation the rest of this track builds on. monitoring vs observability monitoring is watching for known unknowns — you predicted a failure mode and built a dashboard or alert for it. observability is being able to answer unknown unknowns — a question you never anticipated, asked live against production data, without shipping new code. [!key] monitoring answers questions you thought of in advance. observability lets you ask a question you didn't think of, right now, and get an answer from the data you already have. a system is "observable" when its internal state can be inferred from its external outputs — logs, metrics, and traces — without attaching a debugger. the three pillars compared logs metrics traces what it answers what exactly happened, with full context is the system healthy, in aggregate, over time where did this one request spend its time cardinality high — free text, any field must be kept low (see below) high per span, but scoped to one trace cost driver storage volume, ingest bytes number of unique time series storage volume, sampling rate retention days to weeks (expensive to keep long) months (cheap once aggregated) days (huge volume, sampled) query shape full text / field search over a window aggregation over time (rate, avg, `,"observability/distributed-tracing":`a single user request in a microservice architecture might touch a gateway, three services, a cache, a database, and a queue. distributed tracing stitches all of that into one timeline so you can answer "where did the 2 extra seconds go" without guessing. it is the tool that turns "latency spiked" into "the payment service call to the fraud check api is slow." traces, spans, and parent child relationships a trace represents one end to end request. a span represents one unit of work within it (an http call, a db query, a function). spans form a tree: each span has a parent, except the root span, and children represent work done because of their parent (a downstream call, a retry, a cache lookup). each span carries: a , its , a shared by the whole tree, start/end timestamps, a status (ok/error), and attributes — key value metadata like , , . field purpose same across every span in the request — the thing you search by unique to this unit of work links to the caller — builds the tree structured metadata for filtering/grouping within a trace timestamped notes within a span (e.g. "retry attempt 2") ok / error, plus an optional message context propagation — the w3c traceparent for the tree to connect across process boundaries, the trace context must travel with the request. the w3c standard header is : over http, middleware reads the incoming , starts a child span, and injects a new into any outgoing request. this is automatic with opentelemetry's asp.net core and instrumentation — you don't hand roll header propagation for synchronous calls. [!warning] the hop people forget is t`,"observability/dashboards-and-alerting":`a dashboard nobody reads and an alert nobody trusts are the two most common failures in observability practice. this page covers the frameworks for deciding what to show (red, use, golden signals) and what to alert on (symptoms, burn rate, multi window), so both are built for the person who gets paged at 3am. red for services, use for resources two complementary checklists cover almost every dashboard you will ever build. red (request driven services) meaning example query shape r ate requests per second e rrors failed requests per second (or ratio) d uration latency distribution (not just average) use (resources — cpu, disk, pool) meaning example u tilisation % time the resource was busy cpu busy %, connection pool in use % s aturation work queued waiting for the resource run queue length, thread pool queue depth e rrors count of resource level errors disk i/o errors, dropped packets red is what you put on a service dashboard; use is what you put on an infrastructure dashboard. a service dashboard with no use style view of its dependencies is blind to "my cpu is fine but my connection pool is saturated." the four golden signals google's sre book generalises red/use into four golden signals for any user facing system: latency, traffic, errors, saturation . it's the same idea from a different angle — latency and errors are red's duration and errors, traffic is red's rate, and saturation borrows directly from use. if you're asked for "the four golden signals" in an interview, this is the expected answer, and pointing out the overlap with red/use shows you understand it's one `,"observability/sli-slo-and-error-budgets":`error budgets turn "reliability" from a vague aspiration into a number a team can plan around. this page covers how to choose an sli that reflects real user experience, how to set an slo without guessing, and how the resulting error budget becomes the tool that decides whether the team ships features or pays down reliability debt this sprint. sli, slo, sla — the vocabulary term definition example sli (indicator) a precise, measured metric of user facing behaviour "proportion of http requests completed in under 300ms" slo (objective) the target value for an sli over a window "99.5% of requests under 300ms, over a rolling 30 days" sla (agreement) a contractual promise, usually looser than the slo, with a penalty for breach "99.0% monthly uptime, or a service credit" [!key] the slo should always be stricter than the sla. the gap between them is your safety margin — you want to notice and fix a problem internally long before you're contractually in breach. choosing an sli that reflects user experience a good sli is measured as close to the user as possible and reflects a dimension they actually care about. the common categories: dimension what it measures example sli availability did the request succeed at all % of requests returning non 5xx latency was it fast enough to feel responsive % of requests under a threshold (not average!) quality was the response actually correct/complete % of search results with zero degraded results served freshness is the data recent enough to be useful % of dashboard reads with data less than 5 minutes old correctness did the system do the right `,"observability/percentiles-and-saturation":`"the average response time is 80ms" can be true while 1 in 50 users waits 4 seconds. percentiles and saturation metrics are how you see past the average to the resource that is actually running out of headroom — and this is usually the fastest path to a root cause once metrics have told you something is wrong. why averages hide problems consider 100 requests: 95 take 50ms and 5 take 2,000ms (a slow downstream dependency timing out for a subset of traffic). statistic value average 147.5ms p50 (median) 50ms p95 50ms p99 2,000ms the average (147.5ms) doesn't represent any real user's experience — it's pulled upward by a handful of outliers while looking only moderately bad, masking that 5% of users are having a genuinely broken experience. the median (p50) looks great and completely hides the problem. only the upper percentiles reveal it. [!key] an average is a single number computed over a skewed distribution — it represents nobody. always look at percentiles, and always look at more than one (p50 and p99 together tell you both "typical" and "worst common" experience). p50 / p90 / p95 / p99 / p99.9 — who experiences each percentile meaning who feels it typical use p50 (median) half of requests are faster than this the "typical" user baseline, day to day health p90 9 in 10 requests are faster a meaningfully worse than typical user secondary slo target p95 19 in 20 requests are faster a commonly cited slo threshold standard slo target for many apis p99 99 in 100 requests are faster 1% of your traffic — at scale, thousands of users/day tail latency slo, often paired with p50 p99`,"observability/capacity-planning":`capacity planning answers a deceptively simple question — "how much load can this handle before it falls over, and do we have enough headroom for tomorrow's peak" — with real numbers instead of a guess. it combines load testing, queueing behaviour, and cost trade offs, and it is the piece that turns "latency spiked" into "we were at 92% of capacity and a deploy pushed us over." measuring current headroom headroom is the gap between current peak usage and the point where the system degrades. you cannot plan capacity without first measuring it honestly against real production peak, not an average day. signal what it tells you peak cpu/memory utilisation over the last 30 days how close you already run to the edge on your worst day peak requests per second vs. known safe capacity direct comparison to your last load test result saturation metrics at peak (queue depth, pool wait time) whether you're already queueing during normal peak, not just in an incident time since last load test stale load tests don't reflect current code, traffic mix, or dependency behaviour [!key] headroom should be measured against your worst recent peak , not your average load. a service running comfortably at 30% average cpu can still be dangerously close to its limit if its peak (black friday, a marketing push, a batch job overlapping with traffic) already touches 85%. load testing to find the knee of the curve the "knee" is the point on the load vs latency curve where latency stops being flat and starts rising sharply — this is your real usable capacity, not the point where the service finally falls `,"observability/incident-management":"every senior interview eventually asks you to walk through an incident — how you found it, what you did first, and what changed afterward. this page covers the lifecycle, the roles that make a large incident manageable instead of chaotic, the single rule that separates good responders from bad ones, and how to structure a blameless postmortem that actually prevents a repeat. the incident lifecycle stage what happens typical duration target detect an alert fires or a human notices impact seconds to minutes (this is what mttd measures) triage confirm real impact, gauge scope/severity minutes declare formally open an incident, assign roles immediate once triage confirms real impact mitigate stop the bleeding — rollback, failover, flag flip minutes to tens of minutes resolve confirm the symptom is gone and stays gone varies review blameless postmortem, action items within days of resolution [!key] mitigate and resolve are not the same step . mitigation stops user impact now (often without knowing the root cause); resolution means the underlying issue is actually fixed. conflating them is why incidents drag on — teams wait to understand why before doing the thing that would stop the pain now . severity classification severity definition response expectation sev1 full outage or severe impact to a critical user journey (e.g., checkout, login down for all users) immediate, all hands, executive visibility sev2 partial degradation, clear but bounded user impact (one region, one feature) immediate response during and outside business hours sev3 minor issue, workaround exists, limited ","observability/production-debugging-playbook":'"production latency just spiked — what do you do?" is close to guaranteed in a senior interview because it tests everything at once: systems knowledge, structured thinking under pressure, and whether you reach for evidence instead of guesses. this page is the playbook — the first questions, the change first heuristic, a layer by layer narrowing process, and a symptom to cause map covering the eleven most common culprits. first questions before touching a dashboard, ask questions that shrink the search space. answering these takes under a minute and determines everything that follows. question why it matters when did it start — exactly? lets you correlate against deploys, config changes, and traffic patterns at that timestamp what changed around that time? deploys, feature flags, config, infra changes are the highest probability cause which endpoints/services are affected — all or specific? narrow blast radius points at a specific code path or dependency; broad points at shared infra which regions/azs? one region points at infra/network there; all regions points at code, a global dependency, or traffic which customers/segments? one customer/tier suggests a data shape or hot key issue, not a systemic one is it latency, errors, or both? pure latency often means contention/saturation; errors alongside it often mean a hard failure, not just slowness [!key] the order matters: when, what changed, how wide — in that order — before you open a single trace. most production incidents are explained by something that changed, and finding that correlation is faster than debugging from fi',"resume/my-resume":'every other page in this track exists to defend, expand, or rehearse something written here. this page is the source of record: the actual "about yourself," career path, strengths, and behavioural answers the author prepared, kept exactly as written rather than paraphrased. read it first, because the six layer drill, the architecture walkthrough template, and the numbers defence pages all assume you already know which claims are on the table. [!key] nothing on this page is aspirational copy — it is the literal script. if a later page asks you to defend "the 87% number" or "the disagreement story," this is where that number and that story originally came from. headline claims at a glance every number below shows up more than once across this track. know where it lives here, and which page is built to help you defend it under follow up questioning. claim comes from drilled into by 7m+ players, 600+ publishers reached about yourself, biggest achievement news feed system 5,000 requests per second sustained about yourself, biggest achievement resume deep dive framework 99.99% availability target about yourself defending your numbers p99 latency cut from 600ms to 150ms biggest achievement, biggest challenge architecture walkthrough template 87% reduction in gateway load biggest achievement, biggest challenge trade off justification bank 20+ legacy systems replaced by one platform biggest achievement news feed system led a team of four engineers about yourself, disagreement story leadership and mentoring zero downtime, dual feed migration disagreement story, biggest challenge fail',"resume/resume-deep-dive-framework":'nobody reads your résumé for facts — they read it for openings . every number and noun ("7m+ players", "cosmos db", "led a team of four") is a door an interviewer can walk through, and in a senior loop at least one of them will pick a single bullet and stay there for the better part of an interview. this page is the drill for making sure that door leads somewhere solid, no matter which one they choose. every bullet is a question in disguise read your own "about yourself" or "biggest achievement" answer the way an interviewer will: as a list of claims, not a story. "built backend platforms that serve 7m+ players and thousands of game publishers, handling around 5,000 requests per second with 99.99% availability" is really five separate questions waiting to happen — scale, load, availability, and two implicit "how do you know?" challenges. [!key] the bullet is the headline. the interview is the fact check. if you cannot answer three "why" questions in a row about any bullet on your résumé, that bullet is not ready yet. the six layer drill for every project you plan to talk about, run it through six layers before the interview, not during it. depth here is what separates "i used cosmos db and redis" from a senior answer. take the news feed platform bullet as an example: the problem was 20+ legacy systems and a clubs dependency slated for deprecation; the architecture was cosmos db as the source of truth with a versioned redis cache in front; your contribution was owning the data access and caching redesign end to end; the trade off was bounded cache staleness in exchange for l',"resume/architecture-walkthrough-template":`given a whiteboard and fifteen minutes, most candidates either freeze or over draw. the fix is not more architecture knowledge — it is a fixed narration order you can run for any system you have genuinely built, so the structure carries you even when nerves would otherwise cost you the thread. the nine part structure use this order every time, out loud, whether or not you're actually at a whiteboard. it works because it mirrors how the interviewer is scoring you: context first, then the shape of the system, then proof you understand the parts, then proof you can defend it. [!key] say the section names out loud as you move through them — "so that's the context, now let me show you what changed" — because it signals structure to the interviewer even before you've drawn a single box. context, constraints, before and after start with the business problem , not the technology: who was affected, and what was actually broken. then state the constraints you didn't choose — a deprecation deadline, an existing client contract you couldn't break, a team of a fixed size. constraints matter because they justify every trade off you make later; skipping them makes your decisions look arbitrary instead of forced. draw the before architecture briefly — a legacy dependency chain, a single region setup, whatever it actually was — then the after . the contrast is what makes the redesign land; nobody appreciates a caching layer in isolation, but everyone understands "p99 latency was 600ms because every read hit a slow legacy service directly." section target length what it must contain context `,"resume/project-story-templates":`a story bank is only useful if it is written down before the interview, not assembled live under pressure. this page gives you one template — problem, constraints, role, architecture, hardest problem, key trade off, outcome, what broke, and behavioural mapping — worked fully for one project so the pattern is obvious, then left blank for the rest of your résumé. why one story answers many questions a single well prepared project rarely maps to just one interview question. the same news feed platform story can answer "tell me about a system you built," "tell me about a disagreement," "tell me about a time you led without authority," and "tell me about a failure" — because a real project has multiple decision points, and which one you foreground depends entirely on which question was asked. [!key] do not prepare a new story for every possible question. prepare a small number of rich projects, then practise re slicing the same facts to foreground a different decision point depending on what's asked. the template fill this table in for every major project on your résumé. nine fields, one project per pass — the discipline of writing it down, not just thinking it, is what makes it retrievable under interview pressure. worked example: the news feed platform this is the résumé's headline achievement, filled in completely so you can see how dense a single project's answer should be. field answer problem a publisher news feed serving millions of players ran on a service being deprecated, coupled to legacy club membership rather than game identity, and was slow — the platform needed a `,"resume/my-preparation-syllabus":`a résumé like the one on my resume can be tested from roughly twenty different technical and behavioural angles, and no candidate has time to prepare all twenty to the same depth. this page is the study plan that resolves that tension: a breadth first sweep across the areas any senior backend interview loop might touch, with deliberate extra depth on the areas this particular résumé actually earns — distributed systems, azure, and technical leadership — rather than an even coat of preparation everywhere. [!key] the strongest interview narrative here is six plus years at microsoft xbox, moving from full stack delivery into backend focused distributed systems, azure platform engineering, technical leadership, and now ai engineering. every topic below is prioritized against how well it supports that specific narrative — not against some universal ranking of "important" computer science topics. the prioritization principle don't try to get equally deep in everything. with around twenty plausible topic areas and a finite number of preparation hours, the only sustainable strategy is breadth first coverage — enough working knowledge everywhere that nothing is a total blank — layered with deep, defensible expertise in the handful of areas where this résumé's actual claims live. an interviewer can spend thirty to forty five minutes on a single bullet like the 5k rps/99.99% availability system, the cosmos/redis redesign, the event driven ai platform, or the security work, so those areas need textbook plus implementation depth, not textbook depth alone. [!tip] read the chain above as `,"resume/defending-your-numbers":'a number on a résumé is a claim, and senior interviewers are trained to test claims. "7m+ players", "5,000 requests per second", "99.99% availability", "87% reduction in gateway load" — each one invites the same two word follow up: how measured? if you cannot answer that in one breath, the number stops helping you and starts hurting you. why numbers get probed harder than anything else prose is hard to fact check in a 45 minute interview; a number is easy. "how did you measure that?", "what was the baseline?", and "how confident are you in that figure?" take seconds to ask and immediately reveal whether a candidate understood their own impact or copied a number from a dashboard someone else built. this is precisely why numbers deserve more preparation per word than any other part of your résumé. [!key] never state a number without being able to say, in the next sentence, what it was measured against and over what window. the number is not the answer — the number plus its measurement is the answer. your claim types, and what each one really invites below is a working table for the claim types that show up across a typical senior backend résumé. fill in the blank column with your own answer before the interview — the exercise of writing it down is where the real preparation happens. claim type what it actually means likely follow up your answer user count (e.g. 7m+ players) reach or scale of the audience affected "monthly active, or total registered? over what window?" [fill in] requests per second (e.g. 5,000 rps) sustained throughput the system handles "average or peak? mea',"resume/trade-off-justification-bank":`"why did you choose x?" is the single most common follow up in a system design or project round, and it is never really about x — it is about whether you considered anything else. this page is a bank of prepared answers for the trade offs that show up on almost every backend résumé, each with the alternative you weighed, the factor that actually decided it, and the downside you'd admit if pushed. the shape of a good trade off answer every answer in this bank follows the same structure, and you should force yourself into it even under pressure: the alternatives considered, the deciding factor, and the honest downside. skipping the downside is the most common mistake — a trade off with no downside is not a trade off, it's a sales pitch, and interviewers can tell the difference immediately. [!key] the downside is not optional. if you can't name what you gave up, you haven't actually made a trade off — you've just picked the option you already knew. the trade off bank decision alternatives considered deciding factor honest downside document database (cosmos db) over relational sql server with a normalized schema access pattern was high volume reads by a known key, with an evolving schema weaker support for ad hoc joins and multi row transactions across entities partition key keyed by game title partition by publisher, by post id, by club membership matched the dominant query — reads scoped to one title — and avoided legacy club coupling a small number of unusually popular titles can still create a hot partition without care distributed cache (redis) in front of the database no `,"resume/failure-scenario-drills":`"what happens if x breaks?" is asked in almost every senior backend round, and it is the question most candidates prepare for least, because it requires imagining failure in a system you're used to describing as a success story. this page drills that question against the reader's own systems — a high scale feed platform, an event driven certification pipeline, and a multi region service estate — so the answer is rehearsed, not improvised. why this question separates levels a mid level engineer describes what a system does. a senior engineer describes what it does when something goes wrong , because production systems spend a meaningful fraction of their life in a degraded state, and the difference between a graceful degradation and an outage is almost always a decision someone made in advance. [!key] the best answer to "what if x breaks?" is one you clearly thought about before it happened, not one you're constructing live. if your answer starts with "hmm, i guess we'd..." you have already lost the thread. the scenario table use this as a drill sheet. for each row, be ready to state the blast radius, how you'd detect it, what you'd do in the first five minutes, and what you'd build afterward so it doesn't happen again. scenario blast radius detection signal immediate mitigation permanent fix database (cosmos) goes down all reads and writes in the affected region elevated error rate, timeouts, region health check failing automatic regional failover to a healthy read region multi region write topology, tested failover runbook, chaos test the failover path distributed cache go`,"security/authentication-vs-authorization":'authentication and authorization are the two questions every request answers before anything else happens: who are you , and what are you allowed to do . interviewers use this topic to check that you have precise vocabulary, not just a vague sense that "auth" is a solved problem. the precise definitions, and the order they happen authentication (authn) always happens before authorization (authz). you cannot decide what someone can do until you know who they are — even an "anonymous" role is still a resolved identity. authentication authorization question answered who are you? what are you allowed to do? input credentials (password, token, certificate) an established identity + a resource + an action output an identity (and usually a token/session) allow or deny happens first, once per session or token second, on every protected request failure status typical mechanisms passwords, mfa, oauth/oidc login, mtls rbac, abac, acls, policy engines [!key] authentication proves identity once; authorization is re evaluated on every single request, because permissions can change between requests even when identity does not. identity, credentials, and claims these three words get used loosely in casual conversation but mean distinct things in a design discussion: identity — the durable "who" a system tracks: a user id, a service principal, a device. it exists whether or not anyone is currently logged in. credentials — the secret or proof used to assert an identity at a point in time: a password, a private key, a client certificate, a one time code. credentials are exchanged once, during',"security/oauth2-and-openid-connect":`oauth 2.0 is an authorization delegation framework — it lets a user grant an application limited access to their data on another service, without handing over a password. openid connect (oidc) is a thin identity layer built on top of oauth that adds "and here is who logged in". interviewers use this pair to check you know where authorization ends and authentication begins. the four roles role definition example resource owner the user who owns the data you, logging into a photo app client the application requesting access the third party photo printing app authorization server issues tokens after authenticating the owner and getting consent google's/okta's oauth endpoint resource server hosts the protected data and validates tokens on each request google photos api the client never sees the resource owner's password. it only ever receives a token scoped to specific permissions, issued by the authorization server after the owner consents — a third party app requesting your github data only ever sees a token, never your github password: [!key] oauth answers "can this app do x on my behalf" — it was never designed to answer "who is this user". that distinction is the single most tested idea in this topic. grant types (flows) grant type who it's for notes authorization code server side web apps, spas, mobile the default choice today; code is exchanged server side (or with pkce) for a token authorization code + pkce spas, mobile/native apps (public clients) mandatory when the client can't hold a secret; prevents code interception client credentials machine to machine, no user in`,"security/jwt-deep-dive":`a json web token (jwt) is a compact, signed, self contained way to carry claims between two parties. it is everywhere in modern apis, and interviewers use it to test whether you actually validate every field or just check the signature and move on. structure: header, payload, signature a jwt is three base64url encoded segments joined by dots: . decoded: the signature is computed over using the algorithm named in the header. anyone can decode and read the payload — it is not encrypted, only encoded — so a jwt must never carry secrets in plaintext claims. [!key] the signature proves the token wasn't tampered with after issuance. it says nothing about confidentiality — treat every claim as visible to whoever holds the token. signing algorithms: hs256 vs rs256 vs es256 algorithm type key distribution typical use symmetric (hmac + sha 256) same secret must be shared with every verifier single service issuing and validating its own tokens asymmetric (rsa + sha 256) private key signs, public key (via jwks) verifies multi service systems — any service can verify without holding the signing secret asymmetric (ecdsa + sha 256) same model as rs256, smaller keys/signatures same as rs256, preferred when token size or cpu matters [!tip] a senior answer names the trade off directly: "hs256 is simpler for a monolith, but every verifier needs the same secret — if one microservice is compromised, it can forge tokens, not just read them. rs256/es256 let dozens of services verify with a public key while only the auth server can sign." the and algorithm confusion attacks two classic jwt library`,"security/access-control-models":'once you know who someone is, access control decides what they can do — and the model you pick determines whether that decision stays maintainable at scale or turns into an unauditable mess of one off roles. interviewers use this topic to see whether you can match a model to a real permission shape, not just recite acronyms. rbac, abac, rebac, and acls compared model how decisions are made flexibility auditability scale typical use rbac (role based) user → role → fixed set of permissions low — permissions are coarse and static per role high — "who has role x" is a simple query good for a modest number of distinct roles internal admin tools, most saas apps abac (attribute based) policy evaluates attributes of user, resource, environment at request time high — arbitrary conditions (department = resource.department, time of day, clearance level) harder — decision depends on runtime context, not a static table scales to many fine grained rules without new roles regulated industries, complex multi dimensional rules rebac (relationship based) access follows a graph of relationships (owner, member of, shared with) high for hierarchical/social data moderate — traceable via the relationship graph scales well for deeply nested sharing (folders, docs, orgs) google drive style sharing, social graphs, nested orgs acl (access control list) explicit per resource list of (subject, permission) pairs high per resource, but no reuse across resources easy per resource, hard in aggregate ("what can alice access everywhere?") poor — list grows with users × resources filesystems, small numbers of',"security/workload-identity-and-secrets":`every service needs to prove who it is to every other service and database it talks to — and for decades the default answer was a connection string or api key sitting in a config file. interviewers use this topic to check whether you understand what replaced that pattern and why, because "we rotate our secrets quarterly" is a much weaker answer than "we don't have secrets to rotate for this call." the problem with connection strings and long lived credentials a connection string or api key embedded in configuration is a long lived, static, bearer credential : whoever has the string has full access, indefinitely, until someone manually rotates it. concretely, this creates four recurring problems: it has to live somewhere — config files, environment variables, ci/cd variables — and every one of those is a place it can be accidentally committed, logged, or copied. rotation is manual and risky — changing it means coordinating an update across every place it's deployed, often causing an outage if done carelessly. it doesn't expire on its own — a leaked key from two years ago might still work today. it grants the same access to anyone holding it — there's no way to tell "this call came from service a vs a leaked copy of service a's key." [!key] the strongest fix isn't better secret storage — it's removing the secret entirely. if a service's identity can be proven by the platform it runs on, there is no credential left to leak, rotate, or scan for. service to service authentication options approach how it proves identity secret to manage? notes shared secret / api key both sides k`,"security/owasp-and-injection":"the owasp top 10 is the industry's reference list of the most impactful web application risk categories, and interviewers use it as a checklist to see how many you can explain from first principles rather than just name. this page covers the full list briefly, then goes deep on the handful — injection, ssrf, access control, deserialization — that show up as actual code review and debugging questions. the owasp top 10 (2021 edition) risk what it is one line defence broken access control users can act outside their intended permissions enforce authorization server side on every request, never trust the client cryptographic failures sensitive data exposed due to weak or missing encryption encrypt in transit and at rest; use vetted algorithms, never roll your own injection untrusted input is interpreted as code/commands (sql, os, ldap) parameterize queries and commands; never concatenate untrusted input insecure design security flaws baked into the architecture itself, not just the code threat model before building; design in controls, don't bolt them on security misconfiguration default credentials, verbose errors, unnecessary features left enabled harden defaults, disable what you don't use, automate config checks vulnerable and outdated components using libraries/frameworks with known cves track dependencies, patch on a schedule, scan ci for known vulnerabilities identification and authentication failures weak login, session, or credential handling mfa, strong session management, rate limit login attempts software and data integrity failures trusting unsigned code/updates or","security/xss-csrf-and-cors":`these three acronyms get confused constantly, and interviewers use that confusion to filter candidates. xss is about untrusted data becoming code, csrf is about a browser's cookies being used against the user, and cors is a relaxation of a browser rule that has nothing to do with server side authorization. keep those three sentences straight and you will outperform most candidates immediately. cross site scripting (xss) xss happens when attacker controlled input is rendered as executable html, javascript or css instead of inert text. type where the payload lives example trigger stored persisted in the database, served to every viewer a comment field rendered without encoding reflected bounced back in the response for one request a search query echoed into a results page dom based never touches the server; a client side sink executes it written into the fix is context aware output encoding , not a single "sanitize everything" function. the encoding rule depends on where the data lands: output context required encoding html body text html entity encode ( → ) html attribute attribute encode, quote every attribute block / js string javascript string encode, never just html encode url query parameter url encode css value css encode, disallow / from user input the rule to say out loud: encode at the point of output , based on the context you are writing into — not at the point of input. input validation (allow lists for shape) and output encoding solve different problems; conflating them is how encoding bugs slip through. dom xss sinks and sanitisation libraries dom xss is worth `,"security/passwords-and-sessions":"password storage and session management are two of the most consequential things a backend engineer implements, and both have a small set of well known right answers. interviewers use this topic to check whether you've internalised those answers or are still reasoning from first principles under pressure — which, for cryptographic primitives, is exactly how mistakes get shipped. never encrypt, never plain hash encrypting passwords is wrong because encryption is reversible — whoever holds the key can recover every password, and that key lives somewhere reachable from the same breach that exposes the database. hashing with a fast general purpose hash ( , ) is also wrong, because those hashes are designed to be fast , which is exactly the wrong property for password storage: an attacker with a stolen hash database can compute billions of hashes per second on commodity gpus and simply try every password in a breach list or dictionary until one matches. [!key] the property you actually want is a hash function that is deliberately slow and memory hard , so that trying billions of guesses against a stolen hash is computationally expensive even though verifying one legitimate login is fast enough for a real user to not notice. salts are a random value stored alongside each hash, mixed into the hashing input, so two users with the same password get different hashes — this defeats precomputed rainbow tables and stops one cracked hash from revealing every user sharing that password. peppers are a secret value shared across all users, stored outside the database (in a key vault or conf","security/tls-and-certificates":`tls is asked about constantly because almost every service depends on it, yet most engineers can only describe it as "the padlock icon". a senior answer explains what tls guarantees, what it deliberately does not guarantee, and how the handshake gets both sides to a shared key without ever sending that key over the wire. what tls provides — and what it doesn't property provided by tls mechanism confidentiality yes symmetric encryption of the session (aes gcm, chacha20) integrity yes aead ciphers / macs detect tampering in transit server authentication yes certificate signed by a trusted ca proves server identity client authentication only if configured (mtls) client certificate, not used by default application layer security no tls doesn't stop sql injection, xss, or a compromised endpoint data at rest protection no tls only protects data in transit [!key] tls answers "am i talking to who i think i am, and can anyone eavesdrop or tamper on the way?" it says nothing about what happens once the data arrives — a server can still log secrets in plaintext or store passwords unhashed after a perfectly valid tls connection. symmetric vs asymmetric roles tls uses both, because each is good at a different job: asymmetric (public/private key) crypto is slow but solves the "we've never met" identity and key exchange problem; symmetric crypto is fast and does the actual bulk encryption once both sides share a secret. the handshake, step by step tls 1.3 collapses this to a single round trip : the client guesses the server's preferred key exchange group and sends its key share in the ver`,"security/cloud-and-network-security":`cloud security interview questions are rarely about a single exotic attack — they're about whether you design systems assuming any one layer will eventually fail. a senior answer names the layer that failed, the layer that caught it, and why neither alone was sufficient. defence in depth and shared responsibility defence in depth means no single control is trusted to be the only thing standing between an attacker and data — network isolation, identity, encryption, monitoring and application layer validation each independently reduce blast radius, so a failure in one still leaves others standing. the shared responsibility model draws a line between what the cloud provider secures and what you do: the provider secures the physical data centre, host hypervisor and, for managed services, the underlying platform; you remain responsible for identity configuration, network rules, data classification, and anything you deploy on top. this split shifts by service model — iaas puts most responsibility on you (os patching included), paas shifts patching to the provider, saas shifts almost everything except your own data and access configuration. [!key] "the cloud is secure" is not a sentence — say "the cloud provider's infrastructure is secure; my configuration on top of it is my responsibility." nearly every major cloud breach in the last decade was a misconfiguration on the customer side (an open storage bucket, an overly broad iam role), not a provider side failure. least privilege in practice practice what it prevents role scoping to specific resources, not subscription wide compro`,"security/vulnerability-management":`vulnerability management is the operational discipline behind "keep dependencies patched", and it's asked about because it reveals whether a candidate has actually run a security program at scale versus just knowing individual vulnerability classes. the core tension interviewers probe for: there are always more findings than engineering hours to fix them, so prioritisation is the real skill. the lifecycle discover covers everything from automated scanners to bug bounty reports to a researcher's email. triage confirms the finding is real (not a false positive) and understands its context — is the vulnerable code path actually reachable? prioritise ranks confirmed findings against limited remediation capacity. remediate is the actual fix — a patch, a config change, or a compensating control. verify re scans or re tests to confirm the fix worked and didn't regress. report closes the loop with metrics (mean time to remediate, backlog age) that feed back into how discovery and triage are tuned next cycle. cve and cvss a cve (common vulnerabilities and exposures) is a unique identifier for a specific publicly known vulnerability — it's a name, not a severity rating. cvss (common vulnerability scoring system) is the severity score, built from three metric groups: cvss component what it captures changes over time? base score intrinsic severity — attack vector, complexity, privileges required, impact no — fixed at disclosure temporal score exploit maturity, availability of a fix, confidence in the report yes — as exploits/patches emerge environmental score your specific deployment c`,"security/threat-modelling-and-secure-design":`threat modelling is what separates "we bolted security on afterwards" from "security was a design constraint from day one" — and interviewers who ask you to threat model a design on a whiteboard are testing exactly that habit of mind. the goal is not to find every possible attack; it's to systematically ask the right questions before code is written, when a fix is a design change instead of a production incident. when to threat model, and who's in the room threat modelling pays off most before implementation — at design review, when a new trust boundary is introduced (a new external integration, a new data store holding sensitive data), or when an existing system changes its exposure (moving from internal only to internet facing). the right room includes the engineers who will build it, someone who understands the data's sensitivity (product or compliance), and ideally someone from security who isn't emotionally attached to the design and will push back. [!key] a threat model is a conversation with a structure , not a document produced in isolation. if the engineers who will actually write the code weren't in the room, the exercise rarely changes anything real. the four questions framework adam shostack's four question framework is the simplest structure to reach for, on a whiteboard or in an interview: question purpose 1. what are we building? draw the data flow diagram — components, data stores, trust boundaries 2. what can go wrong? enumerate threats per component/boundary, typically via stride 3. what are we going to do about it? assign a mitigation or accept the risk e`,"system-design/how-to-run-a-system-design-interview":"a system design interview is not a test of trivia; it is a test of whether you can turn a vague prompt into a working architecture out loud, in real time, while someone watches. the candidates who do well are not the ones who know the most acronyms — they are the ones who run a repeatable process so the interviewer always knows what phase they are in and why. why a framework matters without structure, candidates either freeze on a blank whiteboard or dive straight into boxes and arrows and design something that solves the wrong problem. a framework fixes both failure modes: it forces you to scope the problem before you solve it, and it gives the interviewer natural checkpoints to redirect you if you are heading somewhere they do not care about. [!key] the framework is a checklist for you , not a script to recite. move fluidly between phases and let the interviewer's questions pull you back to whichever one matters. the 45 minute time budget treat the clock as a resource you manage, not something that happens to you. a rough split for a 45 minute round: phase minutes goal requirements (functional, non functional, out of scope) 5 agree on what you are building and what you are explicitly not scale estimation 3 numbers that will justify later design decisions core entities 2 name the nouns the system exchanges api design 5 write the contract between client and system high level architecture 12 a design that satisfies every functional requirement data model 3 key fields and indexes on the entities that matter deep dives 12 fix bottlenecks, satisfy the non functional requirement","system-design/back-of-the-envelope-estimation":'estimation is not a maths exam — it is a tool you use mid design to decide whether a component is even necessary. a design that fits on one database with replicas should not be sharded just because the problem sounds big; the arithmetic is what proves it. why this skill matters every non functional requirement eventually needs a number behind it. "highly available" and "handles a lot of traffic" are not answers; "8.76 hours of downtime a year is acceptable, and we need to sustain roughly 5,000 requests per second at peak" is. estimation converts a fuzzy requirement into a number you can compare against known hardware and service limits, which is what actually justifies "i need a cache here" or "one postgres instance is enough." [!key] the point of estimation is to decide , not to be precise. round aggressively, say the rounding out loud, and move on the moment the number tells you what to do. powers of two and ten, memorised once you will convert between bytes and requests constantly. keep these anchors in your head so you never do long division live. power of 2 value power of 10 value 2¹⁰ 1 thousand (kb) 10³ 1,000 2²⁰ 1 million (mb) 10⁶ 1,000,000 2³⁰ 1 billion (gb) 10⁹ 1,000,000,000 2⁴⁰ 1 trillion (tb) 10¹² 1,000,000,000,000 2⁵⁰ 1 quadrillion (pb) 10¹⁵ 1,000,000,000,000,000 is the single fact that makes every other conversion easy — treat "kilo, mega, giga, tera, peta" as roughly interchangeable between the binary and decimal worlds for interview purposes. latency numbers every engineer should know these numbers are the reference table you compare every design decision aga',"system-design/system-design-walkthrough":`designing complex systems that are scalable, reliable, and maintainable, and that actually solve a real business problem, is the whole job — and there is no single right answer. this page walks one interview end to end, phase by phase, narrating what you'd actually say and draw at each step using a twitter like feed as the running example, then covers the eight recurring patterns — real time updates, long running tasks, contention, scaling reads and writes, large blobs, multi step workflows, and proximity search — whose escalation ladders show up, in some combination, in almost every prompt you'll get. [!key] interviews come in two shapes. product design ("design twitter/uber/netflix") runs requirements → entities → api → high level design → deep dives. infrastructure design ("design a rate limiter/message queue") swaps entities and api for a system interface (inputs and outputs of the component itself) and an explicit data flow step, because there's no end user — just other systems calling in. what you're actually being graded on criterion what a strong transcript shows problem navigation you broke the prompt down, gathered requirements, and had a visible path to a solution solution design each part solves a real requirement, and the parts integrate into one coherent design technical excellence you applied current technology knowledge and named real best practices, not folklore communication you collaborated with the interviewer, explained clearly, and took feedback constructively the six phase delivery framework 1. requirements (5 minutes) functional requirements are what`,"system-design/hld-cheatsheet":`this is a single sheet revision document, not a tutorial — every topic below has its own full explanation elsewhere on this site, and the point here is density: the framework script, the pattern catalogue, the toolbox tables, and the problem playbooks you want available in your head with nothing left to derive live. there is no single right answer in a system design interview; you're graded on problem navigation, solution design, technical excellence, and communication, not on matching a reference diagram. [!key] say the generic concept first, then the product name: "i need a partitioned, durable, ordered log — kafka, or event hubs on azure." this reads as understanding the concept independent of any one vendor's api. the 60 minute script interview type flow product design (uber, whatsapp, twitter, netflix) requirements → entities → api → hld → deep dives infra design (rate limiter, message queue, lb) requirements → system interface (in/out) → data flow → hld → deep dives requirements and non functional trade offs functional requirements are verbs — ask targeted questions as if talking to a product owner, pick the top three, and explicitly park the rest as out of scope. non functional requirements must be quantified ; walk the mnemonic scale for cloud designs to make sure you covered the ground: letter dimension ask yourself s scalability read or write heavy? bursty? dau? c consistency cap — pick c or a (p is a given) a availability uptime target? degraded mode acceptable? l latency p99 budget? (<100ms = "low latency") e environment mobile, low bandwidth, region f fault tol`,"system-design/building-blocks-overview":`almost every system design interview, however different the prompt sounds, gets assembled from the same small parts bin: something to route traffic, something to hold state, something to absorb load, something to make reads fast. this page is the tour of that parts bin — what each component solves, when it earns its place in a design, and which page in this group covers it end to end. treat it as the index you skim before a design, not the place you learn any one component deeply. [!key] a component belongs in your design the moment a stated requirement forces it, not because the problem "sounds like" it needs one. naming a load balancer, a cache, and a queue before you've established why is the single most common way to lose the "problem navigation" score. proxies: the traffic layer underneath everything every one of the network facing building blocks below is, structurally, a proxy — something that sits between a client and the real destination and does useful work in the middle. forward proxy — sits in front of the client , forwarding the client's own requests out to the internet. used for caching outbound traffic, anonymising a client's origin, and region shifting (the "instagram proxy" pattern of appearing to browse from another country). reverse proxy — sits in front of the server , accepting inbound requests from the internet and forwarding them to the correct backend. load balancers, cdns, api gateways, and firewalls are all specialised reverse proxies; nginx is the canonical example of software that can be configured as one. [!tip] when an interviewer asks "what do`,"system-design/load-balancing":`a load balancer is the first thing that decides whether your beautifully designed fleet of stateless servers actually gets used evenly. interviewers ask about it constantly because the "right" algorithm changes with the traffic shape, and picking one without saying why is a missed signal. what a load balancer actually does at its core, a load balancer is a reverse proxy that distributes incoming requests across a pool of backend servers, continuously checks whether each backend is healthy, and removes unhealthy ones from rotation without the client noticing. it exists to turn "one server that can fall over" into "a fleet that can lose members and keep serving." [!key] a load balancer only helps if the backends behind it are interchangeable. the moment one server holds state another doesn't have, you've built a system that looks load balanced but isn't. layer 4 vs layer 7 the first real design decision is which osi layer the balancer operates at. layer 4 (transport) layer 7 (application) operates on ip address and tcp/udp port http method, path, headers, cookies, body can inspect content ❌ ✅ routing decisions by connection only by url, header, cookie, content type speed very fast, minimal overhead slower — must parse the request ssl termination usually passthrough can terminate and inspect decrypted traffic typical use raw tcp/udp, websockets, database proxies http/https apis, content based routing, a/b routing examples azure load balancer, aws nlb, ipvs azure application gateway/front door, aws alb, nginx, haproxy, envoy [!tip] say it as a rule of thumb: "l4 when i need raw`,"system-design/api-gateway-and-reverse-proxy":`the moment a design grows past a single service, something has to decide which requests go where, who is allowed in, and how much of the plumbing every service would otherwise duplicate. that something is a reverse proxy at minimum, and usually an api gateway once the plumbing gets complex enough. reverse proxy: the base layer a reverse proxy sits in front of one or more backend servers and forwards client requests to them, so clients only ever talk to the proxy's address, never to a backend directly. it hides backend topology, can terminate tls, and can do basic routing and load balancing. nginx and haproxy are the classic examples, and every load balancer discussed elsewhere is, structurally, a reverse proxy that also health checks and distributes across a pool. [!key] a reverse proxy answers "how do i reach a backend." an api gateway answers "how do i reach the right backend, safely, with the cross cutting stuff already handled." what an api gateway adds on top an api gateway is a reverse proxy with application awareness — it understands your api surface, not just tcp connections. responsibility what it means routing map a public path/version to a specific internal service — → order service authn/authz validate a jwt/api key once at the edge, so backend services trust a pre verified identity rate limiting per user/api key/tenant throttling, before load ever reaches a service request aggregation fan out one client call into several backend calls and merge the response protocol translation public rest/json in, internal grpc or a different serialization out observability ce`,"system-design/cdn-and-edge":"a cdn is the cheapest latency win available in most designs: it moves bytes physically closer to users instead of making every request cross an ocean to your origin. interviewers use it to test whether you understand caching at a network level, not just inside an application. how a cdn works a cdn is a network of geographically distributed servers, called points of presence (pops) , each holding an edge cache of content close to end users. when a request would normally travel to your origin server, it instead hits the nearest pop; if that pop already has the content cached, it serves it directly (a cache hit ) with none of the round trip to your origin. on a cache miss , the pop fetches from the origin, serves the client, and stores a copy for next time. an origin shield is one designated pop that sits between the edge pops and your origin. without it, a viral piece of content can cause hundreds of edge pops to each independently miss and hit your origin simultaneously; the shield absorbs that fan in, ensuring the origin only sees one request per shield, not one per pop. [!key] a cdn is just another cache layer — the same rules about ttl, invalidation, and cache keys apply, just at a network hop closer to the user instead of inside your data center. push vs pull origin pull (origin pull) push how content arrives at the edge cdn fetches on first request (cache miss), then caches it you proactively upload content to the cdn ahead of time freshness content is only as fresh as the last fetch + ttl you control exactly when new content is available best for dynamic or long tail c","system-design/object-storage-and-file-systems":'every system with images, videos, documents, backups or logs needs an answer to "where do the bytes live", and the answer is almost never "in the database". object storage (s3, azure blob, gcs) is the default, and interviewers expect you to reach for it instantly whenever a file crosses 1 mb. object vs block vs file storage these three are genuinely different technologies, not marketing names for the same thing. model unit access pattern example use for block storage fixed size blocks raw disk, mounted, os manages the filesystem ebs, azure disk databases, vm boot disks file storage files in a directory tree posix paths, shared mounts nfs, efs, azure files shared config, legacy apps expecting a filesystem object storage whole immutable objects http api, flat key space s3, azure blob, gcs images, video, backups, logs, static sites [!key] object storage trades filesystem semantics (partial writes, directory locking, posix permissions) for massive horizontal scale and simplicity . you cannot open a byte range and rewrite it in place — you replace the whole object. buckets, keys and metadata an object store has almost no structure: a bucket (top level namespace, globally or account unique), a key (a string that looks like a path but is not one), and the object itself plus metadata (content type, custom headers, tags). looks like a folder path, but the store has no directories — it is one flat string key, and consoles fake a folder view by splitting on . this matters for interviews: listing "a folder" is really a prefix scan , and a bucket with billions of keys under one prefix c',"system-design/search-and-indexing":`"add search" is one of the most common feature requests in a system design interview, and it is also where candidates default to and get stuck the moment the interviewer asks about scale. full text search needs a fundamentally different data structure — the inverted index — built and served by a dedicated system. why like doesn't scale a query with a leading wildcard cannot use a b tree index at all: the database has no choice but to scan every row and check the string. on a 10 million row table that means tens of gigabytes read for a single query, every time, with no relevance ranking and no tolerance for typos or word variations. need sql dedicated search engine exact prefix match ✅ ( can use an index) ✅ substring/contains match ❌ full table scan ✅ inverted index relevance ranking ❌ none ✅ bm25/tf idf scoring typo tolerance, stemming, synonyms ❌ ✅ analyzers faceting (counts per category) expensive ✅ native aggregations scale degrades past 10⁵–10⁶ rows built for 10⁹+ documents [!key] a relational database is optimized for exact lookups and range scans on indexed columns. full text relevance search is a different problem with a different data structure — reach for a search engine, don't fight the database. the inverted index an inverted index maps terms back to the documents that contain them — the inverse of a document listing its words, hence the name. each term points to a postings list of document ids (plus positions and term frequency for ranking). a query for is now an hash/b tree lookup into the index, not a scan of every document. building the index: the analysis pi`,"system-design/message-queues-and-pubsub":`adding a queue is one of the most common "deep dive" moves in a system design interview, and also one of the most commonly justified with a vague wave — "we'll add a queue for scale." the strong answer names exactly which problem the queue solves and which topology fits it. why you add a queue at all a queue sits between a producer and a consumer so they don't have to interact synchronously. that decoupling buys four concrete things: benefit what it actually means decoupling producer and consumer can be deployed, scaled, and fail independently — the producer doesn't need to know who, or how many, consumers exist load levelling a burst of 100,000 requests can be written to a queue instantly and drained by consumers at a sustainable rate, instead of overwhelming downstream services retries a failed message can be redelivered rather than silently lost, without the original caller needing to retry manually buffering if a downstream consumer is temporarily slow or down, work queues up rather than being dropped or blocking the producer [!key] a queue turns a synchronous dependency into an asynchronous one. that's the whole value proposition — everything else (ordering, fan out, retries) is a detail of how a specific queue implements that core trade. point to point queue vs publish/subscribe vs log based streaming point to point queue publish/subscribe log based streaming model one message, consumed by exactly one consumer one message, delivered to every subscriber an append only, partitioned, ordered log; consumers read at their own offset consumption competing consumers pull fro`,"system-design/distributed-locks":'a distributed lock lets multiple processes on multiple machines agree that only one of them may touch a given resource at a time. it sounds like a simple mutex, but network partitions, process pauses and clock drift mean a naive implementation is almost always subtly broken — and this is one of the favorite "make the candidate think on their feet" topics at senior level. why you need one a regular in process lock (a , a ) only coordinates threads inside one process. the moment your seat reservation logic, your scheduled job runner, or your cache warming routine runs on more than one machine, you need coordination that lives outside any single process — in redis, a database row, or a coordination service like zookeeper or etcd. typical use cases: "only one node should run this cron job", "hold this seat for 10 minutes while the user checks out", "only one worker should process this batch at a time". what a correct distributed lock needs requirement meaning what breaks it mutual exclusion only one holder at a time race between two calls without atomicity deadlock freedom the lock is always eventually released, even if the holder crashes no ttl / expiry on the lock fault tolerance survives the failure of the node holding the lock single point of failure in the lock store safety under a paused holder a holder that resumes after its lock expired must not act as if it still holds it no fencing token — the classic gap [!key] a lock with a ttl solves deadlock freedom but reopens mutual exclusion: the ttl can expire while the original holder is still working, letting a second proces',"system-design/job-scheduling-and-workflows":`every non trivial system eventually needs something to run later, run repeatedly, or run as a sequence of steps that outlives any single process — sending a reminder email, generating a nightly report, or orchestrating a multi day order fulfillment. the moment "run this on one server" becomes "run this exactly once across a fleet", the design gets genuinely hard. cron vs a distributed scheduler a single entry works until you have more than one instance of your service — then every instance fires the job simultaneously, or a server restart silently drops a run. cron (single box) distributed scheduler runs on one machine a cluster, coordinated failure mode job silently doesn't run if the box is down another node picks it up duplicate runs impossible (only one box) must actively prevent (leader election / locking) scale one process's worth of jobs millions of jobs, sharded across workers examples linux , windows task scheduler quartz, kubernetes cronjob + leader election, temporal, airflow, cloud schedulers [!key] the whole problem of distributed scheduling is: exactly one node should decide "it's time", but any node should be able to do the work. those are two separate concerns — don't conflate the trigger with the execution . at least once vs at most once execution distributed systems cannot cheaply guarantee exactly once execution — a scheduler that crashes right after triggering a job but before recording that it did can't tell whether the job actually ran. guarantee behavior risk at most once trigger, don't retry on ambiguous failure job might silently never run at least `,"system-design/caching-strategies":'caching is the highest leverage per line of explanation topic in system design: a correct cache strategy can turn a database that falls over at scale into one that barely notices load. the trap is naming "add a cache" without saying which pattern, what gets evicted, and what happens on a miss. where caches live a request can be served from a cache at several layers before it ever reaches the source of truth, and each layer trades off differently. layer example scope notes client browser cache, mobile app local storage single user fastest, but not server controlled and can go stale silently cdn / edge cloudflare, front door geographic region best for static or widely shared content gateway api gateway response cache all clients of one route good for public, cacheable api responses application in process (local) or redis/memcached (distributed) one instance or the whole fleet the layer most "caching strategy" discussions are about database query cache, buffer pool, materialized views within the database itself often already managed for you; still worth naming layer in practice reference external / distributed cache — shared and scalable (redis, memcached); the default choice for anything the whole fleet needs to agree on in process / in memory cache — fastest possible access, but local to one instance, not shared (l1/l2/l3, plain ram) cdn cache — caches static assets at edge servers close to users specifically to cut cross region latency client side cache — lives on the browser/app, cuts server round trips but is not server controlled and can go stale unnoticed [!key] the fur',"system-design/cache-pitfalls-and-invalidation":`every cache you add is also a new way for a system to fail. the patterns in this page are the ones interviewers reach for specifically to test whether you understand caching's failure modes, not just its happy path — and "the site goes down" is never an acceptable answer to "what happens if the cache fails?" cache stampede (thundering herd) a stampede happens when a single hot key expires or is invalidated, and a large burst of concurrent requests for that exact key all miss the cache at once, all hammering the database simultaneously to recompute the same value. {width=500} fix how it works request coalescing / single flight the first request to miss acquires a lock and recomputes; concurrent requests for the same key wait for that result instead of each hitting the db probabilistic early expiry each read has a small, increasing chance of refreshing the value before it actually expires, spreading recomputation out over time instead of at one instant locking (mutex per key) a distributed lock (e.g. redis ) ensures only one process recomputes a given key at a time; others either wait or serve stale data briefly never let hot keys expire for known always hot keys, use refresh ahead so there's no expiry moment to stampede at all [!key] a stampede is fundamentally a coordination problem — many workers doing redundant work at the same instant. every fix is some form of "let one worker do the work, make the rest wait or serve something slightly stale." a related but distinct race shows up even without an expiry event: two separate worker instances independently check the cache at`,"system-design/choosing-a-database":`"which database?" is one of the few system design questions with a genuinely defensible default answer — and yet candidates lose points either by picking something exotic to sound impressive, or by picking one database for the whole system without asking what each part of the data actually needs. the decision framework before naming a database, answer five questions about the data itself. the answers, not familiarity or hype, should drive the pick. question why it matters access pattern is data fetched by primary key, by range, by relationship traversal, by full text search, or by time window? consistency needs does this data need strong consistency (inventory, payments) or is eventual fine (a like count, a feed)? scale what's the read/write volume, and does it fit on one well provisioned instance or genuinely need horizontal partitioning? query flexibility do you need ad hoc joins and filters, or a small, fixed set of known access patterns? operational cost who runs this in production — is there a managed offering, and does the team already know it? [!key] the senior answer is never "use x because it's popular." it's "given this access pattern and this consistency requirement, x is the closest fit, and here's what i give up by not using y." the big comparison table type data shape best access pattern consistency scale model examples relational (sql) rows in tables, fixed schema, relationships via foreign keys joins, transactions, ad hoc queries strong (acid) by default vertical first, then read replicas / sharding postgresql, mysql, azure sql document semi structured json/`,"system-design/scaling-and-statelessness":`"how would you scale this?" is asked in nearly every system design interview, and the honest answer is almost always "make the service stateless and add more of it — until the database can't keep up, which happens first." this page is about what statelessness really means, where state actually has to live, and why scaling reads and writes are different problems. vertical vs horizontal scaling vertical scaling horizontal scaling how bigger machine (more cpu/ram) more machines complexity trivial — no code changes needs load balancing, statelessness, coordination ceiling hard physical/cost limit on one box effectively unlimited, if the service is stateless downtime to scale often requires a restart/resize rolling — add nodes with zero downtime cost curve non linear — the biggest instances are disproportionately expensive roughly linear, closer to pay for what you use good first move yes — cheapest way to buy time do this once vertical scaling hits diminishing returns [!key] vertical scaling is the right first answer in an interview — "before i add complexity, i'd check whether a bigger box buys enough headroom" — but the real conversation is about horizontal scaling, because that's where statelessness, load balancing and database limits come in. what makes a service stateless, and where the state actually goes a stateless service keeps no request specific data in process memory or on local disk between requests — any instance can handle any request, which is what makes horizontal scaling (and simple round robin load balancing) possible at all. state doesn't disappear — it move`,"system-design/replication":"replication means keeping copies of the same data on multiple machines, and it is the foundation almost every other scaling and reliability technique builds on — read replicas, failover, geo distribution, and disaster recovery all start here. the interview conversation is really about which of three topologies you pick, and what breaks when the network between replicas misbehaves. the three topologies topology writes go to reads scale via conflict handling needed example single leader (default) one primary followers (replicas) no — one writer, no conflicts postgresql, mysql, mongodb (replica set) multi leader any of several leaders all leaders yes — concurrent writes to the same key can conflict multi region active active setups, couchdb leaderless any node (quorum based) any node yes — via versioning/quorum reconciliation dynamodb, cassandra, riak [!key] the number of leaders is the whole decision. one leader means no write conflicts but a single point of write scaling and failover complexity. more than one leader means writes scale and survive a region going down, but you now must resolve conflicting writes to the same key — there is no free lunch. synchronous vs asynchronous vs semi synchronous independent of topology, each replication link has its own durability/latency dial. mode leader waits for data loss risk (rpo) latency cost synchronous at least one follower to confirm before acknowledging the write none, for that follower write latency includes the round trip to the follower asynchronous (most common default) nothing — acknowledges immediately, replicates in the ","system-design/sharding-and-partitioning":'sharding is what you reach for once a single database genuinely cannot hold or serve your data, and it is one of the highest stakes decisions in a system design interview because the shard key choice is nearly impossible to change later without a painful migration. this page covers picking a key, the four partitioning strategies, and the problems sharding introduces that a single database never had. vertical vs horizontal partitioning vertical partitioning horizontal partitioning (sharding) splits by columns/tables rows example user profile table on one db, user activity logs on another users a–m on shard 1, n–z on shard 2 solves one table/feature is a hotspot; isolate it one table is too big/busy for any single machine cross shard complexity low — different tables, different concerns high — same logical table split across machines [!key] "sharding" specifically means horizontal partitioning — splitting rows of the same logical table across multiple machines. vertical partitioning (splitting by table/column) is a different, usually simpler, technique and often the first thing to try before sharding. choosing a shard key — the most important decision everything else in this topic is secondary to this choice, because resharding after data has grown is expensive and risky. property why it matters bad example good example high cardinality enough distinct values to spread across many shards (a few hundred values) , even distribution no single value dominates the dataset (most rows are "active") a well distributed hash of an id matches the query pattern most queries should hit on',"system-design/core-concepts":`every distributed systems page in this group leans on a shared vocabulary — words like reliability, redundancy, and fault tolerance get used precisely, not interchangeably, and a design that mixes them up reads as imprecise even when the diagram is fine. this page is that vocabulary: a fast concept to definition table you can scan before an interview, followed by the handful of ideas — the backend request path, the storage to latency hierarchy, and vertical versus horizontal scaling — that don't have a dedicated page of their own elsewhere in this group. [!key] precision with these words is itself a scoring signal. "the system should be reliable" says nothing; "the system should tolerate a single node failure without dropping a request" is a testable claim built from this vocabulary. concept quick reference term one line definition scalability the ability to handle more load by adding resources, not by rewriting the system maintainability how easily the system can be modified to fix bugs or add features without breaking unrelated parts efficiency doing the job with the least cpu, memory, and storage the job genuinely requires reliability performing correctly and consistently over time, including in the presence of failures redundancy duplicating a critical component so its failure doesn't take the system down with it fault tolerance the system keeps working despite a component failure; partition tolerance is the network failure special case spof (single point of failure) any one component whose failure alone takes down the whole system ci/cd continuous integration and conti`,"system-design/cap-and-consistency-models":'cap theorem gets misquoted in almost every interview that mentions it, usually as "pick two of three, always". the correct statement is much narrower, and the more useful interview skill is knowing the spectrum of consistency models between "always exactly right" and "eventually right" — because real systems rarely sit at either extreme. cap theorem, stated correctly cap says: in the presence of a network partition , a distributed data store must choose between consistency and availability — it cannot offer both. it says nothing about the normal, non partitioned case. guarantee meaning consistency (c) every read receives the most recent write, or an error — not stale data availability (a) every request receives a (non error) response — but not necessarily the latest data partition tolerance (p) the system keeps operating despite dropped/delayed messages between nodes [!key] the common misstatement is "you can only have two of c, a, p, so pick your two." the correct statement: partition tolerance is not optional in any real distributed system — networks will drop packets — so the actual choice is what happens during a partition : return an error/stale refusal (favor c) or return a possibly stale answer (favor a). outside of a partition, a well built system can be both consistent and available. pacelc — the more complete framing pacelc extends cap by also covering the normal, no partition case , which cap is silent on: p artition — trade off a vailability vs c onsistency; e lse (no partition) — trade off l atency vs c onsistency. system during partition (pa/pc) else (el/ec) d',"system-design/consensus-and-leader-election":`consensus is the problem of getting a set of unreliable machines, connected by an unreliable network, to agree on a single value or a single leader — and it underlies leader election, distributed locks, configuration stores, and metadata services throughout distributed systems. it is also a topic where interviewers mostly want to hear that you know the shape of the problem and the names of the tools, not that you can derive paxos from first principles. why consensus is hard the flp result (fischer, lynch, paterson, 1985) proves that in a fully asynchronous network — one with no bound on message delay — it is impossible for a consensus algorithm to guarantee both correctness and termination if even one node might fail. in plain english: you cannot build an algorithm that is guaranteed to always reach agreement in bounded time if the network might arbitrarily delay messages and you can't tell a slow node from a dead one. [!key] real systems escape flp not by disproving it, but by relaxing its assumptions : they use timeouts and heartbeats to treat "no response in time" as "probably failed" (accepting occasional wrong guesses), and they only need to make progress most of the time, not provably always. this is why raft/paxos systems can pause during a bad partition rather than violate correctness — they trade liveness for safety , never the reverse. quorums the core building block every consensus protocol uses is the majority quorum : with nodes, the system tolerates failures, because any two majorities out of nodes must overlap by at least one node — this overlap is what preve`,"system-design/distributed-transactions-and-saga":`a single service gives you atomicity for free. the moment "place order" touches an orders service, a payments service and an inventory service, that guarantee disappears — each service has its own database, and there is no shared transaction log across them. this page covers the tools for getting consistency back without pretending you still have acid. why acid across services is hard each microservice owns its own database (a hard rule — see the microservices page). a single business operation that spans three services means three separate local transactions, each of which can succeed or fail independently, with network calls in between that can also fail or time out. you cannot lock rows in another team's database, and holding a distributed lock across a multi second workflow kills throughput. [!key] there is no free lunch: you either pay with coordinator blocking (2pc), or with application level compensation logic (saga). almost everyone at scale chooses the second. two phase commit and why it doesn't scale two phase commit (2pc) is the classic distributed transaction protocol: a coordinator asks every participant to prepare (lock resources, vote yes/no), then tells everyone to commit or abort based on the votes. the blocking problem: once a participant votes "yes", it holds its locks until it hears back from the coordinator. if the coordinator crashes after collecting votes but before sending the decision, every participant sits blocked, holding locks, indefinitely — nobody else can act unilaterally because they don't know if the others committed. this is the textbook r`,"system-design/clocks-and-ordering":`"just use the timestamp" is one of the most common wrong answers in a system design interview. every machine's clock drifts, jumps and disagrees with every other machine's clock, so ordering events by wall clock time alone silently produces wrong answers. this page covers why, and the actual tools — lamport clocks, vector clocks, hybrid logical clocks and truetime — that let distributed systems agree on order without a shared clock. why wall clock time lies every server has a local clock that is synchronized to real time via ntp (network time protocol), but that synchronization is neither instant nor perfect. problem what happens clock drift cheap crystal oscillators gain or lose a few tens of milliseconds per day; two unsynchronized machines diverge continuously ntp correction jumps ntp periodically corrects drift, sometimes stepping the clock backwards — a timestamp can appear to go back in time leap seconds occasionally a minute has 61 seconds; naive clocks either jump or repeat a second unless "smeared" network delay asymmetry even a perfectly synced clock can't tell you "which event happened first" across machines within the sync error margin (typically 1–10 ms, worse across regions) [!warning] two events on two different machines with timestamps 5 ms apart cannot be reliably ordered — that gap is well within typical ntp sync error. treating as proof that event 1 happened first is a common and dangerous assumption. monotonic vs wall clock most languages expose two different clocks, and conflating them is a classic bug: clock type guarantees use for wall clock ( , ) tel`,"system-design/availability-and-slos":`"make it highly available" means nothing until it's a number. this page covers the vocabulary that turns reliability into something you can measure, alert on and negotiate — slis, slos, slas, the nines table, error budgets, and the maths of composing availability across dependencies. the nines table availability is usually expressed as a percentage of time a system is "up," and each additional nine is an order of magnitude less downtime. availability downtime / year downtime / month downtime / week 99% ("two nines") 3.65 days 7.2 hours 1.68 hours 99.9% ("three nines") 8.76 hours 43.2 minutes 10.1 minutes 99.95% 4.38 hours 21.6 minutes 5.04 minutes 99.99% ("four nines") 52.6 minutes 4.32 minutes 1.01 minutes 99.999% ("five nines") 5.26 minutes 25.9 seconds 6.05 seconds [!key] going from 99.9% to 99.99% is not "a bit better" — it's 10x less tolerance for downtime , and typically requires qualitatively different engineering (multi region failover, no single points of failure, automated remediation) rather than just "trying harder." sli, slo and sla — three different things these three terms are often used interchangeably in casual conversation, but they answer different questions and mixing them up is an easy interview stumble. term question it answers who it's for example sli (service level indicator) what are we actually measuring? engineering "percentage of requests completed in < 300 ms" slo (service level objective) what internal target do we hold ourselves to? engineering + product "99.9% of requests < 300 ms over a rolling 28 days" sla (service level agreement) what hav`,"system-design/latency-and-performance":`"average response time is 50ms" is close to meaningless in a system with real traffic. this page covers the vocabulary that makes latency conversations precise in an interview — percentiles, tail amplification, little's law, queueing behavior, and where the milliseconds actually go on a request's path. latency, throughput and bandwidth these three get conflated constantly, and an interviewer will notice if you don't separate them. term definition unit improving one doesn't fix the other latency time for one request to complete ms fast pipe doesn't mean fast individual response if the request itself is slow throughput requests (or bytes) processed per unit time req/s, mb/s high throughput can coexist with high per request latency if you parallelize enough bandwidth maximum data transfer capacity of a link mbps/gbps bandwidth is a ceiling on throughput, not a guarantee of low latency a classic analogy: a fleet of trucks moving cargo has huge throughput (tons/day) but terrible latency (days per shipment) — bandwidth and throughput can be scaled by adding trucks, but latency for any single shipment doesn't improve. percentiles, and why averages lie the average hides the shape of the distribution — and in production, the shape is almost never a symmetric bell curve; it has a long tail of slow requests dragging behind a cluster of fast ones. percentile meaning typical use p50 (median) half of requests are faster than this "typical" experience p90 90% of requests are faster than this reasonable slo target p99 99% of requests are faster than this where real user pain starts showing`,"system-design/fault-tolerance-and-disaster-recovery":`systems fail constantly at scale — disks die, networks partition, processes get oom killed, entire regions go dark. fault tolerance is not about preventing failure, it's about designing so that failure is contained, expected, and recoverable within a promised time and data loss window. this page covers the failure taxonomy, the resilience patterns, and the disaster recovery maths interviewers expect you to reason through. failure modes taxonomy not all failures look alike, and how a component fails changes what defenses actually work. failure mode what happens defense crash (fail stop) component stops entirely, cleanly health checks + restart/failover omission component silently drops a request/response without responding timeouts, retries timing component responds, but too slowly (violates sla without being "wrong") timeouts, hedged requests, load shedding byzantine component responds with incorrect or malicious data, possibly convincingly checksums, consensus quorums, signature verification grey failure component appears healthy on standard health checks but a subset of real requests fail synthetic/canary probes, per endpoint error tracking, not just liveness [!warning] grey failures are the ones that hurt most in practice: a node that answers fine but times out on 30% of real writes (a failing disk, a saturated connection pool) will happily keep receiving traffic from a load balancer that only checks liveness. health checks need to exercise a real code path, not just "process is running." redundancy strategies strategy description failover time cost n+1 one extra unit of`,"system-design/multi-region-architecture":`going multi region is one of the most expensive decisions in system design, and interviewers probe whether you'd reach for it reflexively or only when the problem genuinely demands it. this page covers the reasons to go multi region, the traffic routing and replication mechanics, and the failure modes — split brain, conflict resolution, data residency — that make it hard. why go multi region there are exactly three legitimate drivers, and naming them precisely (instead of "for scale") is what separates a senior answer. driver what it buys example latency serve users from a nearby region instead of one far away a user in singapore hitting a us only backend eats 200ms+ round trip before any real work happens availability survive a full region outage (power, network, natural disaster) aws/azure region wide outages happen a few times a year across the industry data residency / compliance keep certain users' data physically within a legal jurisdiction gdpr style requirements that eu citizen data stay in the eu [!key] "we might need to scale" is not a reason to go multi region — a single region with multiple availability zones already gives you strong fault isolation and horizontal scale. multi region is specifically for latency to distant users , surviving a whole region's failure , or legal data placement — say which one you're solving for. active passive vs active active vs read local write global model writes reads failover complexity cost active passive only in primary region only in primary (or replicas for read only) manual/automatic promotion, minutes low standby capacity`,"system-design/microservices-and-service-boundaries":'microservices are a way to trade complexity in the code for complexity in the network and organization — and that trade is only worth making when a specific organizational or scaling problem demands it. this page covers when to make that trade, how to find real boundaries using domain driven design, and the anti patterns that turn "microservices" into a distributed monolith with all the downsides of both worlds. monolith vs modular monolith vs microservices monolith modular monolith microservices deployment one unit one unit independent per service codebase single, often tangled over time single, with enforced internal module boundaries separate repos/codebases data one shared database one database, modules own their own tables/schemas each service owns its own database scaling scale the whole app together scale the whole app together scale each service independently team model works for one team, strains past a few teams works well for several teams sharing a runtime built for many independent teams failure isolation a bug can take down everything a bug can still take down everything (shared process) a failure is contained to one service (if done right) operational cost low — one thing to deploy/monitor low — one thing to deploy/monitor high — many things to deploy, monitor, version cross cutting changes trivial — one pr, one deploy trivial — one pr, one deploy hard — coordinated changes across services [!key] a modular monolith is the underrated middle option: enforce module boundaries (via internal apis, not shared mutable state) inside a single deployable, and you get m',"system-design/event-driven-architecture-and-cqrs":"event driven architecture and cqrs both start from the same observation: reads and writes have wildly different shapes and scaling needs, and forcing them through the same model is often what makes a system slow or rigid. this page covers the event vocabulary, cqrs, event sourcing, and the plumbing (cdc, outbox) that keeps it all consistent without dual writes. asynchronous processing: the simplest event driven pattern before cqrs or event sourcing enter the picture, most systems already lean on a simpler version of the same idea: handing work to a queue and letting a separate worker process it later, instead of making the caller wait for it to finish. {width=500} this decouples the producer from the consumer, and the payoff is the same one that motivates event driven architecture generally: the caller gets a fast response, a failure in the worker doesn't take the caller down with it, and workers scale independently of whatever produces the work. an image upload pipeline is the canonical example — the upload call returns immediately once the file is queued, and a pool of workers pulls from that queue to generate thumbnails, run a virus scan, and transcode formats, each at its own pace. {width=500} work handed off this way comes in a few different shapes, and naming which one you're building matters because it changes how the consumer is written: one off (a single task processed once, like sending a confirmation email), recurring (a scheduled job that runs on a timer, like a nightly report), chained (a sequence of dependent steps, like transcode → thumbnail → notify), and ba","system-design/architecture-patterns-cheatsheet":`this is a reference page, not a tutorial — every pattern below has its own body of theory elsewhere on this site (saga, cqrs, event sourcing, circuit breakers all have dedicated pages), but interviews often move fast enough that you need the one line "problem it solves / when to use / when not to" recall without re deriving it. use this to jog your memory mid interview, and to sanity check that you're reaching for the right tool rather than the first one that comes to mind. the pattern reference table pattern problem it solves when to use when not to real example api gateway clients calling n services directly means n client integrations, n auth checks, n places to rate limit many clients, many backend services, need a single entry point for auth/routing/rate limiting a single monolith with one client — there's nothing to gateway netflix zuul, azure api management, kong backend for frontend (bff) one api gateway serving mobile, web, and partner clients ends up with a bloated, compromise shaped contract genuinely different clients need different data shapes/aggregations from the same backend services only one client type exists, or clients' needs are nearly identical a mobile bff returning trimmed payloads vs a web bff returning full detail sidecar cross cutting concerns (mtls, logging, retries) duplicated in every service's code, per language polyglot services need consistent infra behavior without per language libraries a single language shop where a shared library achieves the same thing more simply envoy proxy sidecar in a service mesh ambassador a service needs to talk `,"testing/test-strategy-and-the-pyramid":'every team has a test suite; few have a test strategy . the pyramid is the classic model for how much of each test type to write, and interviewers use it to see whether you can reason about cost, speed and confidence together instead of chasing a coverage number. the pyramid, the trophy, and the ice cream cone the pyramid says: many fast unit tests, fewer integration tests, a handful of end to end (e2e) tests. the testing trophy (kent c. dodds) argues that for typical web apps, integration tests give the best return and should be the largest layer, with a thinner unit layer underneath. the ice cream cone is the anti pattern: mostly manual and e2e tests, few unit tests — slow, flaky, and expensive to maintain. [!key] the shape is not the goal. the goal is fast feedback with enough confidence to ship . the pyramid is just the shape that usually achieves that for services with real business logic. what each layer costs layer typical run time (per test) maintenance cost confidence given failure signal unit 1–10 ms low low–medium (one function/class is correct) precise — points at exact code integration 50 ms–2 s medium medium–high (components cooperate correctly) fairly precise — points at a boundary end to end 2–60 s high highest (the system works for a user) vague — points at "somewhere" a thousand unit tests can run in seconds; a thousand e2e tests would take hours and would be a maintenance nightmare of selectors, timing and environment drift. that asymmetry, not dogma, is why the pyramid is unit heavy by default. the confidence versus speed trade off priority favour why fa',"testing/unit-testing-principles":`anyone can write a test that passes today. the interview question underneath "do you write unit tests" is really "do you write unit tests that stay useful in six months, survive a refactor, and fail with a message that tells you exactly what broke". what a "unit" actually is a unit is a piece of behaviour , not a class or a method. a single public method can require several tests to describe its behaviour fully, and a single test can legitimately span several private methods reached through one public entry point. the line is drawn at the public contract you're willing to keep stable — not at "one class equals one test class" mechanically. [!key] test the behaviour visible through the public api. if you can rename or restructure private methods without changing what the caller observes, your tests should not need to change either. arrange act assert the near universal structure for a unit test's body. it separates setup, the action under test, and the check, so a reader can scan a test in seconds. keep the sections visually separated (blank line or comment) even in a three line test — it's a habit that pays off the moment the test grows. one logical assertion per test "one assertion" doesn't mean literally one call — it means one reason to fail . asserting several properties of the same resulting object is fine; asserting unrelated behaviours in one test is not. if the bundled test fails, the name and the failure line don't tell you which behaviour broke without reading the whole body. naming as a specification a good test name should let someone read a failure report and k`,"testing/test-doubles":`"mock" is used loosely to mean any stand in object, but interviewers listen for whether you know there are five distinct kinds of test double, each with a different purpose. confusing them is how test suites end up asserting on things that don't matter and breaking on refactors that shouldn't matter. the five types type purpose returns/verifies fails the test if... dummy fills a required parameter, never actually used nothing — often or never — it's just there to satisfy a signature stub supplies canned answers to calls made during the test pre programmed return values you assert on the state it helped produce spy records how it was called, for later inspection call history you inspect yourself you manually assert on the recorded calls mock pre programmed with expectations; verifies interactions nothing meaningful returned — its job is verification the expected call didn't happen ( fails) fake a working, lighter weight implementation real (simplified) behaviour state ends up wrong, same as testing the real thing one example of each, in c [!key] moq's class can technically produce a stub, a spy or a mock depending on whether you call only, inspect , or call . the library doesn't distinguish them — your usage does. state verification versus behaviour verification state verification asks: "after the action, is the world in the right shape?" you inspect the result or the final state of an object — this is what stubs and fakes support. behaviour verification asks: "did the code under test call its collaborators correctly?" you verify a specific interaction happened — this is wha`,"testing/integration-testing":"integration tests prove that components which work in isolation actually cooperate — the wiring, the sql, the serialization, the middleware pipeline. they sit deliberately between the speed of unit tests and the realism of end to end tests, and getting that boundary right is most of what makes them worth their cost. what counts as an integration test an integration test exercises two or more real components together , crossing at least one boundary a unit test would fake — a database, an in process http pipeline, a message broker, the file system. the key distinction interviewers probe for is narrow versus broad. scope narrow integration test broad integration test what it touches one service + one real dependency (e.g., repository + real database) multiple services, or a service through its full external boundary (api in, db + queue + cache all real) speed fast (100ms–1s) slower (seconds) failure signal precise — points at the one boundary vague — could be any of several components where it runs every ci build ci, maybe a dedicated stage [!key] prefer narrow integration tests. test one boundary at a time — repository against a real database, message handler against a real broker — rather than wiring everything together, which pushes you toward e2e cost without e2e level confidence. in process api testing with webapplicationfactory boots your asp.net core app in memory, including the full middleware pipeline, routing, model binding and filters, without a real network socket or a deployed process. this exercises real routing, real model binding, real ef core queries against ","testing/contract-testing":`once a system has more than a couple of services, proving they still work together becomes the hardest testing problem you have. full end to end tests across every service answer that question honestly but slowly and flakily; contract testing answers a narrower version of it — "do we still agree on the shape of our interaction" — cheaply and on every commit. the problem with cross service end to end tests to e2e test service a calling service b, you need both deployed, both configured, both seeded with compatible data, and the network path between them working — for every commit, on every branch. as the number of services grows, the number of pairwise interactions grows faster, and a flaky test in any dependency blocks everyone's pipeline. [!key] contract testing decomposes "does the whole system work together" into "does each consumer's expectation match each provider's actual behaviour" — checked independently, without either side needing the other running. consumer driven contracts and the pact workflow in consumer driven contract testing, the consumer (the service making the call) writes a test describing exactly what it expects from the provider (the service being called) — specific requests and the responses it needs. running that test produces a contract (a pact file: json describing the interactions). the provider then replays those exact requests against its real implementation and verifies its real responses match what the consumer expects. provider verification in ci, and the broker the pact broker is the shared store: consumers publish contracts to it, providers`,"testing/end-to-end-testing":`end to end tests are the most convincing test you can write — they exercise the real system the way a user would — and also the most expensive to keep healthy. the interview signal here is knowing exactly why e2e suites rot and having concrete fixes, not just "e2e tests are slow and flaky" as an observation. what belongs at this level e2e tests should cover a small number of critical user journeys — the paths that generate revenue, that legal/compliance care about, or whose failure is a front page incident. sign up, login, checkout, and password reset are typical candidates; "every combination of filter and sort on a search page" is not. [!key] if you can name the specific business or safety reason a journey must never break, it belongs in the e2e suite. if the answer is "just to be thorough," it belongs at a lower, cheaper layer. the cost curve and why e2e suites rot e2e tests cost more per test than any other layer, and that cost compounds: they're the slowest to run, the most sensitive to ui and environment changes, and the hardest to debug on failure because so many real components are involved. layer cost to write cost to maintain as ui changes cost to debug a failure unit low low low (precise) integration medium low–medium medium e2e medium–high high — breaks on unrelated ui/selector changes high (many moving parts) left unmanaged, e2e suites grow linearly with every new feature (each adds "just one more test") while maintenance cost grows faster than that, because every ui refactor touches many tests at once. eventually the suite takes hours, flakes constantly, and g`,"testing/load-and-performance-testing":'performance testing is where a lot of engineers say correct sounding words ("we tested at 1000 rps") without understanding what the number actually measured. the interview signal is precision: which test type, which workload model, which metric, and what you did with the result. the six types of performance test type question it answers typical shape smoke does the system work at all under a trivial load? very low load, short duration, run after every deploy load does the system meet its slos at expected/peak traffic? realistic load profile, sustained for minutes stress where does the system break, and how does it fail? load increased past expected peak until errors/degradation appear spike can the system survive a sudden, large jump in traffic? near instant jump from baseline to high load, then back down soak (endurance) does the system degrade over long sustained operation? moderate load sustained for hours/days breakpoint (capacity) what is the exact maximum throughput before failure? load ramped gradually until the system fails, to find the ceiling [!key] each type answers a different question — running only "load" tests and calling it "performance testing" leaves soak related bugs (memory leaks, connection pool exhaustion, log disk fill up) and stress related failure modes (does it degrade gracefully or fall over entirely) completely uncovered. defining a workload model from real traffic a performance test is only as good as its workload model — the shape of requests it replays. pull the model from production observability, not guesswork: request mix — the real ratio o',"testing/chaos-and-resilience-testing":`chaos engineering has a reputation for "randomly breaking production," which is precisely backwards — done properly it is one of the most rigorous, scientific testing disciplines in this whole list. the interview signal is whether you can describe it as a controlled experiment with a hypothesis, not a stunt. the hypothesis driven method chaos engineering follows a fixed method, deliberately borrowed from the scientific method, so that an experiment produces a clear answer rather than just noise. 1. steady state — define measurable normal behaviour first (error rate, latency, throughput), not vague "the system feels fine." 2. hypothesis — state what you believe will happen: "if the payments service loses connectivity to its database for 30 seconds, checkout latency will rise but error rate will stay under 1% because of the retry with fallback logic." 3. inject — introduce the fault, ideally the smallest version that would still test the hypothesis. 4. observe — watch the same steady state metrics during and after the fault. 5. learn — the hypothesis was right (confidence gained), or wrong (a real gap found before a customer found it) — either is a good outcome. [!key] the point of chaos engineering isn't to break things — it's to find out whether your belief about how the system handles failure matches reality, under conditions you control, before reality tests it for you at 3 a.m. starting small and in non production chaos engineering is a maturity ladder, not a single leap to "kill random production servers." stage where example 1 local/dev kill a dependency container, see`,"xbox-systems/guide-overview":"a field guide to the platform behind the xbox store: the cloud pieces, how code ships, how it's watched, how services talk securely, and deep dives into the systems this guide documents. it is built for fast understanding — short explanations, plenty of diagrams, and one consistent five layer mental model that applies to every system covered here, so a single mental picture carries across dozens of otherwise unrelated services. [!key] everything in this guide reduces to the same five layers: people and clients, front doors, core services, data and messaging, and the platform that runs and watches all of it. learn that shape once and every individual system becomes a variation on a theme instead of a new thing to memorise. how to use this guide four foundation pages come before any individual system, and each one answers a different kind of question an interviewer is likely to ask. page what it covers how it all fits together the azure building blocks, how code ships through ci/cd and gitops, how autoscaling controllers keep services right sized, how observability and alerting work, and how services trust each other concepts a to z a plain english glossary — every technology and acronym used anywhere in the guide, with a short explanation and a concrete in practice example systems and tools eight deep dives, one per system this guide's author owns or co owns, each with an architecture diagram, its dependencies, and interview talking points project and behavioural q and a likely project, architecture, leadership, and behavioural questions, grounded in real project history rat","xbox-systems/how-it-all-fits-together":`one journey from a player's tap to the cloud and back. we'll follow a single request end to end, then peel back each platform layer underneath it: the azure building blocks, how code ships, how it auto scales, how it's watched, and how services trust each other. every one of the 220 services in this estate is built from the same handful of pieces described below — azure, aks and kubernetes, flux gitops, prometheus and grafana, and managed identity — so this page is worth reading once, carefully, rather than once per system. 1. follow a single request everything below exists to make this one trip fast, safe, and reliable. a request enters through the istio gateway, is authenticated by a front door (fd) service, forwarded with a service to service token to a core service, which reads and writes data and emits events. front door (fd) the public face. handles auth (entra id / aad), routing, feature flags and a/b flighting. example: , . core the brain. pure business logic in clean architecture layers. trusts callers via s2s tokens. example: , . data & messaging cosmos db for documents, redis for speed, service bus for async work, sql where relational fits. 2. inside a service — clean architecture every one of the 220 c /.net 8 services is built from the same four layers. learn it once and you understand the shape of all of them. controllers stay thin; business logic holds the rules; domain data aggregates; repositories touch storage. supporting cast: validators, static mappers, and "fabric clients" — typed sdks used to call other services. [!key] most systems ship as a pair: a c`,"xbox-systems/concepts-a-to-z":"every technology and acronym used anywhere in this guide — 96+ concepts across eight categories — with a plain english meaning and a concrete in practice example. this is the fallback page whenever an unfamiliar term surfaces mid conversation in one of the system deep dives. the concept map the eight categories are not separate lists — they form one operating system for taking a client request, doing useful work, and keeping that work safe and reliable. solid arrows show the request and data path; dotted arrows show the platform systems that deploy, scale, observe, and increasingly help operate that path. [!key] every term below belongs to exactly one of these eight categories, chained in the order above — client, security, runtime, state and messaging, watched by observability, changed by delivery, assisted by ai tooling. data & storage a typical read uses redis for speed and cosmos for durable state; writes use concurrency checks and can fan out through the change feed. cache aside connects redis and cosmos; partitioning and ru/s govern scale; etags protect writes; the change feed drives reactions. term meaning, in practice cosmos db globally distributed nosql document store, replicated across regions — primary store for xevents, xgem, xproduct redis in memory key value store, microsecond lookups — xevents caches feed pages in azure managed redis via managed identity sql server relational tables, joins, strong transactions — xsale keeps sales records in azure sql via ef core partition key field deciding which physical chunk stores a record; a bad key creates a hot partiti","xbox-systems/project-and-behavioural-q-and-a":`the stories below are grounded in real review feedback spanning july 2020 through may 2026, covering end to end ownership, azure services, full stack delivery, security, reliability, ai assisted engineering, cross team influence, mentoring, inclusion, and lessons from setbacks. each one is built to deliver in roughly 60–90 seconds, with a clear headline, the scale or constraint involved, the decisions actually made, and a measured result. [!warning] use these as speaking notes, not a script. keep every number exactly as stated. say "i led" for direction, design, and coordination, and "we delivered" for the team outcome. if asked for implementation detail, explain only the part personally designed, built, reviewed, or operated — and be precise about the difference between a shipped result, a design, a prototype, and a future target. the answer formula every strong answer converts a résumé claim into an ownership story rather than a list of technologies: state the headline in the first sentence, give the context (scale, constraint, or customer problem), explain two or three decisions made personally, and close with a measured result . your strongest story bank the same handful of stories answer nearly every question below — only the emphasis changes depending on what's actually asked. story best questions to answer evidence to remember xevents clubs deprecation ownership, ambiguity, cross team leadership, migration, launch readiness hybrid clubs/cosmos flow; p0/p1 plan; new follow apis; 4–5 partner teams; halo first onboarded title; test and go live plans official clubs news `,"xbox-systems/xevents-and-xguide":"xevents and xguide are the two halves of the xbox engagement platform. publishers use xevents to announce season launches, tournaments and limited time sales; xguide serves those events back to players across every xbox surface. the platform now reaches more than 7 million monthly players through 70 plus onboarded publishers, and it is also the foundation a newer initiative — the news feed system — was built inside of rather than shipped as a separate service. what it is the platform splits cleanly into a write side for publishers and a read side for players, and both sit on top of the same core service. write side — authoring publishers use the web events editor to create, schedule, localize and manage events. it calls , the self serve front door, which routes into the core business logic. every action — draft, publish, unpublish, delete — passes through the same self serve pipeline, so the write path never touches the player facing front door directly, and a bad publisher request can't degrade reads. read side — discovery serves the read apis that player surfaces query directly: featured, active, coming soon, ending soon, events filtered by product, and follow/unfollow state. it is backed by cosmos db for durable storage and a redis cache in front of it, which is what makes it possible to serve reads at close to 5,000 requests per second without hammering cosmos on every call. [!note] a separate initiative, the news feed system, migrates the 600 plus publisher official clubs news feed into xevents as a new news post content type, with its own cosmos containers, a versione","xbox-systems/news-feed-system":`the xbox live clubs service that powers today's publisher news feed is being deprecated, so rather than build a new service, the feed is being migrated into xevents by adding a news post content type alongside the existing events. it is a content channel where 600 plus game publishers push news — announcements, patch notes, images, links — onto product detail pages across every xbox surface, unifying events and news posts on one platform instead of running two. what it is the feed is the "official clubs news feed" reborn on xevents, keyed by game titleid instead of club membership. write side — publishers publishers author image and external link posts in the events editor spa — with pinning, scheduling, inline locale descriptions, and multi title association. authoring moves off the legacy xblc tool entirely, so publishers get one authoring surface for both events and news. read side — players players see a scrollable feed, pinned post on top, on a game's product detail page, plus post details, likes and view counts. club branding is replaced by product name and box art; comments, shares and follow are deliberately dropped from the mvp scope. [!note] this system is built inside the parent xevents service rather than as its own deployment — it reuses xevents' front doors, cosmos account, redis cache and worker infrastructure. see the xevents and xguide page for the platform this sits on top of. architecture everything reduces to three flows: publisher authoring, player feed reads, and an async engagement pipeline for likes and views. (1) a publisher authors in the events ed`,"xbox-systems/certextensibility":`certextensibility is the certification backend behind xshield, xbox's automated content certification engine. every game submission is scanned for unsafe, copycat, malicious or non compliant content, and what used to take roughly 1.7 hours of manual review per title now takes seconds — across more than 150 titles a day, both before and after launch. what it is it is an event driven pipeline that runs automated checks on submissions and routes hard cases to human reviewers, rather than trying to fully automate every decision. pre launch when a publisher submits in partner center, smart cert triggers xshield. automated scans run, and decisions flow back as allow, hold, take down or notify — a synchronous feeling gate at the point of submission. post launch live games are re scanned continuously, on a daily cadence, to catch new threats that didn't exist at submission time: malware discovered after release, copycat titles that launched later, pricing arbitrage, and hidden games. [!note] certextensibility is triggered by, and reports back into, other systems on this platform — partner center submissions and smart cert on the way in, xpmc and cmx on the way out. it is the automated judgment layer sitting between "a publisher submitted something" and "the store allowed, held or took it down." architecture a submission flows from smart cert into certexcore. workflows run static rule checks plus ai detection, persist results in cosmos, escalate hard cases to xcert reviewers, and publish the final decision to xpmc over service bus. direction system why trigger partner center + smart`,"xbox-systems/xservicesinsights":`xservicesinsights (internally "coral", reached at ) is the internal developer productivity and live site operations platform for a 600 plus microservice estate. it gives 130 plus engineers across 10 plus services one governed console for a gateway proxy, data explorers, workflow investigation, aks management and s2s tokens — with rbac, just in time access, and a read only default — instead of a scattered toolbox of one off scripts. what it is a front door, , plus a devtools ui that collapses a scattered toolbox into one governed console. tool what it does gateway proxy safely calls service apis — import a curl, save and replay requests — without hand crafting s2s auth each time data explorers read only cosmos and sql explorers to inspect records live while debugging an incident workflow investigation traces long running jobs (for example xpackage): recursive tree, history, job details, requeue or stop aks management cluster diagnostics — overview, list pods, events, describe resources, pod logs, service mesh traffic s2s tokens generates service to service tokens on demand for authorized debugging xpci copilot an embedded natural language assistant to ask questions over icm, prometheus and kubernetes in plain english [!note] xservicesinsights is not the same thing as nexus hub. xservicesinsights is the governed, rbac gated platform for inspecting apis, data stores, workflows and aks across the whole estate. nexus hub is an individual engineer's local, configurable daily dashboard that can surface some of that operational context alongside ado, icm and personal notes. see the`,"xbox-systems/nexus-hub":"nexus hub is a local developer dashboard and control center that brings engineering, planning, operations, ai assisted review and personal workflow into one customizable workspace. it ships as a copilot cli plugin: a react and typescript single page app backed by an express server, with persistent tabs and draggable, resizable widgets, built around 15 registered widgets across 4 categories. what it is widgets are grouped by the kind of decision they help an engineer make, and any widget type can appear more than once with different configuration. engineering pipeline builds, pr reviews, build trends, repository activity, copilot assisted pr review, and design document review. planning work items, sprint burndown, and team velocity turn ado backlog data into a compact delivery view. operations icm incidents, on call schedules, and deployment status keep live site and release context visible. personal utilities meetings, quick links, sticky notes, and streaming copilot chat support the engineer's daily workflow. [!note] nexus hub is not the same thing as xservicesinsights. nexus hub is an individual engineer's configurable daily dashboard, running locally, combining ado, icm, meetings, repositories, agents and personal notes. xservicesinsights is the shared, governed platform for inspecting service apis, data stores, workflows and aks across the whole estate. nexus hub can surface operational context; xservicesinsights is where the deeper service operations tooling lives. architecture the browser renders independently configured widget instances. express resolves each widget ","xbox-systems/xsale-web":'xsale web is the sales authoring web portal for xbox commerce: a react spa plus a backend for frontend, used by marketing and business teams to create discount campaigns. the portal itself is deliberately stateless — it owns the ux, an ai draft assistant, csv upload and analytics, but delegates all business logic and persistence to the separate xsale (xsalecore) backend, which turns sales into offers that xprice evaluates in real time. what it is — three layers authoring happens in the portal, the backend defines and publishes offers, and xprice enforces them at the moment a player sees a price. 1. xsale web — portal and bff a react 18 spa (typescript, partner center ui) with a thin backend for frontend that handles the ux, an ai draft assistant, csv upload, and analytics dashboards. it is stateless: it calls the xsalecore backend for everything and keeps no database of its own. 2. xsale (xsalecore) — backend the core service that owns the sale business logic, sql and cosmos persistence, and transforms sales into offers (via xoffercore), tracking publish state over service bus. 3. xprice — evaluation evaluates the price a specific user sees, in roughly 50ms, across discovery, the product page, cart and purchase — using eligibility signals sourced from xtarget. [!note] "xsale web" is the portal and bff, not the backend. it has no database of its own; all sale logic, validation and persistence live in the separate xsale/xsalecore backend it calls. the rest of this page covers the portal first, then the backend it orchestrates. architecture teams author in the xsale web spa; i',"xbox-systems/xgem-and-product-configuration":"xgem and productconfiguration are the metadata and product properties backbone of publishing. xgem is the source of truth for gaming metadata; productconfiguration manages the product properties, listings and pricing modules. together they shape what appears on a game's store page and feed the wider catalog, powering more than 150 game publishes a day. what they are xgem — gaming metadata the source of truth for gaming metadata: tags like accessibility, languages, and gameplay features. on commit, it publishes committed instance updates to xpmc and xproduct so the store reflects changes as soon as a publisher finalizes them. productconfiguration — properties manages gamingproperties modules — genre, modes, tech specs, xbox services — in draft state, with callbacks for jaguar and submission. it stores listings, properties and pricing in cosmos, and uses service bus for async events. architecture publishers author properties and metadata in partner center. productconfigurationcore manages property modules; xgemcore owns metadata and publishes committed changes on to xpmc/xproduct, which surface on the store pdp. direction system why authoring ui partner center client apps (packages, properties, metadata) publisher input callers partnercentercore, submissionserviceclient, cmsfabricclient submission and content context publishes to xpmc / xproduct via service bus propagate committed metadata to catalog callbacks jaguar + submission (productconfiguration) publishing flow integration stores in cosmos db ( ; listings/properties/pricing) draft and committed state downstream xproduc","xbox-systems/xsale-self-serve":"xsale self serve lets publishers create their own discounts instead of emailing xbox and waiting weeks for approval. it is a self serve dashboard to build, schedule, target and track sales on the xbox store, backed by an xbox reviewer queue, and it is the first app in the xmt family. discounts built through it now drive more than $1 billion in discounted third party revenue. what it is the self serve front of the pricing platform: publishers author, xbox reviews, and approved sales become offers that xprice evaluates. for publishers create percentage off, edition upgrade and flexible bundle sales. target audiences using five predicates — ownership, play history, game pass, and wishlist among them. schedule up to 12 months out, and bulk upload campaigns via excel. for xbox reviewers a dedicated review queue with filters, bulk approvals, product level review and a full validation and audit trail. a publisher submits, xbox decides, and both sides track live status from the same underlying record. [!note] xsale web and xsale self serve share the same pricing platform but have different authors. xsale web is used by internal marketing and business teams to author campaigns. xsale self serve is used by publishers to author their own discounts, gated by an xbox review step and rule engine validation. both ultimately produce offers evaluated by xprice. architecture publishers author in the self serve sales app behind the xmt front door, with rbac enforced on every call. validates via a rule engine, persists to sql, and on approval creates offers (xoffercore) that xprice evaluates. "};export{e as default,e as searchIndex};
